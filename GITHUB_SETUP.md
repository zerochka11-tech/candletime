# 📦 Пошаговая инструкция: Подключение проекта к GitHub

## Шаг 1: Установка Git (если еще не установлен)

### Проверка установки Git
Открой терминал (PowerShell в Windows) и выполни:
```bash
git --version
```

Если Git не установлен:
1. Скачай Git с [git-scm.com](https://git-scm.com/download/win)
2. Установи с настройками по умолчанию
3. Перезапусти терминал

---

## Шаг 2: Настройка Git (первый раз)

Выполни эти команды один раз (замени на свои данные):

```bash
git config --global user.name "Твое Имя"
git config --global user.email "твой-email@example.com"
```

**Пример:**
```bash
git config --global user.name "Иван Иванов"
git config --global user.email "ivan@example.com"
```

---

## Шаг 3: Создание репозитория на GitHub

### 3.1. Регистрация/Вход
1. Зайди на [github.com](https://github.com)
2. Зарегистрируйся или войди в аккаунт

### 3.2. Создание нового репозитория
1. Нажми на **"+"** в правом верхнем углу
2. Выбери **"New repository"**
3. Заполни форму:
   - **Repository name:** `candletime` (или другое название)
   - **Description:** `Символические свечи онлайн` (опционально)
   - **Visibility:** выбери **Public** (или Private, если хочешь приватный)
   - **НЕ ставь галочки** на:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   (у тебя уже есть эти файлы)
4. Нажми **"Create repository"**

### 3.3. Копирование URL репозитория
После создания GitHub покажет страницу с инструкциями. Скопируй URL репозитория:
- Он будет выглядеть так: `https://github.com/твой-username/candletime.git`
- Или: `git@github.com:твой-username/candletime.git`

**Сохрани этот URL!** Он понадобится в следующем шаге.

---

## Шаг 4: Инициализация Git в проекте

### 4.1. Открой терминал в папке проекта
1. Открой PowerShell или Command Prompt
2. Перейди в папку проекта:
   ```bash
   cd C:\Projects\online-candles
   ```

### 4.2. Проверь, что Git еще не инициализирован
```bash
git status
```

Если увидишь ошибку "not a git repository" — значит Git еще не инициализирован, продолжай.

Если Git уже инициализирован, переходи к Шагу 5.

### 4.3. Инициализация Git
```bash
git init
```

Должно появиться: `Initialized empty Git repository in C:\Projects\online-candles\.git`

---

## Шаг 5: Добавление файлов в Git

### 5.1. Проверь статус
```bash
git status
```

Ты увидишь список файлов, которые еще не добавлены в Git.

### 5.2. Добавь все файлы
```bash
git add .
```

Эта команда добавит все файлы в staging area (кроме тех, что в `.gitignore`).

### 5.3. Создай первый коммит
```bash
git commit -m "Initial commit: CandleTime project"
```

Должно появиться сообщение о создании коммита.

---

## Шаг 6: Подключение к GitHub

### 6.1. Добавь remote репозиторий
Замени `твой-username` и `candletime` на свои значения:

```bash
git remote add origin https://github.com/твой-username/candletime.git
```

**Пример:**
```bash
git remote add origin https://github.com/ivan123/candletime.git
```

### 6.2. Переименуй ветку в main (если нужно)
```bash
git branch -M main
```

### 6.3. Проверь подключение
```bash
git remote -v
```

Должно показать:
```
origin  https://github.com/твой-username/candletime.git (fetch)
origin  https://github.com/твой-username/candletime.git (push)
```

---

## Шаг 7: Загрузка кода на GitHub

### 7.1. Отправь код на GitHub
```bash
git push -u origin main
```

### 7.2. Авторизация
GitHub попросит авторизоваться:
- Если используешь HTTPS, может потребоваться Personal Access Token
- Или используй GitHub Desktop для удобной авторизации

**Если возникла ошибка авторизации:**

#### Вариант 1: Personal Access Token
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выбери scope: `repo` (полный доступ к репозиториям)
4. Скопируй токен
5. При запросе пароля вставь токен вместо пароля

#### Вариант 2: GitHub Desktop (проще)
1. Скачай [GitHub Desktop](https://desktop.github.com/)
2. Войди через GitHub Desktop
3. Используй его для push

---

## Шаг 8: Проверка

1. Обнови страницу репозитория на GitHub
2. Ты должен увидеть все файлы проекта
3. Готово! 🎉

---

## 📝 Полезные команды Git

### Проверка статуса
```bash
git status
```

### Добавление изменений
```bash
git add .
git commit -m "Описание изменений"
```

### Отправка на GitHub
```bash
git push
```

### Получение изменений с GitHub
```bash
git pull
```

### Просмотр истории коммитов
```bash
git log
```

---

## 🆘 Решение проблем

### Проблема: "fatal: not a git repository"
**Решение:** Выполни `git init` в папке проекта

### Проблема: "fatal: remote origin already exists"
**Решение:** Удали старый remote и добавь заново:
```bash
git remote remove origin
git remote add origin https://github.com/твой-username/candletime.git
```

### Проблема: "error: failed to push"
**Решение:** 
1. Проверь авторизацию (см. Шаг 7.2)
2. Убедись, что репозиторий существует на GitHub
3. Проверь URL репозитория

### Проблема: "Permission denied"
**Решение:**
1. Проверь, что ты авторизован на GitHub
2. Используй Personal Access Token вместо пароля
3. Или используй GitHub Desktop

---

## ✅ Чеклист

- [ ] Git установлен и настроен
- [ ] Создан репозиторий на GitHub
- [ ] Git инициализирован в проекте (`git init`)
- [ ] Файлы добавлены (`git add .`)
- [ ] Создан первый коммит (`git commit`)
- [ ] Remote добавлен (`git remote add origin`)
- [ ] Код загружен на GitHub (`git push`)
- [ ] Файлы видны на GitHub

---

## 🎯 Следующий шаг

После успешной загрузки кода на GitHub:
1. Переходи к деплою на Vercel
2. Следуй инструкции из [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

