# ⚙️ Быстрая настройка админ-панели

## 1. Переменные окружения

Добавьте в `.env.local`:

```env
# Supabase Service Role Key (для API routes)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Email адреса администраторов (через запятую, БЕЗ пробелов)
NEXT_PUBLIC_ADMIN_EMAILS=admin@example.com,another-admin@example.com
```

### Где найти Service Role Key?

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте **service_role** key (⚠️ НЕ anon key!)

**Важно:** Service Role Key обходит все RLS политики. Храните его в секретах!

## 2. Добавление администратора

Отредактируйте `.env.local`:

```env
NEXT_PUBLIC_ADMIN_EMAILS=your-email@example.com
```

Для нескольких администраторов:
```env
NEXT_PUBLIC_ADMIN_EMAILS=admin1@example.com,admin2@example.com,admin3@example.com
```

## 3. Проверка доступа

1. Убедитесь, что вы вошли в систему (email должен совпадать с одним из админов)
2. Откройте `/admin/articles`
3. Если видите список статей — всё работает! ✅

## 4. Первый запуск

```bash
# Установите зависимости (если еще не установлены)
npm install

# Запустите dev сервер
npm run dev

# Откройте в браузере
open http://localhost:3000/admin/articles
```

## 📚 Дополнительная документация

Полное руководство: [ADMIN_PANEL_GUIDE.md](./ADMIN_PANEL_GUIDE.md)

