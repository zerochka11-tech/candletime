# 🚀 Комплексный план улучшения SEO для CandleTime

**Дата анализа:** 2025-01-27  
**Версия проекта:** 0.1.0

---

## 📊 Текущее состояние SEO

### ✅ Уже реализовано:

1. **Базовые мета-теги**
   - ✅ Open Graph теги на всех страницах
   - ✅ Twitter Cards на всех страницах
   - ✅ Canonical URLs
   - ✅ Динамические мета-теги для страниц свечей
   - ✅ Google Search Console verification

2. **Структурированные данные (JSON-LD)**
   - ✅ Schema.org Organization (глобально)
   - ✅ Schema.org WebSite (главная страница)
   - ✅ Schema.org CreativeWork (страницы свечей)
   - ✅ Schema.org BreadcrumbList (навигация)
   - ✅ Schema.org ItemList (страница /candles)

3. **Техническое SEO**
   - ✅ Sitemap.xml (динамический, включает активные свечи)
   - ✅ Robots.txt (с правилами для разных ботов)
   - ✅ Адаптивный дизайн
   - ✅ Язык сайта (`lang="ru"`)

4. **Контент**
   - ✅ Уникальные описания для каждой страницы
   - ✅ Семантическая HTML разметка

---

## 🎯 План улучшений (по приоритетам)

### 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ (Немедленно)

#### 1. Оптимизация производительности (Core Web Vitals)

**Проблема:**
- Нет оптимизации изображений (если будут добавлены)
- Нет lazy loading для компонентов
- Возможны проблемы с LCP (Largest Contentful Paint)
- Нет оптимизации шрифтов

**Решения:**

1. **Оптимизация изображений:**
   ```typescript
   // Использовать next/image для всех изображений
   import Image from 'next/image';
   
   // В next.config.ts добавить:
   images: {
     formats: ['image/avif', 'image/webp'],
     deviceSizes: [640, 750, 828, 1080, 1200],
     imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
   }
   ```

2. **Lazy loading компонентов:**
   ```typescript
   // Для тяжелых компонентов
   import dynamic from 'next/dynamic';
   
   const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
     loading: () => <Skeleton />,
     ssr: false, // если не нужен SSR
   });
   ```

3. **Оптимизация шрифтов:**
   ```typescript
   // В layout.tsx
   import { Inter } from 'next/font/google';
   
   const inter = Inter({
     subsets: ['latin', 'cyrillic'],
     display: 'swap',
     preload: true,
   });
   ```

4. **Prefetching для навигации:**
   ```typescript
   // Next.js автоматически prefetch'ит Link компоненты
   // Убедиться, что все важные ссылки используют <Link>
   ```

**Файлы для изменения:**
- `next.config.ts` - настройка изображений
- `app/layout.tsx` - оптимизация шрифтов
- Все страницы - lazy loading тяжелых компонентов

**Ожидаемый результат:**
- LCP < 2.5s
- FID < 100ms
- CLS < 0.1
- Lighthouse Performance > 90

---

#### 2. Улучшение структурированных данных

**Проблема:**
- Нет FAQPage для страницы /light (часто задаваемые вопросы)
- Нет HowTo для инструкций
- Нет Article для страниц свечей (лучше чем CreativeWork)
- Нет Review/Rating (если будут отзывы)

**Решения:**

1. **Добавить FAQPage для /light:**
   ```typescript
   // lib/seo.ts
   export function generateFAQPageStructuredData() {
     return {
       '@context': 'https://schema.org',
       '@type': 'FAQPage',
       mainEntity: [
         {
           '@type': 'Question',
           name: 'Как зажечь свечу?',
           acceptedAnswer: {
             '@type': 'Answer',
             text: 'Выберите тип свечи, введите название и сообщение, установите длительность и нажмите "Зажечь".'
           }
         },
         // ... больше вопросов
       ]
     };
   }
   ```

2. **Изменить CreativeWork на Article для свечей:**
   ```typescript
   // lib/seo.ts
   export function generateCandleArticleStructuredData({
     title,
     description,
     createdAt,
     expiresAt,
     url,
     author,
   }) {
     return {
       '@context': 'https://schema.org',
       '@type': 'Article',
       headline: title,
       description: description,
       datePublished: createdAt,
       dateModified: expiresAt,
       author: {
         '@type': author?.is_anonymous ? 'Person' : 'Organization',
         name: author?.is_anonymous ? 'Анонимный пользователь' : author.name,
       },
       publisher: {
         '@type': 'Organization',
         name: 'CandleTime',
         logo: {
           '@type': 'ImageObject',
           url: `${siteUrl}/favicon.svg`,
         },
       },
       url,
       inLanguage: 'ru',
     };
   }
   ```

3. **Добавить HowTo для инструкций:**
   ```typescript
   export function generateHowToStructuredData() {
     return {
       '@context': 'https://schema.org',
       '@type': 'HowTo',
       name: 'Как зажечь символическую свечу онлайн',
       description: 'Пошаговая инструкция по созданию символической свечи',
       step: [
         {
           '@type': 'HowToStep',
           position: 1,
           name: 'Выберите тип свечи',
           text: 'Выберите один из типов: память, поддержка, благодарность, спокойствие или фокус',
         },
         // ... больше шагов
       ],
     };
   }
   ```

**Файлы для изменения:**
- `lib/seo.ts` - добавить новые функции
- `app/light/layout.tsx` - добавить FAQPage
- `components/DynamicMeta.tsx` - изменить на Article

**Ожидаемый результат:**
- Rich snippets в поисковой выдаче
- Улучшенное понимание контента поисковыми системами

---

#### 3. Улучшение мета-описаний и заголовков

**Проблема:**
- Некоторые описания могут быть слишком короткими
- Нет ключевых слов в некоторых описаниях
- Заголовки могут быть неоптимальными

**Решения:**

1. **Оптимизировать мета-описания:**
   - Длина: 150-160 символов
   - Включать ключевые слова естественным образом
   - Призыв к действию

2. **Улучшить заголовки:**
   - Длина: 50-60 символов
   - Включать бренд (CandleTime)
   - Уникальность для каждой страницы

3. **Добавить keywords (опционально, но полезно):**
   ```typescript
   // В generateMetadata
   keywords: [
     'символические свечи',
     'онлайн свечи',
     'зажечь свечу',
     'память',
     'поддержка',
     'благодарность',
     // ... больше ключевых слов
   ],
   ```

**Файлы для изменения:**
- `lib/seo.ts` - улучшить описания
- Все `layout.tsx` файлы - проверить мета-теги

---

### 🟡 ВЫСОКИЙ ПРИОРИТЕТ (В течение недели)

#### 4. Оптимизация для мобильных устройств

**Проблема:**
- Нужно убедиться, что все элементы оптимизированы для мобильных
- Touch targets должны быть минимум 44x44px
- Контент должен быть читаемым без зума

**Решения:**

1. **Проверить viewport:**
   ```html
   <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=5" />
   ```

2. **Добавить mobile-friendly теги:**
   ```typescript
   // В metadata
   viewport: {
     width: 'device-width',
     initialScale: 1,
     maximumScale: 5,
   },
   ```

3. **Оптимизировать touch targets:**
   - Убедиться, что все кнопки минимум 44x44px
   - Достаточные отступы между интерактивными элементами

**Файлы для изменения:**
- `app/layout.tsx` - добавить viewport
- Все компоненты - проверить размеры кнопок

---

#### 5. Internal Linking (Внутренняя перелинковка)

**Проблема:**
- Нет стратегии внутренних ссылок
- Нет связанных свечей
- Нет ссылок на похожие типы свечей

**Решения:**

1. **Добавить связанные свечи на странице свечи:**
   ```typescript
   // app/candle/[id]/page.tsx
   // Показывать 3-5 свечей того же типа
   const relatedCandles = await getRelatedCandles(candle.candle_type, candle.id);
   ```

2. **Добавить ссылки на похожие страницы:**
   - На странице свечи: ссылки на другие свечи того же типа
   - На главной: ссылки на популярные свечи
   - В футере: ссылки на важные страницы

3. **Использовать якорные ссылки:**
   - Для длинных страниц добавить якоря (#section)

**Файлы для изменения:**
- `app/candle/[id]/page.tsx` - добавить связанные свечи
- `components/SiteFooter.tsx` - улучшить ссылки
- `app/page.tsx` - добавить ссылки на популярные свечи

---

#### 6. Оптимизация URL структуры

**Проблема:**
- URL свечей: `/candle/[id]` - не очень SEO-friendly
- Нет slug в URL

**Решения:**

1. **Добавить slug в URL (опционально, требует миграции):**
   ```typescript
   // Изменить на /candle/[slug]
   // Или использовать /candle/[id]/[slug] для обратной совместимости
   ```

2. **Или улучшить текущую структуру:**
   - Убедиться, что canonical URLs правильные
   - Добавить redirects для старых URL

**Приоритет:** Средний (требует миграции БД)

---

#### 7. Добавление alt-текстов для изображений

**Проблема:**
- Если будут добавлены изображения, нужны alt-тексты
- OG изображения должны иметь описательные alt

**Решения:**

1. **Для всех изображений:**
   ```typescript
   <Image
     src="/candle.jpg"
     alt="Символическая свеча для памяти и поддержки"
     width={800}
     height={600}
   />
   ```

2. **Для OG изображений:**
   ```typescript
   // В generateMetadata
   images: [{
     url: ogImage,
     alt: `${title} - ${description}`,
   }]
   ```

**Файлы для изменения:**
- Все места, где используются изображения

---

### 🟢 СРЕДНИЙ ПРИОРИТЕТ (В течение месяца)

#### 8. hreflang для мультиязычности

**Проблема:**
- Проект использует next-intl, но нет hreflang тегов
- Поисковые системы не знают о языковых версиях

**Решения:**

1. **Добавить hreflang теги:**
   ```typescript
   // В generateMetadata
   alternates: {
     canonical: url,
     languages: {
       'ru': `${siteUrl}/ru${path}`,
       'en': `${siteUrl}/en${path}`,
     },
   },
   ```

**Файлы для изменения:**
- `lib/seo.ts` - добавить hreflang
- `app/[locale]/layout.tsx` - использовать hreflang

---

#### 9. Оптимизация для социальных сетей

**Проблема:**
- Нужно убедиться, что все OG теги правильные
- Добавить дополнительные теги для лучшего отображения

**Решения:**

1. **Добавить дополнительные OG теги:**
   ```typescript
   openGraph: {
     // ... существующие
     locale: 'ru_RU',
     siteName: 'CandleTime',
     // Добавить для статей
     publishedTime: createdAt,
     modifiedTime: expiresAt,
     authors: ['CandleTime'],
   },
   ```

2. **Добавить Twitter Card дополнительные теги:**
   ```typescript
   twitter: {
     // ... существующие
     creator: '@candletime', // если есть Twitter
     site: '@candletime',
   },
   ```

**Файлы для изменения:**
- `lib/seo.ts` - улучшить OG теги

---

#### 10. Добавление rel="next" и rel="prev" для пагинации

**Проблема:**
- На странице /candles есть пагинация, но нет rel="next"/"prev"

**Решения:**

1. **Добавить rel="next" и rel="prev":**
   ```typescript
   // В app/candles/page.tsx
   <head>
     {page > 1 && (
       <link rel="prev" href={`/candles?page=${page - 1}`} />
     )}
     {hasNextPage && (
       <link rel="next" href={`/candles?page=${page + 1}`} />
     )}
   </head>
   ```

**Файлы для изменения:**
- `app/candles/page.tsx` - добавить rel="next"/"prev"

---

#### 11. Улучшение контента для SEO

**Проблема:**
- Некоторые страницы могут иметь недостаточно контента
- Нет H1-H6 структуры на некоторых страницах

**Решения:**

1. **Проверить структуру заголовков:**
   - Один H1 на страницу
   - Логическая иерархия H2-H6
   - Использовать семантические теги (article, section, aside)

2. **Добавить больше контента:**
   - На странице /light: добавить описание типов свечей
   - На странице /candles: добавить описание перед списком
   - На странице свечи: добавить больше контекста

**Файлы для изменения:**
- Все страницы - проверить структуру заголовков

---

### 🔵 НИЗКИЙ ПРИОРИТЕТ (Опционально)

#### 12. Добавление видео Schema.org

**Если будут добавлены видео:**
```typescript
export function generateVideoStructuredData({
  name,
  description,
  thumbnailUrl,
  uploadDate,
  duration,
}) {
  return {
    '@context': 'https://schema.org',
    '@type': 'VideoObject',
    name,
    description,
    thumbnailUrl,
    uploadDate,
    duration,
  };
}
```

---

#### 13. Добавление Review/Rating Schema

**Если будут добавлены отзывы:**
```typescript
export function generateReviewStructuredData({
  rating,
  reviewCount,
  bestRating = 5,
}) {
  return {
    '@context': 'https://schema.org',
    '@type': 'AggregateRating',
    ratingValue: rating,
    reviewCount: reviewCount,
    bestRating: bestRating,
  };
}
```

---

#### 14. XML Sitemap для изображений

**Если будут добавлены изображения:**
```typescript
// В sitemap.ts добавить image sitemap
export default async function sitemap() {
  return [
    {
      url: siteUrl,
      lastModified: new Date(),
      images: [
        {
          loc: `${siteUrl}/og-image.jpg`,
          title: 'CandleTime - Символические свечи онлайн',
        },
      ],
    },
  ];
}
```

---

## 📋 Чек-лист реализации

### Критический приоритет:
- [ ] Оптимизация производительности (Core Web Vitals)
- [ ] Улучшение структурированных данных (FAQPage, Article, HowTo)
- [ ] Улучшение мета-описаний и заголовков

### Высокий приоритет:
- [ ] Оптимизация для мобильных устройств
- [ ] Internal Linking (внутренняя перелинковка)
- [ ] Оптимизация URL структуры
- [ ] Добавление alt-текстов для изображений

### Средний приоритет:
- [ ] hreflang для мультиязычности
- [ ] Оптимизация для социальных сетей
- [ ] rel="next" и rel="prev" для пагинации
- [ ] Улучшение контента для SEO

### Низкий приоритет:
- [ ] Видео Schema.org
- [ ] Review/Rating Schema
- [ ] XML Sitemap для изображений

---

## 🛠️ Инструменты для проверки

1. **Google Search Console**
   - Мониторинг индексации
   - Проверка ошибок
   - Анализ производительности

2. **Google PageSpeed Insights**
   - Проверка Core Web Vitals
   - Рекомендации по оптимизации

3. **Google Rich Results Test**
   - Проверка структурированных данных
   - Валидация JSON-LD

4. **Schema.org Validator**
   - Валидация структурированных данных

5. **Lighthouse (Chrome DevTools)**
   - SEO аудит
   - Performance аудит
   - Accessibility аудит

6. **Yandex.Webmaster** (для российского рынка)
   - Мониторинг индексации
   - Проверка ошибок

---

## 📊 Метрики успеха

### Технические метрики:
- ✅ Lighthouse SEO Score: > 95
- ✅ Lighthouse Performance Score: > 90
- ✅ Core Web Vitals: все зеленые
- ✅ Индексация: 100% важных страниц

### Бизнес-метрики:
- 📈 Органический трафик: +50% за 3 месяца
- 📈 Позиции в поиске: топ-10 по ключевым запросам
- 📈 CTR из поиска: > 3%
- 📈 Конверсия: улучшение на 20%

---

## 🎯 Следующие шаги

1. **Немедленно:**
   - Начать с оптимизации производительности
   - Улучшить структурированные данные
   - Оптимизировать мета-описания

2. **На этой неделе:**
   - Оптимизировать для мобильных
   - Добавить внутренние ссылки
   - Улучшить контент

3. **В течение месяца:**
   - Реализовать все средние приоритеты
   - Настроить мониторинг
   - Анализировать результаты

---

## 📚 Полезные ресурсы

- [Google Search Central](https://developers.google.com/search)
- [Next.js SEO Guide](https://nextjs.org/learn/seo/introduction-to-seo)
- [Schema.org Documentation](https://schema.org/)
- [Open Graph Protocol](https://ogp.me/)
- [Twitter Cards](https://developer.twitter.com/en/docs/twitter-for-websites/cards/overview/abouts-cards)
- [Core Web Vitals](https://web.dev/vitals/)

---

**Примечание:** Этот план должен регулярно обновляться по мере реализации улучшений и получения новых данных из аналитики.

