# 🚀 Быстрый старт: Деплой CandleTime за 30 минут

## Шаг 1: Supabase (10 минут)

1. **Создай проект:**
   - Зайди на [supabase.com](https://supabase.com)
   - Создай новый проект (Free план)
   - Сохрани пароль базы данных!

2. **Создай таблицу:**
   - Перейди в **SQL Editor**
   - Скопируй содержимое файла `supabase-setup.sql`
   - Вставь и нажми **Run**

3. **Получи ключи:**
   - **Settings** → **API**
   - Скопируй **Project URL** и **anon public key**

---

## Шаг 2: GitHub (5 минут)

1. **Создай репозиторий:**
   - Зайди на [github.com](https://github.com)
   - Создай новый репозиторий (например, `candletime`)

2. **Загрузи код:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/твой-username/candletime.git
   git push -u origin main
   ```

---

## Шаг 3: Vercel (5 минут)

1. **Подключи проект:**
   - Зайди на [vercel.com](https://vercel.com)
   - Войди через GitHub
   - **Add New Project** → выбери репозиторий

2. **Добавь переменные:**
   - `NEXT_PUBLIC_SUPABASE_URL` = твой Supabase URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = твой Supabase anon key

3. **Деплой:**
   - Нажми **Deploy**
   - Подожди 2-5 минут
   - Получишь URL: `candletime.vercel.app`

---

## Шаг 4: Домен (10-30 минут)

### 4.1. Покупка домена

1. **Выбери регистратора:**
   - **Российские:** [reg.ru](https://reg.ru), [timeweb.com](https://timeweb.com), [nic.ru](https://nic.ru)
   - **Международные:** [namecheap.com](https://namecheap.com), [godaddy.com](https://godaddy.com)
   - Для `.ru` доменов лучше использовать российские регистраторы

2. **Проверь доступность домена:**
   - Введи желаемое имя (например, `candletime.ru`)
   - Проверь, что домен свободен
   - Если занят, попробуй варианты: `candletime.online`, `candletime.site`, `candletime.app`

3. **Купи домен:**
   - Добавь домен в корзину
   - Выбери срок регистрации (минимум 1 год)
   - Заполни данные владельца (для `.ru` доменов нужны паспортные данные)
   - Оплати домен

4. **Активируй домен:**
   - После оплаты домен появится в твоей панели управления
   - Обычно активация занимает 5-30 минут

---

### 4.2. Подключение домена в Vercel

1. **Открой настройки проекта:**
   - Зайди на [vercel.com](https://vercel.com)
   - Выбери свой проект `candletime`
   - Перейди в **Settings** (вкладка вверху)

2. **Добавь домен:**
   - В меню слева выбери **Domains**
   - В поле "Add Domain" введи свой домен (например, `candletime.ru`)
   - Нажми **Add**

3. **Получи DNS записи:**
   - Vercel покажет, какие DNS записи нужно добавить
   - Обычно это:
     - **Тип:** `A` или `CNAME`
     - **Имя:** `@` (для корневого домена) или `www` (для поддомена)
     - **Значение:** IP адрес или CNAME запись от Vercel
   - **Важно:** Скопируй эти записи, они понадобятся в следующем шаге

4. **Пример DNS записей от Vercel:**
   ```
   Тип: A
   Имя: @
   Значение: 76.76.21.21
   
   Тип: CNAME
   Имя: www
   Значение: cname.vercel-dns.com
   ```

---

### 4.3. Настройка DNS в панели регистратора

**Важно:** Есть два способа подключить домен к Vercel. Рекомендуется **Способ 1** (проще и надежнее).

---

#### **Способ 1: Использование DNS-серверов Vercel (РЕКОМЕНДУЕТСЯ)**

Это самый простой способ — просто укажи DNS-серверы Vercel в настройках домена.

**Для reg.ru:**

1. **Зайди в управление доменом:**
   - Открой [reg.ru](https://reg.ru) и войди в личный кабинет
   - Перейди в раздел **Домены** → выбери свой домен `candletime.ru`
   - Найди раздел **DNS-серверы и управление зоной**

2. **Открой настройки DNS-серверов:**
   - Нажми кнопку **Управление** или найди раздел **DNS-серверы**
   - Выбери опцию **Свой список DNS-серверов**
   - Откроется модальное окно с полями для ввода

3. **Укажи DNS-серверы Vercel:**
   - В поле **NS1** (или первое поле) введи: `ns1.vercel-dns.com`
   - В поле **NS2** (или второе поле) введи: `ns2.vercel-dns.com`
   - Поля NS3 и NS4 оставь пустыми (или удали их, если есть кнопка удаления)
   - Нажми **Сохранить** или **Применить**

4. **Проверь результат:**
   - После сохранения в списке DNS-серверов должны появиться:
     - `ns1.vercel-dns.com`
     - `ns2.vercel-dns.com`
   - Статус должен измениться на "Активно" или "Применено"

5. **Вернись в Vercel:**
   - Зайди в Vercel → **Settings** → **Domains**
   - Убедись, что домен `candletime.ru` добавлен
   - Статус должен быть "Valid Configuration" (может занять 5-15 минут)

6. **Ожидание распространения:**
   - Изменения DNS-серверов распространяются от 15 минут до 24 часов
   - Обычно это занимает 30-60 минут
   - Можно проверить на [whatsmydns.net](https://www.whatsmydns.net) → выбери тип `NS`

**Преимущества этого способа:**
- ✅ Не нужно настраивать A/CNAME записи вручную
- ✅ Vercel автоматически управляет DNS записями
- ✅ Проще и надежнее

---

#### **Способ 2: Использование DNS регистратора (альтернативный)**

Если по каким-то причинам не можешь использовать DNS-серверы Vercel, можно использовать DNS регистратора и добавить записи вручную.

**Для reg.ru:**

1. **Зайди в панель управления доменом:**
   - Открой [reg.ru](https://reg.ru) и войди в личный кабинет
   - Перейди в раздел **Домены** → выбери свой домен `candletime.ru`
   - Найди раздел **DNS-серверы и управление зоной**

2. **Выбери DNS регистратора:**
   - Убедись, что выбраны DNS-серверы reg.ru (обычно `ns1.reg.ru` и `ns2.reg.ru`)
   - Если нужно, переключись на "DNS регистратора"

3. **Получи DNS записи из Vercel:**
   - Зайди в Vercel → **Settings** → **Domains**
   - Добавь домен `candletime.ru` (если еще не добавлен)
   - Vercel покажет, какие DNS записи нужно добавить
   - Обычно это:
     - **Тип:** `A`
     - **Имя:** `@` (для корневого домена)
     - **Значение:** IP адрес (например, `76.76.21.21`)
     - **Тип:** `CNAME`
     - **Имя:** `www`
     - **Значение:** `cname.vercel-dns.com`

4. **Добавь DNS записи в reg.ru:**
   - В разделе **Ресурсные записи** нажми **Добавить запись**
   - Для корневого домена (`candletime.ru`):
     - **Тип:** `A`
     - **Имя/Хост:** `@` или оставь пустым
     - **Значение/Направление:** IP адрес из Vercel
     - **TTL:** оставь по умолчанию (3600)
   - Для поддомена `www`:
     - **Тип:** `CNAME`
     - **Имя/Хост:** `www`
     - **Значение/Направление:** `cname.vercel-dns.com`
     - **TTL:** оставь по умолчанию (3600)
   - Сохрани записи

5. **Проверь записи:**
   - Убедись, что записи добавлены правильно
   - Не должно быть конфликтующих записей (старых A или CNAME записей)

6. **Ожидание распространения DNS:**
   - Изменения DNS распространяются от 5 минут до 48 часов
   - Обычно это занимает 15-60 минут
   - Можно проверить статус на [whatsmydns.net](https://www.whatsmydns.net)

---

### 4.3.1. Что делать, если Vercel показывает "Invalid Configuration"

**Это нормально!** Сразу после настройки DNS Vercel может показывать статус "Invalid Configuration" (красный значок с восклицательным знаком). Это означает, что DNS изменения еще не применились.

**Пошаговая проверка:**

1. **Проверь DNS-серверы в reg.ru:**
   - Убедись, что указаны `ns1.vercel-dns.com` и `ns2.vercel-dns.com`
   - Статус должен быть "Активно" или "Применено"
   - Если не применено, подожди 10-15 минут и проверь снова

2. **Проверь распространение DNS:**
   - Зайди на [whatsmydns.net](https://www.whatsmydns.net)
   - Выбери тип записи **NS** (Name Server)
   - Введи свой домен `candletime.ru`
   - Должны отображаться `ns1.vercel-dns.com` и `ns2.vercel-dns.com`
   - Если видишь старые DNS-серверы (например, `ns1.reg.ru`), подожди еще 30-60 минут

3. **Обнови статус в Vercel:**
   - В Vercel нажми кнопку **Refresh** рядом с доменом
   - Или просто подожди — Vercel проверяет DNS автоматически каждые несколько минут

4. **Сколько ждать:**
   - **Минимум:** 15-30 минут (редко, но возможно)
   - **Обычно:** 30-60 минут
   - **Максимум:** до 24 часов (в очень редких случаях)

5. **Когда стоит беспокоиться:**
   - Если через **2-3 часа** статус все еще "Invalid Configuration"
   - Если на [whatsmydns.net](https://www.whatsmydns.net) не видно DNS-серверов Vercel
   - В этом случае проверь настройки в reg.ru еще раз

6. **Проверка правильности настроек:**
   - В reg.ru должны быть указаны **именно** `ns1.vercel-dns.com` и `ns2.vercel-dns.com`
   - Не должно быть других DNS-серверов (ns3, ns4 должны быть пустыми)
   - Не должно быть старых DNS-серверов reg.ru

**После успешной настройки:**
- Статус в Vercel изменится на "Valid Configuration" (синяя галочка)
- Домен будет доступен по адресу `https://candletime.ru`
- SSL сертификат будет выдан автоматически

---

### 4.3.2. Ошибка ERR_NAME_NOT_RESOLVED

Если при попытке открыть `candletime.ru` видишь ошибку **ERR_NAME_NOT_RESOLVED**, это означает, что DNS еще не распространился или настроен неправильно.

**Пошаговая диагностика:**

1. **Проверь, сколько времени прошло:**
   - Если прошло меньше 30 минут — это нормально, подожди еще
   - Если прошло больше 2 часов — нужно проверить настройки

2. **Проверь DNS-серверы в reg.ru:**
   - Зайди в личный кабинет reg.ru
   - **Домены** → `candletime.ru` → **DNS-серверы**
   - Убедись, что указаны:
     - `ns1.vercel-dns.com`
     - `ns2.vercel-dns.com`
   - Проверь статус — должно быть "Активно" или "Применено"
   - Если статус "Ожидает применения", подожди 10-15 минут

3. **Проверь распространение DNS:**
   - Зайди на [whatsmydns.net](https://www.whatsmydns.net)
   - Выбери тип записи **NS** (Name Server)
   - Введи `candletime.ru`
   - **Что должно быть:**
     - На большинстве серверов должны отображаться `ns1.vercel-dns.com` и `ns2.vercel-dns.com`
   - **Если видишь:**
     - Старые DNS-серверы (например, `ns1.reg.ru`) — подожди еще 30-60 минут
     - "No nameservers found" — DNS-серверы не применены в reg.ru, проверь настройки
     - Смешанные результаты (одни серверы показывают Vercel, другие reg.ru) — это нормально, DNS распространяется постепенно

4. **Проверь домен в Vercel:**
   - Зайди в Vercel → **Settings** → **Domains**
   - Убедись, что домен `candletime.ru` добавлен
   - Нажми кнопку **Refresh** рядом с доменом
   - Статус должен измениться на "Valid Configuration" после распространения DNS

5. **Проверь правильность написания:**
   - Убедись, что в reg.ru указаны **именно** эти DNS-серверы (без опечаток):
     - `ns1.vercel-dns.com` (не `ns1.vercel.com` или другие варианты)
     - `ns2.vercel-dns.com` (не `ns2.vercel.com` или другие варианты)
   - Регистр не важен, но точки и дефисы важны

6. **Очисти кеш DNS на своем компьютере:**
   - **Windows:** Открой командную строку (cmd) и выполни:
     ```bash
     ipconfig /flushdns
     ```
   - **Mac/Linux:** Выполни в терминале:
     ```bash
     sudo dscacheutil -flushcache
     ```
   - После этого попробуй открыть сайт снова

7. **Попробуй открыть через другой браузер или устройство:**
   - Иногда кеш браузера мешает
   - Попробуй открыть в режиме инкогнито
   - Или попробуй с другого устройства/сети

**Если через 2-3 часа все еще ERR_NAME_NOT_RESOLVED:**

1. **Перепроверь DNS-серверы в reg.ru:**
   - Удались все DNS-серверы
   - Добавь заново:
     - `ns1.vercel-dns.com`
     - `ns2.vercel-dns.com`
   - Сохрани и подожди 30-60 минут

2. **Проверь, что домен не заблокирован:**
   - Убедись, что домен активен в reg.ru
   - Проверь, что не истек срок регистрации

3. **Свяжись с поддержкой reg.ru:**
   - Если все настройки правильные, но DNS не работает
   - Возможно, нужна помощь техподдержки

**Важно:** DNS распространяется постепенно по всему миру. Даже если на [whatsmydns.net](https://www.whatsmydns.net) некоторые серверы еще показывают старые значения, это нормально. Главное, чтобы большинство серверов уже показывали правильные DNS-серверы Vercel.

---

### 4.4. Обновление настроек в Supabase

1. **Открой настройки аутентификации:**
   - Зайди на [supabase.com](https://supabase.com)
   - Выбери свой проект
   - В меню слева выбери **Authentication**
   - Перейди во вкладку **URL Configuration**

2. **Обнови Site URL:**
   - В поле **Site URL** замени `https://candletime.vercel.app` на `https://candletime.ru`
   - Нажми **Save**

3. **Добавь Redirect URLs (если нужно):**
   - В разделе **Redirect URLs** добавь:
     - `https://candletime.ru/**`
     - `https://www.candletime.ru/**` (если используешь www)
   - Это нужно для корректной работы авторизации после редиректа

4. **Проверь Email Templates (опционально):**
   - Если используешь email-авторизацию, убедись, что в шаблонах указан правильный URL
   - **Authentication** → **Email Templates**
   - Проверь, что ссылки ведут на твой домен

---

## ✅ Готово!

Твой сайт теперь доступен по адресу: `https://candletime.ru`

**Что дальше:**
- Сайт автоматически обновляется при каждом `git push`
- Все изменения деплоятся на production
- SSL сертификат выдается автоматически Vercel

---

## 💡 Полезные команды

### Локальная разработка

```bash
# Установка зависимостей (если еще не установлены)
npm install

# Запуск локального сервера разработки
npm run dev
# Откроется на http://localhost:3000

# Проверка сборки перед деплоем
npm run build

# Запуск production сборки локально
npm start

# Проверка линтера (проверка кода на ошибки)
npm run lint

# Исправление ошибок линтера автоматически
npm run lint -- --fix
```

### Git команды

```bash
# Проверка статуса изменений
git status

# Добавление всех изменений
git add .

# Создание коммита
git commit -m "Описание изменений"

# Отправка изменений в GitHub
git push

# Просмотр истории коммитов
git log --oneline

# Откат последнего коммита (если что-то пошло не так)
git reset --soft HEAD~1
```

### Проверка перед деплоем

```bash
# 1. Проверь, что проект собирается без ошибок
npm run build

# 2. Проверь линтер
npm run lint

# 3. Проверь, что все переменные окружения установлены
# (в Vercel: Settings → Environment Variables)
```

---

## 🆘 Если что-то не работает

### Проблема: Сайт не открывается после деплоя

1. **Проверь логи в Vercel:**
   - Зайди в проект на Vercel
   - Перейди в **Deployments**
   - Выбери последний деплой
   - Нажми **View Function Logs** или **View Build Logs**
   - Ищи ошибки (красным цветом)

2. **Проверь переменные окружения:**
   - **Settings** → **Environment Variables**
   - Убедись, что добавлены:
     - `NEXT_PUBLIC_SUPABASE_URL`
     - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - Проверь, что значения правильные (без лишних пробелов)

3. **Проверь сборку локально:**
   ```bash
   npm run build
   ```
   - Если сборка падает локально, исправь ошибки
   - После исправления сделай `git push`

### Проблема: Домен не работает / "Invalid Configuration" в Vercel

**Важно:** Если видишь "Invalid Configuration" сразу после настройки DNS — это нормально! Подожди 30-60 минут.

1. **Проверь DNS-серверы (если используешь Способ 1):**
   - Зайди на [whatsmydns.net](https://www.whatsmydns.net)
   - Выбери тип записи **NS** (Name Server)
   - Введи свой домен `candletime.ru`
   - Должны отображаться: `ns1.vercel-dns.com` и `ns2.vercel-dns.com`
   - Если видишь старые DNS-серверы (например, `ns1.reg.ru`), подожди еще 30-60 минут
   - Если через 2 часа все еще старые серверы, проверь настройки в reg.ru

2. **Проверь DNS записи (если используешь Способ 2):**
   - Зайди на [whatsmydns.net](https://www.whatsmydns.net)
   - Введи свой домен
   - Выбери тип записи `A` или `CNAME`
   - Проверь, что значения совпадают с теми, что дал Vercel
   - Если записи не совпадают, подожди еще 30-60 минут (DNS распространяется медленно)

3. **Проверь настройки в Vercel:**
   - **Settings** → **Domains**
   - Убедись, что домен добавлен
   - Нажми кнопку **Refresh** рядом с доменом
   - Статус должен измениться на "Valid Configuration" после распространения DNS
   - Если через 2-3 часа все еще "Invalid Configuration", проверь DNS-серверы/записи еще раз

4. **Проверь DNS-серверы в reg.ru:**
   - Зайди в личный кабинет reg.ru
   - Перейди в **Домены** → выбери `candletime.ru`
   - Проверь раздел **DNS-серверы**
   - Должны быть указаны: `ns1.vercel-dns.com` и `ns2.vercel-dns.com`
   - Статус должен быть "Активно" или "Применено"
   - Если не применено, подожди 10-15 минут и проверь снова

5. **Проверь SSL сертификат:**
   - Vercel автоматически выдает SSL сертификат
   - Обычно это занимает 5-10 минут после того, как DNS настроен правильно
   - В **Settings** → **Domains** должен быть статус "Valid"
   - SSL выдается только после успешной настройки DNS

6. **Частые ошибки:**
   - ❌ Указаны не те DNS-серверы (например, `ns1.reg.ru` вместо `ns1.vercel-dns.com`)
   - ❌ DNS-серверы не применены в reg.ru (статус "Ожидает применения")
   - ❌ Добавлены лишние DNS-серверы (ns3, ns4 должны быть пустыми)
   - ❌ Не прошло достаточно времени (нужно минимум 30-60 минут)

### Проблема: ERR_NAME_NOT_RESOLVED при открытии сайта

Эта ошибка означает, что браузер не может найти IP-адрес для домена. Обычно это происходит сразу после настройки DNS.

**Быстрая диагностика:**

1. **Проверь время:**
   - Если прошло меньше 30 минут — подожди еще (это нормально)
   - Если прошло больше 2 часов — проверь настройки

2. **Проверь DNS-серверы в reg.ru:**
   - Должны быть: `ns1.vercel-dns.com` и `ns2.vercel-dns.com`
   - Статус должен быть "Активно"

3. **Проверь на [whatsmydns.net](https://www.whatsmydns.net):**
   - Тип записи: **NS**
   - Домен: `candletime.ru`
   - Должны отображаться DNS-серверы Vercel

4. **Очисти кеш DNS:**
   - **Windows:** `ipconfig /flushdns` в командной строке
   - **Mac/Linux:** `sudo dscacheutil -flushcache` в терминале

5. **Попробуй другой браузер или режим инкогнито**

**Подробная инструкция:** См. раздел 4.3.2 выше.

### Проблема: Авторизация не работает

1. **Проверь настройки в Supabase:**
   - **Authentication** → **URL Configuration**
   - **Site URL** должен быть `https://candletime.ru` (не vercel.app)
   - **Redirect URLs** должны включать твой домен

2. **Проверь переменные окружения:**
   - В Vercel должны быть установлены правильные Supabase ключи
   - Переменные должны быть доступны для Production, Preview и Development

3. **Проверь RLS политики:**
   - В Supabase: **Authentication** → **Policies**
   - Убедись, что политики настроены правильно
   - Для тестирования можно временно отключить RLS (не для production!)

### Проблема: База данных не работает

1. **Проверь подключение:**
   - В Supabase: **Settings** → **API**
   - Убедись, что URL и ключи правильные
   - Проверь, что проект не приостановлен (Free план может приостанавливаться)

2. **Проверь таблицы:**
   - В Supabase: **Table Editor**
   - Убедись, что таблица `candles` существует
   - Проверь, что структура таблицы соответствует схеме

3. **Проверь RLS политики:**
   - **Authentication** → **Policies**
   - Для таблицы `candles` должны быть политики:
     - SELECT для всех (публичное чтение)
     - INSERT для всех (публичное создание)
     - UPDATE/DELETE для владельцев (если нужно)

### Проблема: Сборка падает в Vercel

1. **Проверь логи сборки:**
   - **Deployments** → выбери упавший деплой → **View Build Logs**
   - Ищи ошибки компиляции

2. **Частые проблемы:**
   - Отсутствуют переменные окружения → добавь в Vercel
   - Ошибки TypeScript → исправь в коде
   - Проблемы с зависимостями → проверь `package.json`

3. **Проверь версию Node.js:**
   - В Vercel: **Settings** → **General** → **Node.js Version**
   - Рекомендуется использовать LTS версию (например, 20.x)

### Полезные ссылки для диагностики

- **Проверка DNS:** [whatsmydns.net](https://www.whatsmydns.net)
- **Проверка SSL:** [ssllabs.com](https://www.ssllabs.com/ssltest/)
- **Проверка скорости сайта:** [pagespeed.web.dev](https://pagespeed.web.dev/)
- **Документация Vercel:** [vercel.com/docs](https://vercel.com/docs)
- **Документация Supabase:** [supabase.com/docs](https://supabase.com/docs)

