# 🕯️ CandleTime

Спокойный сервис для зажигания символических свечей онлайн.

## 🚀 Быстрый старт

### Установка

```bash
npm install
```

### Настройка переменных окружения

Создай файл `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=твой_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=твой_supabase_anon_key
```

### Запуск локально

```bash
npm run dev
```

Открой [http://localhost:3000](http://localhost:3000) в браузере.

## 📦 Технологии

- **Next.js 16** - React фреймворк с App Router
- **TypeScript** - строгая типизация
- **Tailwind CSS** - утилитарная стилизация
- **Supabase** - база данных PostgreSQL и аутентификация
- **Google Gemini API** - генерация статей (опционально)
- **Leaflet** - интерактивные карты

## 🚢 Деплой в продакшн

Подробная инструкция: [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

Быстрый старт: [QUICK_START_DEPLOY.md](./QUICK_START_DEPLOY.md)

### Основные шаги:

1. **Supabase** - создай проект и выполни `supabase-setup.sql`
2. **Vercel** - задеплой проект через GitHub
3. **Домен** - купи и подключи домен в Vercel
4. **Настройка** - обнови Site URL в Supabase
5. **Reverse Proxy** - настрой VPS с Nginx для доступа из РФ (опционально, но рекомендуется)

**Примечание:** Для обеспечения доступности из РФ рекомендуется настроить reverse proxy через VPS. Подробная инструкция: [docs/infrastructure/REVERSE_PROXY_SETUP_PLAN.md](./docs/infrastructure/REVERSE_PROXY_SETUP_PLAN.md)

## 📝 Скрипты

```bash
npm run dev              # Запуск dev сервера (http://localhost:3000)
npm run build            # Сборка для продакшна
npm run start            # Запуск production сервера
npm run lint             # Проверка кода ESLint
npm run generate:seo     # Генерация SEO статьи через Gemini
```

## 📚 Документация

Вся документация проекта организована в папке `docs/`. См. [главный индекс документации](./docs/README.md).

### 📘 Главная техническая документация

- **[Техническая документация](./docs/TECHNICAL_DOCUMENTATION.md)** - Полная техническая документация проекта (архитектура, API, БД, компоненты, развертывание)
- **[Процесс обновления документации](./docs/DOCUMENTATION_UPDATE_PROCESS.md)** - Как поддерживать документацию в актуальном состоянии

### Быстрый доступ:
- [Полное руководство по деплою](./docs/deployment/DEPLOYMENT_GUIDE.md)
- [Быстрый старт деплоя](./docs/deployment/QUICK_START.md)
- [План развития проекта](./docs/architecture/ROADMAP.md)
- [API Документация](./docs/API.md) - Полная документация всех API endpoints
- [Декоративные элементы HERO блока](./docs/ui/HERO_DECORATIVE_ELEMENTS.md) - Документация по декоративным элементам главной страницы

## 🎯 Основные функции

- ✅ **Зажигание символических свечей** - 5 типов свечей (спокойствие, поддержка, память, благодарность, фокус)
- ✅ **Шаблоны свечей** - 12 готовых шаблонов для быстрого создания
- ✅ **Публичные ссылки** - каждая свеча имеет уникальную ссылку для шаринга
- ✅ **Личный кабинет** - история всех созданных свечей
- ✅ **Анонимное использование** - можно использовать без регистрации
- ✅ **Автоматическое погашение** - свечи гаснут по истечении времени (1 час, 24 часа, 7 дней)
- ✅ **Ручное погашение** - возможность погасить свечу вручную
- ✅ **Интерактивная карта** - визуализация свечей на карте мира
- ✅ **FAQ/Блог** - система статей с поддержкой Markdown
- ✅ **Админ-панель** - управление статьями и генерация контента через AI
- ✅ **Рождественская тема** - новогодние декорации и анимация снежинок (управляется через админ-панель)
- ✅ **Загадывание желаний на новый год** - специальная страница `/wish-2026` для создания свечей с желаниями на 2026 год

## 💻 Примеры использования

### Создание свечи через API

```typescript
import { supabase } from '@/lib/supabaseClient';

// Создание свечи
const { data, error } = await supabase
  .from('candles')
  .insert({
    title: 'Моя свеча',
    message: 'Текст сообщения',
    candle_type: 'calm',
    duration_hours: 24,
    expires_at: new Date(Date.now() + 24 * 60 * 60 * 1000).toISOString(),
    is_anonymous: false
  })
  .select()
  .single();

if (error) {
  console.error('Ошибка создания свечи:', error);
} else {
  console.log('Свеча создана:', data.id);
}
```

### Получение свечей для карты

```typescript
// Получение свечей через API
const response = await fetch('/api/map/candles?type=calm&status=active');
const { candles } = await response.json();

candles.forEach(candle => {
  console.log(`${candle.title} - ${candle.city}, ${candle.country}`);
});
```

### Использование утилит

```typescript
import { getCandleTypeMeta, formatDate, formatRemainingTime } from '@/lib/utils';

// Получение метаданных типа свечи
const meta = getCandleTypeMeta('calm');
console.log(meta.label); // 'Спокойствие'
console.log(meta.emoji); // '🕊️'

// Форматирование даты
const formatted = formatDate(new Date());
console.log(formatted); // '15.01.2025'

// Форматирование оставшегося времени
const remaining = formatRemainingTime(new Date(Date.now() + 3600000));
console.log(remaining); // 'Осталось ~1.0 ч'
```

### Генерация SEO метаданных

```typescript
import { generateMetadata } from '@/lib/seo';

// Генерация мета-тегов для страницы
export const metadata = generateMetadata({
  title: 'Моя страница',
  description: 'Описание страницы',
  path: '/my-page',
  keywords: ['ключевое', 'слово']
});
```

### Работа с темой

```typescript
'use client';
import { useTheme } from '@/lib/theme';

function ThemeToggle() {
  const { theme, resolvedTheme, setTheme, mounted } = useTheme();

  if (!mounted) return null; // Предотвращает hydration mismatch

  return (
    <button onClick={() => setTheme(resolvedTheme === 'dark' ? 'light' : 'dark')}>
      Текущая тема: {resolvedTheme}
    </button>
  );
}
```

### Обработка ошибок

```typescript
import { handleSupabaseError, logError } from '@/lib/errorHandler';

try {
  await supabase.from('candles').insert(data);
} catch (error) {
  // Логирование с контекстом
  logError(error, {
    component: 'LightPage',
    action: 'createCandle',
    metadata: { userId: user?.id }
  });

  // Получение понятного сообщения для пользователя
  const message = handleSupabaseError(error, { component: 'LightPage' });
  setError(message);
}
```

## 🏗️ Структура проекта

```
online-candles/
├── app/                    # Next.js App Router
│   ├── api/                # API Routes
│   │   ├── admin/         # Админские endpoints
│   │   │   └── settings/  # API для настроек сайта
│   │   ├── geocode/       # Геокодирование
│   │   └── map/           # API для карты
│   ├── auth/              # Страницы авторизации
│   ├── admin/             # Админ-панель
│   │   └── settings/      # Настройки сайта (рождественская тема)
│   ├── candle/            # Страница свечи
│   ├── candles/           # Список свечей
│   ├── dashboard/         # Личный кабинет
│   ├── faq/               # FAQ/Блог
│   ├── light/             # Создание свечи
│   ├── map/               # Карта мира
│   └── wish-2026/         # Страница загадывания желания на 2026 год
├── components/            # React компоненты
│   ├── admin/            # Компоненты админ-панели
│   ├── map/              # Компоненты карты
│   ├── Snowflakes.tsx    # Компонент анимации снежинок
│   └── ...               # Общие компоненты
├── lib/                   # Утилиты и хелперы
│   ├── admin.ts          # Функции админ-панели
│   ├── christmasTheme.ts # Функции для работы с рождественской темой
│   ├── constants.ts       # Константы
│   ├── errorHandler.ts    # Обработка ошибок
│   ├── gemini.ts          # Интеграция с Gemini API
│   ├── seo.ts             # SEO утилиты
│   ├── supabaseClient.ts  # Supabase клиент
│   ├── types.ts           # TypeScript типы
│   └── utils.ts           # Общие утилиты
├── hooks/                  # React хуки
│   └── useChristmasTheme.ts # Хук для проверки состояния рождественской темы
├── docs/                  # Документация
│   └── API.md            # API документация
└── public/                # Статические файлы
```

## 🔧 Разработка

### Добавление новой функции

1. Создай типы в `lib/types.ts` (если нужно)
2. Добавь константы в `lib/constants.ts` (если нужно)
3. Создай утилиты в `lib/utils.ts` или отдельном файле
4. Создай компоненты в `components/`
5. Создай страницы в `app/`
6. Добавь JSDoc комментарии ко всем публичным функциям

### Стиль кода

- Используй TypeScript для всех файлов
- Следуй правилам ESLint
- Добавляй JSDoc комментарии к публичным функциям
- Используй функциональные компоненты React
- Применяй Tailwind CSS для стилизации

## 📄 Лицензия

Проект создан для личного использования.
