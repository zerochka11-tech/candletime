# 🔥 Генератор SEO-статей для CandleTime

Автоматический инструмент для генерации SEO-оптимизированных статей с использованием OpenAI.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

Это установит:
- `openai` - SDK для работы с OpenAI API
- `ts-node` - для запуска TypeScript файлов

### 2. Настройка API ключа

Добавьте ваш OpenAI API ключ в `.env.local`:

```env
OPENAI_API_KEY=sk-your-api-key-here
```

### 3. Генерация первой статьи

```bash
npm run generate:seo -- \
  --slug practice-gratitude-digital-candle \
  --title "Практика благодарности: как начать с простой цифровой свечи" \
  --h1 "Практика благодарности: как начать с простой цифровой свечи" \
  --candleType gratitude \
  --language ru
```

Статья будет сохранена в `seo-articles/practice-gratitude-digital-candle.md`

## 📋 Параметры команды

| Параметр | Обязательный | Описание | Пример |
|----------|-------------|----------|--------|
| `--slug` | ✅ Да | Уникальный идентификатор (для имени файла) | `practice-gratitude-digital-candle` |
| `--title` | ✅ Да | Мета-заголовок статьи | `"Практика благодарности..."` |
| `--h1` | ✅ Да | Заголовок H1 статьи | `"Практика благодарности..."` |
| `--candleType` | ❌ Нет | Тип свечи для CTA (`calm`, `support`, `memory`, `gratitude`, `focus`) | `gratitude` |
| `--language` | ❌ Нет | Язык статьи (`ru`, `en`) | `ru` |

## 💡 Примеры использования

### Статья про спокойствие (русский)

```bash
npm run generate:seo -- \
  --slug calm-mindfulness-daily \
  --title "Осознанность каждый день: простые техники спокойствия" \
  --h1 "Осознанность каждый день: простые техники спокойствия" \
  --candleType calm \
  --language ru
```

### Статья про поддержку (русский)

```bash
npm run generate:seo -- \
  --slug support-during-difficult-times \
  --title "Поддержка в трудные времена: как помочь себе и близким" \
  --h1 "Поддержка в трудные времена: как помочь себе и близким" \
  --candleType support \
  --language ru
```

### Статья про память (английский)

```bash
npm run generate:seo -- \
  --slug remembering-important-moments \
  --title "Remembering Important Moments: Digital Rituals of Memory" \
  --h1 "Remembering Important Moments: Digital Rituals of Memory" \
  --candleType memory \
  --language en
```

### Статья про фокус (русский)

```bash
npm run generate:seo -- \
  --slug focus-productivity-techniques \
  --title "Фокус и продуктивность: техники концентрации внимания" \
  --h1 "Фокус и продуктивность: техники концентрации внимания" \
  --candleType focus \
  --language ru
```

## 📝 Что генерируется?

- **Длина:** 1200-1800 слов
- **Формат:** Markdown (H1, H2, H3, списки, параграфы)
- **Тон:** спокойный, теплый, без пафоса
- **Стиль:** простой, человеческий, без инфобизнеса
- **CTA:** мягкий призыв в конце статьи с упоминанием соответствующего типа свечи

## 📁 Структура файлов

```
online-candles/
├── scripts/
│   ├── generate-seo-article.ts  # Основной скрипт
│   └── README.md                # Детальная документация
├── seo-articles/                # Сгенерированные статьи
│   └── *.md
└── SEO_ARTICLE_GENERATOR.md     # Этот файл
```

## 🔄 Следующие шаги

После генерации статьи:

1. **Проверьте и отредактируйте** статью вручную при необходимости
2. **Добавьте метаданные** (SEO title, description, keywords) если нужно
3. **Загрузите в базу данных** через админ-панель или API
4. **Опубликуйте** через систему FAQ/Блога

## ⚙️ Технические детали

- **Модель:** GPT-4o
- **Temperature:** 0.7
- **Формат вывода:** Markdown
- **Промпт оптимизирован** для специфики CandleTime

## 📚 Дополнительная информация

Подробная документация доступна в `scripts/README.md`

