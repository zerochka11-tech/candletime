# 🚀 План интеграции Gemini API для генерации SEO-статей

## 📋 Обзор

Интеграция Google Gemini API для автоматической генерации SEO-оптимизированных статей прямо из админ-панели. Статьи будут автоматически сохраняться как черновики.

**Источник документации:** [Gemini API Quickstart](https://ai.google.dev/gemini-api/docs/quickstart)

---

## 🎯 Цели

1. ✅ Добавить кнопку "Сгенерировать статью" в админ-панель
2. ✅ Создать форму для ввода параметров генерации
3. ✅ Интегрировать Gemini API для генерации контента
4. ✅ Автоматически сохранять статьи как черновики в БД
5. ✅ Обработать ошибки и показать прогресс генерации

---

## 📦 Шаг 1: Установка зависимостей

### 1.1 Установить Google GenAI SDK

```bash
npm install @google/genai
```

### 1.2 Добавить переменную окружения

Добавить в `.env.local`:

```env
GEMINI_API_KEY=your-gemini-api-key-here
```

**Где получить ключ:** [Google AI Studio](https://aistudio.google.com/app/apikey)

---

## 🏗 Шаг 2: Создание API Route для генерации

### 2.1 Создать файл `app/api/admin/articles/generate/route.ts`

**Функционал:**
- Принимает параметры генерации (тема, тип свечи, язык)
- Вызывает Gemini API для генерации контента
- Парсит ответ и извлекает заголовок, контент, SEO метаданные
- Сохраняет статью в БД как черновик
- Возвращает созданную статью

**Структура запроса:**
```typescript
{
  topic: string;           // Тема статьи
  candleType?: string;     // Тип свечи (calm, support, memory, gratitude, focus)
  language?: 'ru' | 'en';  // Язык статьи
  categoryId?: string;      // ID категории (опционально)
}
```

**Промпт для Gemini:**
- Длина: 1200-1800 слов
- Формат: Markdown (H1, H2, H3, списки)
- Тон: спокойный, теплый, без пафоса
- Стиль: простой, человеческий
- CTA: мягкий призыв в конце с упоминанием типа свечи

---

## 🎨 Шаг 3: UI компонент в админ-панели

### 3.1 Добавить кнопку "Сгенерировать статью"

**Место:** `app/admin/articles/page.tsx`
- Рядом с кнопкой "+ Создать статью"
- Или в отдельном блоке "AI Генерация"

### 3.2 Создать модальное окно/форму

**Компонент:** `components/admin/GenerateArticleDialog.tsx`

**Поля формы:**
- **Тема статьи** (обязательно) - текстовое поле
- **Тип свечи** (опционально) - выпадающий список:
  - Спокойствие (calm)
  - Поддержка (support)
  - Память (memory)
  - Благодарность (gratitude)
  - Фокус (focus)
- **Язык** (опционально) - переключатель: Русский / English
- **Категория** (опционально) - выпадающий список категорий

**Состояния:**
- `loading` - показывать спиннер при генерации
- `error` - показывать ошибки
- `success` - показывать успешное создание

---

## ⚙️ Шаг 4: Интеграция с Gemini API

### 4.1 Создать утилиту для работы с Gemini

**Файл:** `lib/gemini.ts`

**Функции:**
```typescript
// Инициализация клиента
export function getGeminiClient()

// Генерация статьи
export async function generateArticle(params: {
  topic: string;
  candleType?: string;
  language?: 'ru' | 'en';
}): Promise<{
  title: string;
  content: string;
  excerpt: string;
  seoTitle: string;
  seoDescription: string;
  seoKeywords: string[];
  readingTime: number;
}>
```

### 4.2 Промпт для Gemini

**Структура промпта:**
```
Ты - эксперт по написанию SEO-оптимизированных статей для сайта CandleTime.

CandleTime - это тихое место для зажигания символических свечей онлайн. 
Без ленты и лайков, только спокойный жест внимания.

Задача: Напиши SEO-статью на тему "{topic}".

Требования:
- Длина: 1200-1800 слов
- Формат: Markdown (используй H1, H2, H3, списки, параграфы)
- Тон: спокойный, теплый, без пафоса
- Стиль: простой, человеческий, без инфобизнеса
- Язык: {language}

{если candleType указан:}
- В конце статьи добавь мягкий призыв к действию с упоминанием свечи типа "{candleType}"
- Типы свечей:
  - calm (Спокойствие) - для умиротворения и гармонии
  - support (Поддержка) - чтобы поддержать кого-то
  - memory (Память) - в память о ком-то или о чем-то
  - gratitude (Благодарность) - чтобы выразить благодарность
  - focus (Фокус) - для концентрации и намерений

Структура статьи:
1. H1 заголовок (главный заголовок статьи)
2. Введение (2-3 параграфа)
3. Основной контент (разделы с H2, H3)
4. Практические советы (если применимо)
5. Заключение с CTA (если указан тип свечи)

Верни только Markdown контент статьи, без дополнительных комментариев.
```

---

## 🔄 Шаг 5: Обработка ответа и сохранение

### 5.1 Парсинг ответа Gemini

**Логика:**
1. Извлечь H1 как `title`
2. Остальной контент как `content`
3. Сгенерировать `excerpt` из первых 150 символов
4. Вычислить `readingTime` (200 слов/минуту)
5. Сгенерировать SEO метаданные:
   - `seoTitle`: оптимизированный заголовок
   - `seoDescription`: краткое описание (150-160 символов)
   - `seoKeywords`: массив ключевых слов

### 5.2 Генерация slug

```typescript
function generateSlug(title: string): string {
  return title
    .toLowerCase()
    .replace(/[^\w\s-]/g, '')
    .replace(/\s+/g, '-')
    .replace(/-+/g, '-')
    .trim();
}
```

### 5.3 Сохранение в БД

**Использовать существующий API:** `/api/admin/articles` (POST)

**Поля для сохранения:**
- `title` - из H1
- `slug` - сгенерированный из title
- `content` - полный Markdown контент
- `excerpt` - первые 150 символов
- `reading_time` - вычисленное время
- `seo_title` - оптимизированный заголовок
- `seo_description` - SEO описание
- `seo_keywords` - массив ключевых слов
- `category_id` - выбранная категория (если указана)
- `author_id` - ID текущего админа
- `published` - `false` (черновик)
- `published_at` - `null`

---

## 🎨 Шаг 6: UI/UX улучшения

### 6.1 Индикатор прогресса

- Показывать спиннер при генерации
- Текст: "Генерация статьи... Это может занять 10-30 секунд"

### 6.2 Обработка ошибок

- Показывать понятные сообщения об ошибках
- Логировать ошибки в консоль для отладки
- Предлагать повторить попытку

### 6.3 Успешное создание

- Показывать уведомление об успехе
- Автоматически переходить к созданной статье
- Или обновлять список статей

---

## 📝 Шаг 7: Дополнительные функции (опционально)

### 7.1 Предпросмотр перед сохранением

- Показывать сгенерированную статью в модальном окне
- Возможность редактировать перед сохранением
- Кнопки: "Сохранить", "Редактировать", "Отменить"

### 7.2 Шаблоны промптов

- Сохранять популярные темы как шаблоны
- Быстрый выбор из списка шаблонов

### 7.3 Пакетная генерация

- Генерация нескольких статей за раз
- Очередь генерации с прогресс-баром

---

## 🔒 Шаг 8: Безопасность и ограничения

### 8.1 Проверка прав доступа

- Использовать существующую функцию `checkAdminAuth`
- Только админы могут генерировать статьи

### 8.2 Rate limiting

- Ограничить количество запросов в минуту
- Показывать предупреждение при превышении лимита

### 8.3 Валидация входных данных

- Проверять длину темы (мин 10, макс 200 символов)
- Санитизировать пользовательский ввод

---

## 📊 Шаг 9: Логирование и мониторинг

### 9.1 Логирование

- Логировать все запросы на генерацию
- Сохранять параметры генерации
- Отслеживать время генерации

### 9.2 Метрики

- Количество сгенерированных статей
- Среднее время генерации
- Процент успешных генераций

---

## 🧪 Шаг 10: Тестирование

### 10.1 Unit тесты

- Тестировать парсинг ответа Gemini
- Тестировать генерацию slug
- Тестировать вычисление readingTime

### 10.2 Интеграционные тесты

- Тестировать полный flow генерации
- Тестировать сохранение в БД
- Тестировать обработку ошибок

### 10.3 Ручное тестирование

- Протестировать генерацию статей разных типов
- Проверить качество сгенерированного контента
- Проверить SEO метаданные

---

## 📋 Чеклист реализации

### Фаза 1: Базовая интеграция
- [ ] Установить `@google/genai`
- [ ] Добавить `GEMINI_API_KEY` в `.env.local`
- [ ] Создать `lib/gemini.ts` с базовыми функциями
- [ ] Создать API route `/api/admin/articles/generate`
- [ ] Протестировать генерацию через API

### Фаза 2: UI компоненты
- [ ] Создать `components/admin/GenerateArticleDialog.tsx`
- [ ] Добавить кнопку в админ-панель
- [ ] Реализовать форму с полями
- [ ] Добавить обработку состояний (loading, error, success)

### Фаза 3: Интеграция и сохранение
- [ ] Интегрировать генерацию с сохранением в БД
- [ ] Реализовать парсинг ответа Gemini
- [ ] Добавить генерацию SEO метаданных
- [ ] Протестировать полный flow

### Фаза 4: Улучшения
- [ ] Добавить предпросмотр статьи
- [ ] Улучшить обработку ошибок
- [ ] Добавить валидацию
- [ ] Оптимизировать промпты

---

## 🚀 Пример использования

### После реализации:

1. Админ заходит в `/admin/articles`
2. Нажимает кнопку "🤖 Сгенерировать статью"
3. Заполняет форму:
   - Тема: "Практика осознанности с символическими свечами"
   - Тип свечи: "Спокойствие"
   - Язык: "Русский"
4. Нажимает "Сгенерировать"
5. Ждет 10-30 секунд (показывается прогресс)
6. Статья автоматически создается как черновик
7. Админ может отредактировать и опубликовать

---

## 📚 Дополнительные ресурсы

- [Gemini API Documentation](https://ai.google.dev/gemini-api/docs)
- [Google GenAI SDK](https://www.npmjs.com/package/@google/genai)
- [Google AI Studio](https://aistudio.google.com/) - для получения API ключа
- [Gemini Models](https://ai.google.dev/gemini-api/docs/models) - список доступных моделей

---

## ⚠️ Важные замечания

1. **API ключ:** Хранить в `.env.local`, не коммитить в репозиторий
2. **Модель:** Использовать `gemini-2.5-flash` для быстрой генерации или `gemini-2.0-flash-exp` для лучшего качества
3. **Rate limits:** Учесть лимиты Gemini API (проверить в документации)
4. **Стоимость:** Учесть стоимость API запросов (Gemini имеет бесплатный tier)
5. **Качество:** Всегда проверять и редактировать сгенерированный контент перед публикацией

---

## 🎯 Ожидаемый результат

После реализации админ сможет:
- ✅ Генерировать SEO-статьи одним кликом
- ✅ Выбирать тему и параметры генерации
- ✅ Автоматически получать оптимизированный контент
- ✅ Сохранять статьи как черновики для дальнейшего редактирования
- ✅ Экономить время на создании контента

---

**Дата создания:** 2025-01-XX  
**Статус:** План готов к реализации

