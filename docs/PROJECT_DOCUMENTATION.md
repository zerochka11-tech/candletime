# 📚 Полная документация проекта CandleTime

**Версия:** 1.1  
**Дата обновления:** 2025-12-12  
**Проект:** CandleTime - Спокойный сервис для зажигания символических свечей онлайн

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура](#архитектура)
3. [Технологический стек](#технологический-стек)
4. [Структура проекта](#структура-проекта)
5. [Библиотеки и утилиты](#библиотеки-и-утилиты)
6. [API Endpoints](#api-endpoints)
7. [Компоненты](#компоненты)
8. [База данных](#база-данных)
9. [Производительность и оптимизация](#производительность-и-оптимизация)
10. [Развертывание](#развертывание)
11. [Тестирование](#тестирование)

---

## 🎯 Обзор проекта

CandleTime — это веб-приложение для зажигания символических свечей онлайн. Проект создан с фокусом на спокойствие, минимализм и внимание к деталям.

### Основные функции

- ✅ **Зажигание символических свечей** - 5 типов свечей (спокойствие, поддержка, память, благодарность, фокус)
- ✅ **Шаблоны свечей** - 12 готовых шаблонов для быстрого создания
- ✅ **Публичные ссылки** - каждая свеча имеет уникальную ссылку для шаринга
- ✅ **Личный кабинет** - история всех созданных свечей
- ✅ **Анонимное использование** - можно использовать без регистрации
- ✅ **Автоматическое погашение** - свечи гаснут по истечении времени (1 час, 24 часа, 7 дней)
- ✅ **Ручное погашение** - возможность погасить свечу вручную
- ✅ **Интерактивная карта** - визуализация свечей на карте мира
- ✅ **FAQ/Блог** - система статей с поддержкой Markdown
- ✅ **SEO оптимизация** - полная поддержка мета-тегов, структурированных данных, sitemap
- ✅ **Генерация статей** - автоматическая генерация SEO-статей через Gemini API
- ✅ **Админ-панель** - управление статьями и промпт-шаблонами

---

## 🏗️ Архитектура

### Паттерн архитектуры

Проект использует **Next.js 16 App Router** с следующими принципами:

- **Server Components** - по умолчанию для лучшей производительности
- **Client Components** - только когда нужна интерактивность (`'use client'`)
- **API Routes** - для серверной логики и интеграций
- **Middleware** - для обработки запросов и редиректов

### Структура данных

```
┌─────────────────┐
│   Next.js App   │
│   (App Router)  │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
┌───▼───┐ ┌──▼────┐
│Client │ │Server │
│Comp.  │ │Comp.  │
└───┬───┘ └──┬────┘
    │        │
    └───┬────┘
        │
┌───────▼────────┐
│  Supabase      │
│  (PostgreSQL)  │
└────────────────┘
```

---

## 💻 Технологический стек

### Frontend

- **Next.js 16** - React фреймворк с App Router
- **React 19** - UI библиотека
- **TypeScript** - строгая типизация
- **Tailwind CSS** - утилитарная стилизация
- **Framer Motion** - анимации
- **Leaflet** - интерактивные карты

### Backend

- **Supabase** - PostgreSQL база данных и аутентификация
- **Google Gemini API** - генерация статей
- **OpenStreetMap Nominatim** - геокодирование

### Инструменты разработки

- **Jest** - тестирование
- **ESLint** - линтинг
- **TypeScript** - проверка типов

### Оптимизация производительности

- **Resource Hints** - preconnect и dns-prefetch для ускорения загрузки
- **Next.js оптимизации** - compress, swcMinify, optimizeCss
- **Динамическая загрузка** - lazy loading не-критических компонентов
- **HTTP заголовки** - оптимизация кэширования и безопасности

---

## 📁 Структура проекта

```
online-candles/
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   │   ├── admin/         # Админские endpoints
│   │   ├── geocode/       # Геокодирование
│   │   └── map/           # Карта свечей
│   ├── admin/             # Админ-панель
│   ├── auth/              # Аутентификация
│   ├── candle/            # Страница свечи
│   ├── candles/           # Список свечей
│   ├── dashboard/         # Личный кабинет
│   ├── faq/               # FAQ/Блог
│   ├── light/             # Зажигание свечи
│   ├── map/               # Карта мира
│   └── page.tsx           # Главная страница
├── components/            # React компоненты
│   ├── admin/            # Админские компоненты
│   ├── geographic/       # Географические компоненты
│   ├── map/              # Компоненты карты
│   └── ui/               # UI компоненты
├── lib/                  # Библиотеки и утилиты
│   ├── admin.ts          # Админские функции
│   ├── constants.ts      # Константы
│   ├── decorativeElements.ts # Декоративные элементы
│   ├── errorHandler.ts   # Обработка ошибок
│   ├── gemini.ts         # Gemini API
│   ├── promptTemplates.ts # Промпт-шаблоны
│   ├── seo.ts            # SEO утилиты
│   ├── supabaseClient.ts # Supabase клиент
│   ├── theme.ts          # Управление темой
│   ├── types.ts          # TypeScript типы
│   ├── utils.ts          # Общие утилиты
│   └── yandexMetrika.ts  # Яндекс.Метрика
├── __tests__/            # Юнит-тесты
│   ├── components/       # Тесты компонентов
│   └── lib/             # Тесты библиотек
├── docs/                 # Документация
│   ├── API.md           # API документация
│   ├── HERO_DECORATIVE_ELEMENTS.md # Декоративные элементы
│   └── PROJECT_DOCUMENTATION.md # Эта документация
└── public/               # Статические файлы
```

---

## 📚 Библиотеки и утилиты

### `lib/utils.ts`

Общие утилиты для работы с данными.

**Функции:**
- `cn(...inputs)` - Объединение классов Tailwind
- `getCandleTypeMeta(type)` - Получение метаданных типа свечи
- `getComputedStatus(candle)` - Вычисление статуса свечи
- `getStatusLabel(status)` - Получение текстовой метки статуса
- `formatDate(date)` - Форматирование даты в DD.MM.YYYY
- `formatRemainingTime(expires)` - Форматирование оставшегося времени

**Примеры использования:**
```typescript
import { formatDate, getCandleTypeMeta } from '@/lib/utils';

const formatted = formatDate(new Date());
const meta = getCandleTypeMeta('calm');
```

### `lib/errorHandler.ts`

Централизованная обработка ошибок.

**Функции:**
- `logError(error, context?)` - Логирование ошибок с контекстом
- `handleSupabaseError(error, context?)` - Обработка ошибок Supabase
- `createErrorMessage(error)` - Создание пользовательского сообщения об ошибке

**Примеры использования:**
```typescript
import { logError, handleSupabaseError } from '@/lib/errorHandler';

try {
  await supabase.from('candles').insert(data);
} catch (error) {
  const message = handleSupabaseError(error, { component: 'LightPage' });
  setError(message);
}
```

### `lib/gemini.ts`

Интеграция с Google Gemini API для генерации статей.

**Функции:**
- `getGeminiClient()` - Создание клиента Gemini API
- `generateSlug(title)` - Генерация URL-friendly slug
- `calculateReadingTime(content)` - Вычисление времени чтения
- `generateExcerpt(content)` - Генерация краткого описания
- `extractTitle(content)` - Извлечение H1 заголовка
- `generateSEOMetadata(title, content)` - Генерация SEO метаданных
- `createArticlePrompt(params)` - Создание промпта для генерации
- `generateArticle(params)` - Генерация статьи через Gemini API

**Примеры использования:**
```typescript
import { generateArticle } from '@/lib/gemini';

const article = await generateArticle({
  topic: 'Медитация',
  candleType: 'calm',
  language: 'ru'
});
```

### `lib/seo.ts`

SEO утилиты для мета-тегов и структурированных данных.

**Функции:**
- `generateMetadata(params)` - Генерация полного набора мета-тегов
- `generateHomePageStructuredData()` - Структурированные данные главной страницы
- `generateCandleStructuredData(params)` - Структурированные данные свечи
- `generateOrganizationStructuredData()` - Структурированные данные организации
- `generateBreadcrumbList(items)` - Структурированные данные хлебных крошек
- `generateCandlesItemList(candles)` - Структурированные данные списка свечей
- `generateArticleStructuredData(params)` - Структурированные данные статьи
- `generateFAQPageStructuredData(faqItems)` - Структурированные данные FAQ
- `generateHowToStructuredData(params)` - Структурированные данные инструкции
- `generateMapStructuredData(params)` - Структурированные данные карты

**Примеры использования:**
```typescript
import { generateMetadata, generateCandleStructuredData } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'Моя свеча',
  description: 'Описание',
  path: '/candle/123'
});
```

### `lib/admin.ts`

Функции для работы с админ-панелью.

**Функции:**
- `checkAdminAccess()` - Проверка прав администратора
- `getAuthToken()` - Получение токена доступа

**Примеры использования:**
```typescript
import { checkAdminAccess } from '@/lib/admin';

const { isAdmin, user } = await checkAdminAccess();
if (isAdmin) {
  // Показать админ-панель
}
```

### `lib/promptTemplates.ts`

Утилиты для работы с промпт-шаблонами.

**Функции:**
- `extractVariablesFromPrompt(prompt)` - Извлечение переменных из промпта
- `replaceTemplateVariables(template, variables)` - Замена переменных в шаблоне
- `validateTemplateVariables(template, providedVariables)` - Валидация переменных
- `validatePromptTemplate(template)` - Валидация промпт-шаблона
- `createVariablesFromSimpleMode(params)` - Создание переменных из простого режима
- `getVariableValue(variables, variableName)` - Получение значения переменной

**Примеры использования:**
```typescript
import { extractVariablesFromPrompt, replaceTemplateVariables } from '@/lib/promptTemplates';

const vars = extractVariablesFromPrompt('Напиши про {topic}');
const result = replaceTemplateVariables('Напиши про {topic}', { topic: 'медитация' });
```

### `lib/yandexMetrika.ts`

Интеграция с Яндекс.Метрикой.

**Функции:**
- `reachGoal(target, params?)` - Отправка цели (конверсии)
- `hit(url, params?)` - Отправка виртуального хита
- `params(params)` - Отправка параметров визита

**Константы:**
- `YandexMetrikaGoals` - Предопределенные цели

**Примеры использования:**
```typescript
import { reachGoal, YandexMetrikaGoals } from '@/lib/yandexMetrika';

reachGoal(YandexMetrikaGoals.LIGHT_CANDLE, { candle_type: 'calm' });
```

### `lib/theme.ts`

Управление темой приложения (light/dark/system).

**Хук:**
- `useTheme()` - React хук для управления темой

**Примеры использования:**
```typescript
import { useTheme } from '@/lib/theme';

const { theme, resolvedTheme, setTheme, mounted } = useTheme();
```

### `lib/decorativeElements.ts`

Утилиты для работы с декоративными элементами HERO блока.

**Функции:**
- `countStarsInRange(minTop, maxTop)` - Подсчет звезд в диапазоне
- `getTotalStarCount()` - Общее количество звезд
- `countCloudsInRange(minTop, maxTop)` - Подсчет облаков в диапазоне
- `getTotalCloudCount()` - Общее количество облаков
- `countSymbolsInRange(minTop, maxTop)` - Подсчет символов в диапазоне
- `getTotalSymbolCount()` - Общее количество символов
- `countGoldenElementsInRange(minTop, maxTop)` - Подсчет золотых элементов
- `getTotalGoldenElementCount()` - Общее количество золотых элементов
- `getTotalDecorativeElementCount()` - Общее количество всех элементов
- `isValidPosition(top)` - Проверка валидности позиции
- `isValidZIndex(decorativeZIndex, contentZIndex)` - Проверка валидности z-index

**Примеры использования:**
```typescript
import { getTotalStarCount, isValidPosition } from '@/lib/decorativeElements';

const total = getTotalStarCount();
const isValid = isValidPosition(50);
```

### `lib/types.ts`

Централизованные TypeScript типы.

**Типы:**
- `CandleTypeId` - Типы свечей
- `CandleStatus` - Статусы свечей
- `Candle` - Интерфейс свечи
- `CandleType` - Интерфейс типа свечи
- `CandleTypeStyle` - Интерфейс стилей типа свечи
- `UserInfo` - Интерфейс информации о пользователе
- `UserStats` - Интерфейс статистики пользователя
- `HomePageStats` - Интерфейс статистики главной страницы

### `lib/constants.ts`

Централизованные константы.

**Константы:**
- `CANDLE_TYPES` - Массив типов свечей
- `DURATION_OPTIONS` - Варианты длительности
- `CANDLE_TYPE_STYLES` - Стили для каждого типа свечи
- `DEFAULT_CANDLE_TYPE_STYLE` - Стили по умолчанию

---

## 🔌 API Endpoints

Полная документация API доступна в [docs/API.md](./API.md).

### Публичные API

- `GET /api/geocode` - Геокодирование адресов
- `GET /api/map/candles` - Получение свечей для карты
- `GET /api/map/stats` - Статистика карты

### Админские API

- `GET /api/admin/check` - Проверка прав администратора
- `GET /api/admin/articles` - Список статей
- `POST /api/admin/articles` - Создание статьи
- `GET /api/admin/articles/[id]` - Получение статьи
- `PUT /api/admin/articles/[id]` - Обновление статьи
- `DELETE /api/admin/articles/[id]` - Удаление статьи
- `POST /api/admin/articles/[id]/approve` - Одобрение статьи
- `POST /api/admin/articles/generate` - Генерация статьи через Gemini
- `POST /api/admin/articles/import` - Импорт статьи
- `GET /api/admin/prompt-templates` - Список промпт-шаблонов
- `POST /api/admin/prompt-templates` - Создание промпт-шаблона
- `GET /api/admin/prompt-templates/[id]` - Получение промпт-шаблона
- `PUT /api/admin/prompt-templates/[id]` - Обновление промпт-шаблона
- `DELETE /api/admin/prompt-templates/[id]` - Удаление промпт-шаблона

---

## 🧩 Компоненты

### Основные компоненты

- `SiteHeader` - Шапка сайта с навигацией
- `SiteFooter` - Подвал сайта
- `ErrorBoundary` - Граница обработки ошибок
- `DynamicMeta` - Динамические мета-теги
- `StructuredData` - Структурированные данные Schema.org
- `ThemeToggle` - Переключатель темы
- `LanguageSwitcher` - Переключатель языка
- `CookieConsent` - Согласие на cookies
- `Analytics` - Компонент аналитики

### Компоненты карты

- `WorldMap` - Основной компонент карты
- `MapMarkers` - Маркеры на карте
- `MapPopup` - Попап для маркера
- `MapControls` - Элементы управления картой
- `MapLegend` - Легенда карты

### Компоненты админ-панели

- `AdminGuard` - Защита админ-роутов
- `MarkdownEditor` - Редактор Markdown
- `PromptTemplateManager` - Менеджер промпт-шаблонов
- `GenerateArticleDialog` - Диалог генерации статьи
- `ConfirmDialog` - Диалог подтверждения

### UI компоненты

- `ArticleSkeleton` - Скелетон для статьи
- `CandleSkeleton` - Скелетон для свечи
- `EmptyState` - Пустое состояние
- `Skeleton` - Базовый скелетон

---

## 🗄️ База данных

Проект использует **Supabase (PostgreSQL)**.

### Основные таблицы

- `candles` - Свечи
- `articles` - Статьи
- `prompt_templates` - Промпт-шаблоны
- `users` - Пользователи (через Supabase Auth)

### Схема базы данных

См. `supabase-setup.sql` для полной схемы.

---

## ⚡ Производительность и оптимизация

### Реализованные оптимизации

#### 1. Resource Hints

В `app/layout.tsx` и `app/[locale]/layout.tsx` добавлены Resource Hints для ускорения загрузки:

```tsx
<link rel="preconnect" href="https://candletime.ru" />
<link rel="preconnect" href="https://vercel.com" crossOrigin="anonymous" />
<link rel="dns-prefetch" href="https://www.googletagmanager.com" />
<link rel="dns-prefetch" href="https://mc.yandex.ru" />
```

**Эффект:**
- Предварительное установление соединений с доменами
- Ускорение загрузки аналитики (Google Analytics, Yandex Metrika)
- Улучшение работы с CDN (Vercel)

#### 2. Next.js конфигурация

В `next.config.ts` настроены оптимизации:

- **compress: true** - включено gzip/brotli сжатие
- **swcMinify: true** - минификация через SWC
- **optimizeCss: true** - оптимизация CSS
- **optimizePackageImports** - tree-shaking для:
  - `lucide-react`
  - `@radix-ui/react-icons`
  - `next-intl`
- **images** - оптимизация изображений (AVIF, WebP)
- **HTTP заголовки** - безопасность и кэширование:
  - `X-DNS-Prefetch-Control: on`
  - `X-Frame-Options: SAMEORIGIN`
  - `X-Content-Type-Options: nosniff`
  - `Referrer-Policy: strict-origin-when-cross-origin`

#### 3. Динамическая загрузка компонентов

Не-критические компоненты загружаются асинхронно:

- `Analytics` - загружается после основного контента
- `YandexMetrika` - загружается после основного контента
- `CookieConsent` - загружается после основного контента

**Реализация:**
```tsx
import dynamic from 'next/dynamic';

const Analytics = dynamic(() => import('@/components/Analytics').then(mod => ({ default: mod.Analytics })), {
  ssr: false,
});
```

#### 4. Оптимизация компонентов аналитики

Компоненты `Analytics` и `YandexMetrika` используют:
- `strategy="lazyOnload"` - загрузка после полной загрузки страницы
- Проверка `mounted` state для избежания hydration mismatch
- Загрузка только при наличии согласия пользователя

### Метрики производительности

**Целевые показатели:**
- **LCP (Largest Contentful Paint):** < 2.5 с
- **FID (First Input Delay):** < 100 мс
- **CLS (Cumulative Layout Shift):** < 0.1
- **Время блокировки запросов:** < 100 мс
- **onContentLoad:** < 2,000 мс
- **onLoad:** < 3,000 мс

### Мониторинг производительности

Для отслеживания производительности используются:
- **Google Analytics** - Web Vitals
- **Yandex Metrika** - метрики загрузки
- **Chrome DevTools** - Performance tab
- **Lighthouse** - аудит производительности

### Дополнительные рекомендации

См. [PERFORMANCE_OPTIMIZATION_PLAN.md](../PERFORMANCE_OPTIMIZATION_PLAN.md) для полного плана оптимизации.

---

## 🚀 Развертывание

### Локальная разработка

```bash
npm install
npm run dev
```

### Продакшн

1. Настройка Supabase
2. Деплой на Vercel
3. Настройка переменных окружения
4. Настройка домена

Подробная инструкция: [DEPLOYMENT_GUIDE.md](../DEPLOYMENT_GUIDE.md)

---

## 🧪 Тестирование

### Запуск тестов

```bash
npm test              # Запуск всех тестов
npm run test:watch    # Режим watch
npm run test:coverage # С покрытием кода
```

### Структура тестов

- `__tests__/lib/` - Тесты библиотек
- `__tests__/components/` - Тесты компонентов

### Покрытие тестами

**Статус:** ✅ Все тесты проходят (223 теста, 9 тестовых наборов)

Текущее покрытие:
- ✅ `lib/utils.ts` - 100%
- ✅ `lib/decorativeElements.ts` - 100%
- ✅ `lib/errorHandler.ts` - покрыт тестами
- ✅ `lib/gemini.ts` - покрыт тестами
- ✅ `lib/seo.ts` - покрыт тестами
- ✅ `lib/admin.ts` - покрыт тестами
- ✅ `lib/promptTemplates.ts` - покрыт тестами
- ✅ `lib/yandexMetrika.ts` - покрыт тестами
- ✅ `lib/theme.ts` - покрыт тестами
- ✅ `components/HeroDecorativeElements.tsx` - покрыт тестами

**Зависимости для тестирования:**
- `@testing-library/react` - для тестирования React компонентов и хуков
- `@testing-library/jest-dom` - дополнительные матчеры для DOM
- `jest` - тестовый фреймворк
- `jest-environment-jsdom` - окружение для тестирования браузерных API

---

## 📖 Дополнительная документация

### Техническая документация

- [API Документация](./API.md) - Полная документация всех API endpoints
- [Декоративные элементы HERO блока](./HERO_DECORATIVE_ELEMENTS.md) - Документация по декоративным элементам
- [README.md](../README.md) - Основной README проекта

### Гайды по настройке

- [DEPLOYMENT_GUIDE.md](../DEPLOYMENT_GUIDE.md) - Руководство по развертыванию
- [ANALYTICS_SETUP_GUIDE.md](../ANALYTICS_SETUP_GUIDE.md) - Настройка аналитики (Google Analytics, Yandex Metrika)
- [CHECK_INDEXATION_GUIDE.md](../CHECK_INDEXATION_GUIDE.md) - Проверка индексации в поисковых системах
- [GDPR_PRIVACY_GUIDE.md](../GDPR_PRIVACY_GUIDE.md) - Настройка GDPR и приватности

### Оптимизация и производительность

- [PERFORMANCE_OPTIMIZATION_PLAN.md](../PERFORMANCE_OPTIMIZATION_PLAN.md) - План оптимизации производительности
- [HAR_ANALYSIS_REPORT.md](../HAR_ANALYSIS_REPORT.md) - Анализ производительности из HAR файла

**Важно:** Вся основная проектная документация ведется в этом файле (`docs/PROJECT_DOCUMENTATION.md`). Остальные документы являются специфичными гайдами и инструкциями.

---

## 🔄 История изменений

- **2025-12-12**: 
  - Добавлены оптимизации производительности (Resource Hints, Next.js конфигурация, динамическая загрузка компонентов)
  - Исправлены все тесты (223 теста проходят успешно)
  - Установлена зависимость `@testing-library/react`
  - Обновлена документация проекта
- **2025-01-15**: Создана полная документация проекта, добавлены юнит-тесты


