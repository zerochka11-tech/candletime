# 🚀 Настройка Gemini API для генерации SEO-статей

## ✅ Что реализовано

1. ✅ Установлен `@google/genai` SDK
2. ✅ Создан `lib/gemini.ts` с утилитами для работы с Gemini API
3. ✅ Создан API route `/api/admin/articles/generate`
4. ✅ Создан компонент `GenerateArticleDialog.tsx`
5. ✅ Интегрирована кнопка генерации в админ-панель
6. ✅ Добавлена обработка ошибок и состояний

## 📋 Настройка

### 1. Получение API ключа

1. Перейдите на [Google AI Studio](https://aistudio.google.com/app/apikey)
2. Войдите в свой Google аккаунт
3. Нажмите "Get API key" или "Create API key"
4. Скопируйте полученный API ключ

### 2. Добавление ключа в переменные окружения

Добавьте в `.env.local`:

```env
GEMINI_API_KEY=your-gemini-api-key-here
```

**Важно:** 
- Никогда не коммитьте `.env.local` в репозиторий
- В production добавьте переменную в настройки Vercel/хостинга

### 3. Проверка работы

1. Запустите dev сервер: `npm run dev`
2. Войдите в админ-панель: `/admin/articles`
3. Нажмите кнопку "🤖 Сгенерировать статью"
4. Заполните форму и нажмите "Сгенерировать статью"

## 🎯 Использование

### Генерация статьи

1. **Тема статьи** (обязательно) - опишите тему подробно (10-200 символов)
2. **Тип свечи** (опционально) - выберите тип для CTA в конце статьи
3. **Язык** - Русский или English
4. **Категория** (опционально) - выберите категорию из списка

### Что происходит при генерации

1. Запрос отправляется в Gemini API
2. Генерируется статья длиной 1200-1800 слов
3. Автоматически извлекаются:
   - Заголовок (из H1)
   - Контент (Markdown)
   - Excerpt (первые 150 символов)
   - SEO метаданные (title, description, keywords)
   - Время чтения
   - Slug
4. Статья сохраняется в БД как **черновик** (`published: false`)
5. Статья появляется в разделе "Черновики"

### Редактирование и публикация

После генерации:
1. Откройте статью из списка черновиков
2. Проверьте и отредактируйте контент при необходимости
3. Нажмите "Опубликовать" для публикации

## 🔧 Технические детали

### Модель Gemini

Используется модель: `gemini-2.0-flash-exp`

**Параметры генерации:**
- Temperature: 0.7 (баланс между креативностью и точностью)
- Формат: Markdown
- Длина: 1200-1800 слов

### Структура статьи

1. H1 заголовок (извлекается в `title`)
2. Введение (2-3 параграфа)
3. Основной контент (H2, H3 разделы)
4. Практические советы
5. Заключение с CTA (если указан тип свечи)

### SEO оптимизация

Автоматически генерируются:
- `seo_title` - оптимизированный заголовок
- `seo_description` - описание 150-160 символов
- `seo_keywords` - массив ключевых слов (до 10)

## ⚠️ Ограничения и рекомендации

### Rate Limits

Gemini API имеет лимиты на количество запросов:
- Бесплатный tier: проверьте актуальные лимиты в [документации](https://ai.google.dev/gemini-api/docs/quota)
- При превышении лимита система автоматически:
  1. Повторит запрос через 2 секунды
  2. Если снова ошибка - через 4 секунды
  3. Если снова ошибка - через 8 секунд
  4. Если все попытки исчерпаны - покажет понятное сообщение об ошибке

**Fallback на другие модели:**
- При rate limit система автоматически пробует другие модели Gemini
- Порядок: `gemini-2.0-flash-exp` → `gemini-2.0-flash` → `gemini-1.5-flash` → `gemini-1.5-pro`

### Качество контента

- Всегда проверяйте сгенерированный контент перед публикацией
- Редактируйте при необходимости
- Добавляйте свои примеры и опыт

### Стоимость

- Gemini API имеет бесплатный tier
- Проверьте актуальные цены в [документации](https://ai.google.dev/gemini-api/docs/pricing)

## 🐛 Решение проблем

### Ошибка "GEMINI_API_KEY is not set"

**Решение:** Добавьте `GEMINI_API_KEY` в `.env.local`

### Ошибка "API rate limit exceeded"

**Решение:** 
1. Система автоматически повторит запрос несколько раз с задержкой
2. Если все попытки исчерпаны - подождите 1-2 минуты и попробуйте снова
3. Проверьте лимиты в [Google AI Studio](https://aistudio.google.com/)
4. Рассмотрите возможность обновления до платного плана для увеличения лимитов

### Ошибка "Failed to generate article"

**Решение:**
1. Проверьте, что API ключ правильный
2. Проверьте подключение к интернету
3. Проверьте логи в консоли браузера

### Статья не сохраняется

**Решение:**
1. Проверьте права доступа (должны быть админом)
2. Проверьте логи сервера
3. Убедитесь, что база данных доступна

## 📚 Дополнительные ресурсы

- [Gemini API Documentation](https://ai.google.dev/gemini-api/docs)
- [Google AI Studio](https://aistudio.google.com/)
- [Gemini Models](https://ai.google.dev/gemini-api/docs/models)
- [Pricing](https://ai.google.dev/gemini-api/docs/pricing)

## 🎉 Готово!

Теперь вы можете генерировать SEO-статьи одним кликом прямо из админ-панели!

