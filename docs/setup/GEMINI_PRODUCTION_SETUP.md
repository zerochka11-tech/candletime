# 🚀 Настройка Gemini API для Production

## ❌ Ошибка: "GEMINI_API_KEY is not set in environment variables"

Эта ошибка возникает, когда переменная окружения `GEMINI_API_KEY` не установлена в Vercel.

## ✅ Решение

### Шаг 1: Получи Gemini API ключ

1. Перейди на [Google AI Studio](https://aistudio.google.com/app/apikey)
2. Войди в свой Google аккаунт
3. Нажми **"Get API key"** или **"Create API key"**
4. Скопируй полученный API ключ

### Шаг 2: Добавь переменную в Vercel

1. Зайди в свой проект на [vercel.com](https://vercel.com)
2. Перейди в **Settings** → **Environment Variables**
3. Нажми **"Add New"**
4. Заполни:
   - **Key:** `GEMINI_API_KEY`
   - **Value:** твой Gemini API ключ (скопированный из Google AI Studio)
   - **Environment:** выбери все (Production, Preview, Development)
5. Нажми **Save**

### Шаг 3: Перезапусти деплой

**Важно:** После добавления переменной нужно перезапустить деплой!

1. Перейди в **Deployments**
2. Найди последний деплой
3. Нажми на **"..."** (три точки) → **Redeploy**
4. Или сделай новый коммит и push в GitHub (автоматически запустится новый деплой)

### Шаг 4: Проверка

После перезапуска деплоя:
1. Зайди в админ-панель: `/admin/articles`
2. Нажми "🤖 Сгенерировать статью"
3. Ошибка должна исчезнуть

---

## 🔍 Проверка переменных в Vercel

Чтобы убедиться, что переменная установлена:

1. В Vercel: **Settings** → **Environment Variables**
2. Должна быть видна:
   - ✅ `GEMINI_API_KEY`

**Важно:** 
- Переменная должна быть добавлена для **Production** окружения
- Если используешь Preview/Development - добавь и для них тоже

---

## ⚠️ Важные замечания

1. **Безопасность:**
   - `GEMINI_API_KEY` - это секретный ключ, не коммить его в Git
   - Хранится только в Vercel Environment Variables
   - Не используй префикс `NEXT_PUBLIC_` (это серверная переменная)

2. **Опциональность:**
   - Если не используешь генерацию статей, эта переменная не обязательна
   - Функция генерации просто не будет работать без неё

3. **Лимиты:**
   - Gemini API имеет бесплатный tier с ограничениями
   - Проверь лимиты в [Google AI Studio](https://aistudio.google.com/)

---

## 🆘 Если ошибка сохраняется

1. **Проверь имя переменной:**
   - Должно быть точно: `GEMINI_API_KEY` (без пробелов, с заглавными буквами)

2. **Проверь окружение:**
   - Убедись, что переменная добавлена для **Production**

3. **Перезапусти деплой:**
   - После добавления переменной обязательно перезапусти деплой

4. **Проверь логи:**
   - В Vercel: **Deployments** → выбери деплой → **View Function Logs**
   - Ищи ошибки связанные с `GEMINI_API_KEY`

5. **Проверь API ключ:**
   - Убедись, что ключ скопирован полностью (без пробелов в начале/конце)
   - Попробуй создать новый ключ в Google AI Studio

---

## 📝 Пример значения

```
GEMINI_API_KEY=AIzaSyAbCdEfGhIjKlMnOpQrStUvWxYz1234567
```

**Важно:** Используй свой реальный ключ из Google AI Studio!

---

## 🔗 Полезные ссылки

- [Google AI Studio](https://aistudio.google.com/app/apikey) - получить API ключ
- [Gemini API Documentation](https://ai.google.dev/gemini-api/docs)
- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

---

## ✅ Чеклист

- [ ] Получен API ключ в Google AI Studio
- [ ] Добавлена переменная `GEMINI_API_KEY` в Vercel
- [ ] Переменная добавлена для Production окружения
- [ ] Перезапущен деплой в Vercel
- [ ] Проверена работа генерации статей

---

**После выполнения всех шагов ошибка должна исчезнуть!** 🎉

