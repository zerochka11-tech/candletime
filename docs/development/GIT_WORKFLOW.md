# 🔄 Git Workflow: Как работать с GitHub

## 📋 Основные понятия

### Что такое Git?
Git — система контроля версий, которая отслеживает изменения в коде.

### Что такое GitHub?
GitHub — облачное хранилище для Git репозиториев (твой код в интернете).

---

## 🔄 Процесс работы с кодом

### 1. Локальные изменения
Когда ты изменяешь файлы на своем компьютере, они находятся только у тебя локально.

### 2. Коммит (commit)
Сохраняешь изменения в Git с описанием:
```bash
git add .
git commit -m "Описание изменений"
```

### 3. Push (отправка на GitHub)
Отправляешь изменения на GitHub:
```bash
git push
```

---

## ❓ Нужно ли делать push каждый раз?

### ✅ Да, нужно делать push когда:
1. **Хочешь сохранить изменения на GitHub** — чтобы код был в облаке
2. **Работаешь с несколькими компьютерами** — чтобы синхронизировать код
3. **Деплой на Vercel** — Vercel автоматически деплоит при каждом push
4. **Бэкап кода** — чтобы не потерять изменения
5. **Работа в команде** — чтобы другие видели твои изменения

### ❌ Нет, не обязательно когда:
1. **Только локальная разработка** — если работаешь только на одном компьютере
2. **Изменения еще не готовы** — можно делать несколько коммитов локально
3. **Экспериментируешь** — можно тестировать локально без push

---

## 🎯 Рекомендуемый workflow

### Вариант 1: Частые push (рекомендуется)
```bash
# После каждого значимого изменения
git add .
git commit -m "Добавил новую функцию"
git push
```

**Плюсы:**
- ✅ Код всегда в облаке (бэкап)
- ✅ Vercel автоматически деплоит
- ✅ Легко откатить изменения
- ✅ История изменений понятна

### Вариант 2: Накопительные коммиты
```bash
# Делаешь несколько изменений локально
git add .
git commit -m "Исправил баги и добавил функции"
git push  # Отправляешь все сразу
```

**Плюсы:**
- ✅ Меньше push операций
- ✅ Можно группировать связанные изменения

---

## 📝 Типичный рабочий процесс

### День 1: Начало работы
```bash
# Получаешь последние изменения (если работаешь с нескольких мест)
git pull

# Делаешь изменения в коде
# ... редактируешь файлы ...

# Сохраняешь изменения
git add .
git commit -m "Добавил новую страницу"
git push
```

### День 2: Продолжение работы
```bash
# Получаешь изменения (если работал с другого компьютера)
git pull

# Делаешь новые изменения
# ... редактируешь файлы ...

# Сохраняешь и отправляешь
git add .
git commit -m "Исправил ошибку на главной странице"
git push
```

---

## 🚀 Автоматический деплой на Vercel

### Как это работает:
1. Ты делаешь `git push` на GitHub
2. Vercel автоматически видит изменения
3. Vercel запускает новый деплой
4. Через 2-5 минут сайт обновляется

### Важно:
- ✅ Каждый push = новый деплой
- ✅ Можно отключить автодеплой в настройках Vercel
- ✅ Можно настроить деплой только из определенной ветки (например, `main`)

---

## 📚 Полезные команды Git

### Проверка статуса
```bash
git status
```
Показывает, какие файлы изменены, но не добавлены в коммит.

### Просмотр изменений
```bash
git diff
```
Показывает, что именно изменилось в файлах.

### Просмотр истории
```bash
git log
```
Показывает историю коммитов.

### Получение изменений с GitHub
```bash
git pull
```
Скачивает и применяет изменения с GitHub (если работаешь с нескольких мест).

### Отмена изменений (если что-то пошло не так)
```bash
# Отменить изменения в файле (до git add)
git checkout -- имя-файла

# Отменить git add (но оставить изменения)
git reset HEAD имя-файла

# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1
```

---

## 🎯 Рекомендации

### ✅ Хорошие практики:
1. **Делай коммиты часто** — после каждого логического изменения
2. **Пиши понятные сообщения коммитов** — "Добавил поиск" лучше чем "fix"
3. **Делай push регулярно** — хотя бы раз в день
4. **Проверяй перед push** — убедись, что код работает локально

### ❌ Чего избегать:
1. **Не коммить `.env.local`** — он уже в `.gitignore`
2. **Не коммить `node_modules`** — он уже в `.gitignore`
3. **Не делать огромные коммиты** — лучше несколько маленьких
4. **Не забывать делать push** — иначе потеряешь изменения при сбое

---

## 🔄 Типичные сценарии

### Сценарий 1: Добавил новую функцию
```bash
# 1. Редактируешь файлы
# 2. Тестируешь локально (npm run dev)
# 3. Если всё работает:
git add .
git commit -m "Добавил функцию поиска свечей"
git push
# 4. Vercel автоматически задеплоит
```

### Сценарий 2: Исправил баг
```bash
# 1. Нашел и исправил ошибку
# 2. Проверил, что работает
git add .
git commit -m "Исправил ошибку отображения времени"
git push
```

### Сценарий 3: Несколько изменений за раз
```bash
# Можно сделать несколько коммитов локально:
git add файл1.tsx
git commit -m "Исправил баг 1"

git add файл2.tsx
git commit -m "Исправил баг 2"

# И отправить все сразу:
git push
```

---

## 💡 Советы

1. **Используй понятные сообщения коммитов:**
   - ✅ "Добавил поиск на странице свечей"
   - ✅ "Исправил ошибку валидации формы"
   - ❌ "fix"
   - ❌ "update"

2. **Делай push перед важными изменениями:**
   - Перед большими рефакторингами
   - Перед экспериментами
   - В конце рабочего дня

3. **Используй ветки для экспериментов (опционально):**
   ```bash
   git checkout -b experiment
   # делаешь изменения
   git commit -m "Эксперимент"
   git push origin experiment
   # если всё ок, можно смержить в main
   ```

---

## ✅ Чеклист перед push

- [ ] Код работает локально (`npm run dev`)
- [ ] Нет ошибок линтера (`npm run lint`)
- [ ] Сборка проходит (`npm run build`)
- [ ] Понятное сообщение коммита
- [ ] Не коммитишь `.env.local` или другие секреты

---

## 🆘 Если что-то пошло не так

### Ошибка: "Your branch is ahead of origin"
**Решение:** Просто сделай `git push`

### Ошибка: "Updates were rejected"
**Решение:** Сначала сделай `git pull`, потом `git push`

### Ошибка: "Nothing to commit"
**Решение:** Нет изменений для коммита, всё уже сохранено

---

## 📖 Итого

**Короткий ответ:** Да, нужно делать push каждый раз, когда хочешь:
- Сохранить изменения на GitHub
- Задеплоить изменения на Vercel
- Сделать бэкап кода

**Но можно:**
- Делать несколько коммитов локально, потом один push
- Работать локально без push (но риск потерять изменения)

**Рекомендация:** Делай push после каждого значимого изменения или хотя бы раз в день.

