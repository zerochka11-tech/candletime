# 🌙 Руководство по темной теме (Dark Mode)

## Обзор

Темная тема полностью интегрирована в проект CandleTime. Пользователи могут переключаться между светлой и темной темой, а также использовать системную тему.

## Функциональность

### ✅ Реализовано

1. **Хук управления темой** (`lib/theme.ts`)
   - Поддержка трех режимов: `light`, `dark`, `system`
   - Автоматическое определение системной темы
   - Сохранение выбора в `localStorage`
   - Отслеживание изменений системной темы

2. **Переключатель темы** (`components/ThemeToggle.tsx`)
   - Кнопка переключения в хедере
   - Иконки для светлой и темной темы
   - Предотвращение мигания при загрузке

3. **Инициализация темы** (`components/ThemeScript.tsx`)
   - Скрипт для применения темы до гидратации React
   - Предотвращает мигание при загрузке страницы

4. **Адаптированные компоненты**
   - ✅ `SiteHeader` - хедер с темной темой
   - ✅ `SiteFooter` - футер с темной темой
   - ✅ `CookieConsent` - баннер cookies с темной темой
   - ✅ Глобальные стили (`app/globals.css`)

5. **Конфигурация Tailwind**
   - Включен `darkMode: 'class'` в `tailwind.config.js`
   - Все компоненты используют классы `dark:*`

## Использование

### Переключение темы

Пользователи могут переключать тему через кнопку в хедере (иконка солнца/луны).

### Программное управление

```typescript
import { useTheme } from '@/lib/theme';

function MyComponent() {
  const { theme, resolvedTheme, setTheme, mounted } = useTheme();

  // theme: 'light' | 'dark' | 'system'
  // resolvedTheme: 'light' | 'dark' (фактическая тема)
  // setTheme: функция для установки темы
  // mounted: true когда компонент смонтирован (предотвращает мигание)

  return (
    <button onClick={() => setTheme('dark')}>
      Включить темную тему
    </button>
  );
}
```

## Добавление темной темы к новым компонентам

### Базовые классы

```tsx
// Фон
className="bg-white dark:bg-slate-900"

// Текст
className="text-slate-900 dark:text-slate-100"

// Границы
className="border-slate-300 dark:border-slate-700"

// Hover эффекты
className="hover:bg-slate-50 dark:hover:bg-slate-800"
```

### Примеры

```tsx
// Карточка
<div className="bg-white dark:bg-slate-800 border border-slate-300 dark:border-slate-700 rounded-lg p-4">
  <h2 className="text-slate-900 dark:text-slate-100">Заголовок</h2>
  <p className="text-slate-600 dark:text-slate-400">Текст</p>
</div>

// Кнопка
<button className="bg-slate-900 dark:bg-white text-white dark:text-slate-900 hover:bg-slate-800 dark:hover:bg-slate-100">
  Кнопка
</button>

// Input
<input className="bg-white dark:bg-slate-800 border border-slate-300 dark:border-slate-700 text-slate-900 dark:text-slate-100" />
```

## Плавные переходы

Все компоненты используют `transition-colors duration-200` для плавных переходов между темами.

## Технические детали

### Структура файлов

```
lib/
  theme.ts              # Хук useTheme
components/
  ThemeToggle.tsx       # Переключатель темы
  ThemeScript.tsx       # Скрипт инициализации
app/
  globals.css           # Глобальные стили с темной темой
tailwind.config.js      # Конфигурация dark mode
```

### Логика работы

1. При загрузке страницы `ThemeScript` применяет тему из `localStorage` или системную
2. `useTheme` хук синхронизирует состояние с DOM
3. При изменении темы обновляется класс `dark` на `<html>`
4. Tailwind автоматически применяет стили `dark:*`

## Известные ограничения

- Некоторые страницы могут требовать дополнительной адаптации
- OG изображения не зависят от темы (это нормально)
- Некоторые сторонние компоненты могут не поддерживать темную тему

## Будущие улучшения

- [ ] Адаптация всех страниц под темную тему
- [ ] Кастомные цвета для разных типов свечей в темной теме
- [ ] Анимации при переключении темы
- [ ] Настройки темы в профиле пользователя

---

**Последнее обновление:** 2025  
**Версия:** 1.0

