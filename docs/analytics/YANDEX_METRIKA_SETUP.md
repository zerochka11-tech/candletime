# 📊 Настройка Яндекс.Метрики для CandleTime

**Статус:** ✅ Файл верификации добавлен  
**Дата:** Январь 2025

---

## ✅ Что уже сделано

1. ✅ Файл верификации создан: `public/yandex_c1fb551ad90d3556.html`
2. ✅ Компонент Яндекс.Метрики создан: `components/YandexMetrika.tsx`
3. ✅ Компонент подключен в `app/layout.tsx`
4. ✅ ID счетчика настроен: `105780499`
5. ✅ Настройки счетчика обновлены (ssr, webvisor, clickmap, ecommerce, etc.)
6. ✅ GDPR-совместимость (загружается только с согласием пользователя)

---

## 📋 Следующие шаги

### Шаг 1: Подтверждение прав на домен

1. Перейди на страницу верификации в Яндекс.Вебмастере
2. Нажми кнопку **"Подтвердить"** для проверки файла
3. Яндекс проверит наличие файла `yandex_c1fb551ad90d3556.html` по адресу:
   ```
   https://candletime.ru/yandex_c1fb551ad90d3556.html
   ```
4. После подтверждения домен будет добавлен в Яндекс.Вебмастер

---

### Шаг 2: Создание счетчика Яндекс.Метрики

1. **Перейди на [metrika.yandex.ru](https://metrika.yandex.ru)**
2. **Войди в свой Яндекс аккаунт** (или создай новый)
3. **Нажми "Добавить счетчик"**
4. **Заполни данные:**
   - **Название счетчика:** `CandleTime`
   - **Сайт:** `https://candletime.ru`
   - **Часовой пояс:** Москва (или твой часовой пояс)
   - **Согласие:** Прими пользовательское соглашение
5. **Нажми "Создать"**

---

### Шаг 3: Получение ID счетчика

После создания счетчика:

1. **Скопируй ID счетчика** (цифры, например: `12345678`)
   - ID можно найти в верхней части страницы счетчика
   - Или в настройках счетчика → "Код счетчика"

---

### Шаг 4: Добавление ID в переменные окружения (опционально)

**✅ ID счетчика уже настроен в коде как fallback (`105780499`), но рекомендуется использовать переменную окружения.**

1. **Создай файл `.env.local`** в корне проекта (если его нет):
   ```bash
   # В корне проекта
   touch .env.local
   ```

2. **Добавь ID счетчика в `.env.local`:**
   ```env
   NEXT_PUBLIC_YANDEX_METRIKA_ID=105780499
   ```
   
   **Примечание:** Если не добавишь в `.env.local`, счетчик будет использовать ID `105780499`, который уже установлен в коде.

3. **Если файл `.env.local` уже существует**, просто добавь строку выше.

---

### Шаг 5: Добавление ID в Vercel (для продакшена)

Если проект развернут на Vercel:

1. Перейди в настройки проекта на [vercel.com](https://vercel.com)
2. Открой **Settings** → **Environment Variables**
3. Добавь переменную:
   - **Name:** `NEXT_PUBLIC_YANDEX_METRIKA_ID`
   - **Value:** `105780499`
   - **Environment:** Production, Preview, Development
4. **Нажми "Save"**
5. **Перезапусти деплоймент** (или он перезапустится автоматически)

**Примечание:** Если не добавишь переменную в Vercel, счетчик все равно будет работать, так как ID уже установлен в коде как fallback.

---

### Шаг 6: Настройка счетчика в Яндекс.Метрике

После добавления ID счетчика, настрой его в Яндекс.Метрике:

#### 6.1. Включить Webvisor (записи сессий)

1. Перейди в настройки счетчика
2. Включи **"Вебвизор"** (Webvisor)
3. Это позволит записывать сессии пользователей для анализа

#### 6.2. Включить карты кликов

1. В настройках счетчика включи **"Карты кликов"**
2. Это покажет, куда кликают пользователи на сайте

#### 6.3. Настроить цели (опционально, но рекомендуется)

Создай цели для отслеживания важных действий:

1. **Перейди в "Настройки" → "Цели"**
2. **Добавь цели:**

   **Цель 1: Зажигание свечи**
   - Тип: JavaScript-событие
   - Условие: `light_candle` или `candle_created`
   - Название: "Зажигание свечи"

   **Цель 2: Регистрация**
   - Тип: JavaScript-событие
   - Условие: `sign_up` или `user_registered`
   - Название: "Регистрация"

   **Цель 3: Просмотр свечи**
   - Тип: JavaScript-событие
   - Условие: `view_candle` или `candle_viewed`
   - Название: "Просмотр свечи"

   **Цель 4: Поделиться**
   - Тип: JavaScript-событие
   - Условие: `share_clicked`
   - Название: "Поделиться"

#### 6.4. Настроить уведомления

1. В настройках счетчика перейди в **"Уведомления"**
2. Включи уведомления о:
   - Резком изменении посещаемости
   - Достижении целей
   - Ошибках на сайте

---

## 🧪 Проверка работы

### 1. Проверка файла верификации

Открой в браузере:
```
https://candletime.ru/yandex_c1fb551ad90d3556.html
```

Должна отобразиться страница с текстом:
```
Verification: c1fb551ad90d3556
```

### 2. Проверка счетчика

1. **Добавь ID в `.env.local`** (см. Шаг 4)
2. **Перезапусти dev-сервер:**
   ```bash
   npm run dev
   ```
3. **Открой сайт** в браузере
4. **Прими согласие на cookies** (если появится)
5. **Проверь в Яндекс.Метрике:**
   - Перейди на metrika.yandex.ru
   - Открой свой счетчик
   - Должен появиться визит (может занять 1-2 минуты)

### 3. Проверка в консоли браузера

1. Открой **DevTools** (F12)
2. Перейди во вкладку **Console**
3. Не должно быть ошибок связанных с Яндекс.Метрикой
4. Во вкладке **Network** должен быть запрос к `mc.yandex.ru`

---

## 🔧 Отправка событий в Яндекс.Метрику

Компонент уже настроен на автоматическое отслеживание. Для отправки кастомных событий используй файл `lib/yandexMetrika.ts`:

```typescript
import { reachGoal } from '@/lib/yandexMetrika'

// Отправка цели
reachGoal('light_candle', {
  candle_type: 'calm',
  duration: 24
})
```

---

## 📊 Настройки, которые уже включены

В компоненте `YandexMetrika.tsx` уже настроены:

- ✅ **ssr: true** — поддержка Server-Side Rendering (Next.js)
- ✅ **webvisor: true** — записи сессий (Webvisor)
- ✅ **clickmap: true** — карты кликов
- ✅ **ecommerce: "dataLayer"** — поддержка электронной коммерции через dataLayer
- ✅ **accurateTrackBounce: true** — точный показатель отказов
- ✅ **trackLinks: true** — отслеживание переходов по ссылкам

---

## 🔒 GDPR и приватность

Компонент Яндекс.Метрики:

- ✅ Загружается **только с согласия пользователя**
- ✅ Использует **cookie consent** систему
- ✅ Не загружается, если пользователь не дал согласие

Это соответствует требованиям GDPR и российского законодательства.

---

## ⚠️ Важно

1. **Не коммить `.env.local` в git** — файл уже должен быть в `.gitignore`
2. **Добавь ID в Vercel** для продакшена
3. **После добавления ID перезапусти dev-сервер**
4. **Проверь работу на продакшене** после деплоя

---

## 📝 Чек-лист

- [ ] Файл верификации добавлен ✅
- [ ] Подтверждены права на домен в Яндекс.Вебмастере
- [ ] Создан счетчик в Яндекс.Метрике
- [ ] ID счетчика добавлен в `.env.local`
- [ ] ID счетчика добавлен в Vercel (для продакшена)
- [ ] Включен Webvisor в настройках счетчика
- [ ] Включены карты кликов
- [ ] Настроены цели (опционально)
- [ ] Проверена работа счетчика на сайте
- [ ] Проверена работа на продакшене

---

## 🆘 Проблемы и решения

### Счетчик не работает

1. **Проверь ID в `.env.local`** — должен быть правильный формат (только цифры)
2. **Проверь, что принято согласие на cookies** — счетчик загружается только после согласия
3. **Проверь консоль браузера** на наличие ошибок
4. **Убедись, что файл `.env.local` находится в корне проекта**

### Файл верификации не открывается

1. **Проверь, что файл находится в `public/yandex_c1fb551ad90d3556.html`**
2. **Убедись, что сайт развернут** — файл должен быть доступен по URL
3. **Проверь права доступа** к файлу на сервере

### Данные не появляются в Метрике

1. **Подожди 1-2 минуты** — данные могут появляться с задержкой
2. **Проверь, что счетчик активирован** в настройках
3. **Убедись, что ID правильный** и добавлен в переменные окружения

---

**Документ создан:** Январь 2025  
**Версия:** 1.0

