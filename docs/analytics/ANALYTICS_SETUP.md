# 📊 Подробное руководство по настройке аналитики для CandleTime

## 🎯 Обзор

Это руководство поможет настроить три основных инструмента аналитики для проекта CandleTime:
1. **Google Analytics 4** — основной инструмент для отслеживания поведения пользователей
2. **Yandex.Metrica** — дополнительная аналитика для русскоязычной аудитории
3. **Google Search Console** — мониторинг поисковой оптимизации

---

## 1. 📈 Google Analytics 4 (GA4)

### 1.1. Создание аккаунта и свойства

#### Шаг 1: Регистрация
1. Перейди на [analytics.google.com](https://analytics.google.com)
2. Войди в свой Google аккаунт (или создай новый)
3. Нажми **"Начать измерение"** (Start measuring)

#### Шаг 2: Создание аккаунта
1. **Название аккаунта:** `CandleTime` (или любое другое)
2. **Настройки аккаунта:**
   - Страна: Россия (или твоя страна)
   - Валюта: RUB (или другая)
3. Нажми **"Далее"**

#### Шаг 3: Создание свойства
1. **Название свойства:** `CandleTime Website`
2. **Часовой пояс:** Выбери свой часовой пояс (например, Москва)
3. **Валюта:** RUB
4. Нажми **"Далее"**

#### Шаг 4: Информация о бизнесе
1. **Отрасль:** Выбери "Другое" или "Технологии"
2. **Размер бизнеса:** Выбери подходящий вариант
3. **Как планируешь использовать Google Analytics:** Выбери нужные опции
4. Нажми **"Создать"**

#### Шаг 5: Принятие условий
- Прими условия использования Google Analytics
- Нажми **"Принять"**

### 1.2. Получение Measurement ID

После создания свойства ты получишь **Measurement ID** в формате: `G-XXXXXXXXXX`

**Где найти:**
1. В левом меню выбери **"Администратор"** (шестеренка внизу)
2. В разделе **"Свойство"** → **"Потоки данных"**
3. Выбери свой поток данных (обычно называется "Web stream")
4. Скопируй **Measurement ID** (начинается с `G-`)

### 1.3. Установка на сайт (Next.js)

#### Вариант A: Использование библиотеки `@next/third-parties` (рекомендуется)

1. **Установи пакет:**
```bash
npm install @next/third-parties
```

2. **Добавь в `app/layout.tsx`:**
```tsx
import { GoogleAnalytics } from '@next/third-parties/google'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ru">
      <body>
        {children}
        <GoogleAnalytics gaId="G-XXXXXXXXXX" />
      </body>
    </html>
  )
}
```

Замени `G-XXXXXXXXXX` на свой Measurement ID.

#### Вариант B: Ручная установка через `gtag.js`

1. **Создай файл `lib/gtag.ts`:**
```typescript
export const GA_MEASUREMENT_ID = process.env.NEXT_PUBLIC_GA_ID || ''

// Функция для отправки событий
export const pageview = (url: string) => {
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('config', GA_MEASUREMENT_ID, {
      page_path: url,
    })
  }
}

// Функция для отправки событий
export const event = ({
  action,
  category,
  label,
  value,
}: {
  action: string
  category: string
  label?: string
  value?: number
}) => {
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('event', action, {
      event_category: category,
      event_label: label,
      value: value,
    })
  }
}
```

2. **Добавь в `app/layout.tsx`:**
```tsx
import Script from 'next/script'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ru">
      <head>
        <Script
          strategy="afterInteractive"
          src={`https://www.googletagmanager.com/gtag/js?id=${process.env.NEXT_PUBLIC_GA_ID}`}
        />
        <Script
          id="google-analytics"
          strategy="afterInteractive"
          dangerouslySetInnerHTML={{
            __html: `
              window.dataLayer = window.dataLayer || [];
              function gtag(){dataLayer.push(arguments);}
              gtag('js', new Date());
              gtag('config', '${process.env.NEXT_PUBLIC_GA_ID}', {
                page_path: window.location.pathname,
              });
            `,
          }}
        />
      </head>
      <body>
        {children}
      </body>
    </html>
  )
}
```

3. **Добавь в `.env.local`:**
```env
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
```

### 1.4. Настройка отслеживания событий

#### События для отслеживания в CandleTime:

1. **Зажигание свечи** (`light_candle`)
2. **Просмотр свечи** (`view_candle`)
3. **Регистрация** (`sign_up`)
4. **Вход в аккаунт** (`login`)
5. **Просмотр всех свечей** (`view_candles`)
6. **Просмотр dashboard** (`view_dashboard`)
7. **Погашение свечи** (`extinguish_candle`)
8. **Поделиться свечой** (`share_candle`)

#### Пример реализации:

**В `app/light/page.tsx` (после успешного зажигания свечи):**
```tsx
import { event } from '@/lib/gtag'

// После успешного создания свечи
const handleSuccess = async (candleId: string) => {
  // ... существующий код ...
  
  // Отправка события в GA4
  event({
    action: 'light_candle',
    category: 'Candle',
    label: candleType || 'custom',
    value: 1,
  })
}
```

**В `app/candle/[id]/page.tsx` (при просмотре свечи):**
```tsx
import { useEffect } from 'react'
import { event } from '@/lib/gtag'

useEffect(() => {
  if (candle) {
    event({
      action: 'view_candle',
      category: 'Candle',
      label: candle.candle_type || 'unknown',
    })
  }
}, [candle])
```

**В `app/auth/signup/page.tsx` (после регистрации):**
```tsx
import { event } from '@/lib/gtag'

const handleSignup = async () => {
  // ... существующий код регистрации ...
  
  if (success) {
    event({
      action: 'sign_up',
      category: 'User',
    })
  }
}
```

### 1.5. Настройка конверсионных целей

#### В Google Analytics 4:

1. Перейди в **"Администратор"** → **"События"**
2. Нажми **"Создать событие"**
3. Создай следующие события как конверсии:

**Основные конверсии:**
- `light_candle` — зажигание свечи (главная цель)
- `sign_up` — регистрация пользователя
- `view_candle` — просмотр свечи (вторичная цель)

**Как пометить как конверсию:**
1. В списке событий найди нужное событие
2. Включи переключатель **"Пометить как конверсию"**

### 1.6. Настройка отчетов

#### Рекомендуемые отчеты:

1. **Отчет "Жизненный цикл" → "Приобретение"**
   - Показывает источники трафика
   - Какие каналы приводят пользователей

2. **Отчет "Жизненный цикл" → "Вовлеченность"**
   - События и конверсии
   - Активность пользователей

3. **Отчет "Пользователи" → "Демографические данные"**
   - Возраст, пол, интересы (если доступно)

4. **Создай кастомный отчет:**
   - **"События по типам свечей"** — какие типы свечей популярнее
   - **"Путь пользователя"** — как пользователи перемещаются по сайту

---

## 2. 📊 Yandex.Metrica

### 2.1. Создание счетчика

#### Шаг 1: Регистрация
1. Перейди на [metrika.yandex.ru](https://metrika.yandex.ru)
2. Войди в свой Яндекс аккаунт (или создай новый)
3. Нажми **"Добавить счетчик"**

#### Шаг 2: Настройка счетчика
1. **Название счетчика:** `CandleTime`
2. **Адрес сайта:** `https://candletime.ru`
3. **Часовой пояс:** Москва (или твой)
4. **Согласие на обработку данных:** Отметь галочку
5. Нажми **"Создать счетчик"**

#### Шаг 3: Получение кода счетчика
После создания ты получишь **ID счетчика** (число, например: `12345678`)

**Где найти:**
- В настройках счетчика → **"Код счетчика"**
- Или в правом верхнем углу страницы счетчика

### 2.2. Установка на сайт (Next.js)

#### Вариант A: Использование компонента

1. **Создай компонент `components/YandexMetrika.tsx`:**
```tsx
'use client'

import { useEffect } from 'react'
import Script from 'next/script'

const YANDEX_METRIKA_ID = process.env.NEXT_PUBLIC_YANDEX_METRIKA_ID || ''

export function YandexMetrika() {
  useEffect(() => {
    if (typeof window !== 'undefined' && YANDEX_METRIKA_ID) {
      // Инициализация Yandex.Metrica
      ;(window as any).ym = (window as any).ym || function () {
        ((window as any).ym.a = (window as any).ym.a || []).push(arguments)
      }
      ;(window as any).ym.l = 1 * new Date()
      
      const firstScript = document.createElement('script')
      firstScript.async = true
      firstScript.src = 'https://mc.yandex.ru/metrika/tag.js'
      
      const firstElement = document.getElementsByTagName('script')[0]
      firstElement?.parentNode?.insertBefore(firstScript, firstElement)
    }
  }, [])

  if (!YANDEX_METRIKA_ID) return null

  return (
    <>
      <Script id="yandex-metrika" strategy="afterInteractive">
        {`
          (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
          m[i].l=1*new Date();k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
          (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");

          ym(${YANDEX_METRIKA_ID}, "init", {
            clickmap:true,
            trackLinks:true,
            accurateTrackBounce:true,
            webvisor:true
          });
        `}
      </Script>
      <noscript>
        <div>
          <img
            src={`https://mc.yandex.ru/watch/${YANDEX_METRIKA_ID}`}
            style={{ position: 'absolute', left: '-9999px' }}
            alt=""
          />
        </div>
      </noscript>
    </>
  )
}
```

2. **Добавь в `app/layout.tsx`:**
```tsx
import { YandexMetrika } from '@/components/YandexMetrika'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ru">
      <body>
        {children}
        <YandexMetrika />
      </body>
    </html>
  )
}
```

3. **Добавь в `.env.local`:**
```env
NEXT_PUBLIC_YANDEX_METRIKA_ID=12345678
```

### 2.3. Настройка отслеживания событий

#### Создай утилиту для отправки событий:

**В `lib/yandexMetrika.ts`:**
```typescript
declare global {
  interface Window {
    ym?: (counterId: number, method: string, target: string, params?: any) => void
  }
}

const YANDEX_METRIKA_ID = process.env.NEXT_PUBLIC_YANDEX_METRIKA_ID

export const reachGoal = (target: string, params?: any) => {
  if (typeof window !== 'undefined' && window.ym && YANDEX_METRIKA_ID) {
    window.ym(Number(YANDEX_METRIKA_ID), 'reachGoal', target, params)
  }
}

export const hit = (url: string, params?: any) => {
  if (typeof window !== 'undefined' && window.ym && YANDEX_METRIKA_ID) {
    window.ym(Number(YANDEX_METRIKA_ID), 'hit', url, params)
  }
}
```

#### Использование в коде:

**В `app/light/page.tsx`:**
```tsx
import { reachGoal } from '@/lib/yandexMetrika'

const handleSuccess = async (candleId: string) => {
  // ... существующий код ...
  
  // Отправка цели в Yandex.Metrica
  reachGoal('light_candle', {
    candle_type: candleType,
  })
}
```

### 2.4. Настройка карт кликов и записей сессий

#### В настройках Yandex.Metrica:

1. Перейди в **"Настройки"** → **"Общие"**
2. Включи опции:
   - ✅ **"Карта кликов"** — визуализация кликов на странице
   - ✅ **"Запись сессий"** — запись действий пользователей (Webvisor)
   - ✅ **"Прокрутка страниц"** — отслеживание прокрутки
   - ✅ **"Формы"** — отслеживание заполнения форм

3. **Важно:** Запись сессий требует согласия пользователей (GDPR)
   - Добавь уведомление о cookies на сайте
   - Получи согласие перед включением записи

### 2.5. Настройка целей

#### В Yandex.Metrica:

1. Перейди в **"Настройки"** → **"Цели"**
2. Нажми **"Добавить цель"**

**Создай следующие цели:**

1. **"Зажигание свечи"**
   - Тип: JavaScript-событие
   - Условие: `light_candle`

2. **"Регистрация"**
   - Тип: JavaScript-событие
   - Условие: `sign_up`

3. **"Просмотр свечи"**
   - Тип: JavaScript-событие
   - Условие: `view_candle`

4. **"Переход на страницу зажигания"**
   - Тип: Переход на страницу
   - URL содержит: `/light`

---

## 3. 🔍 Google Search Console

### 3.1. Подключение сайта

#### Шаг 1: Регистрация
1. Перейди на [search.google.com/search-console](://search.google.com/search-console)
2. Войди в свой Google аккаунт
3. Нажми **"Добавить ресурс"** (Add property)

#### Шаг 2: Выбор типа ресурса
Выбери **"Префикс URL"** (URL prefix) — проще для настройки

#### Шаг 3: Ввод URL
Введи: `https://candletime.ru`

#### Шаг 4: Подтверждение права собственности

**Вариант A: HTML-тег (рекомендуется для Next.js)**

1. Google предоставит HTML-тег вида:
```html
<meta name="google-site-verification" content="XXXXXXXXXXXXX" />
```

2. **Добавь в `app/layout.tsx`:**
```tsx
export const metadata = {
  // ... существующие метаданные ...
  other: {
    'google-site-verification': 'XXXXXXXXXXXXX',
  },
}
```

Или через компонент:
```tsx
<head>
  <meta name="google-site-verification" content="XXXXXXXXXXXXX" />
</head>https
```

3. Задеплой изменения на сайт
4. Вернись в Search Console и нажми **"Проверить"**

**Вариант B: HTML-файл**

1. Скачай HTML-файл от Google
2. Положи его в `public/googleXXXXXXXXXXXXX.html`
3. Задеплой изменения
4. Нажми **"Проверить"** в Search Console

**Вариант C: DNS-запись**

1. Добавь TXT-запись в DNS настройках домена
2. Подожди распространения DNS (до 48 часов)
3. Нажми **"Проверить"**

### 3.2. Отправка Sitemap

#### Шаг 1: Убедись, что sitemap существует

Проверь, что файл `app/sitemap.ts` создан и генерирует sitemap.

#### Шаг 2: Отправь sitemap в Search Console

1. В Search Console перейди в **"Карты сайта"** (Sitemaps)
2. В поле **"Добавить новую карту сайта"** введи: `sitemap.xml`
3. Нажми **"Отправить"**

**URL sitemap:** `https://candletime.ru/sitemap.xml`

#### Шаг 3: Проверка

- Google начнет индексировать страницы из sitemap
- Процесс может занять несколько дней
- Статус можно отслеживать в разделе "Карты сайта"

### 3.3. Мониторинг позиций

#### Просмотр запросов:

1. Перейди в **"Производительность"** (Performance)
2. Здесь ты увидишь:
   - **Запросы** — какие запросы приводят пользователей
   - **Позиции** — средняя позиция в поиске
   - **CTR** — процент кликов
   - **Показы** — сколько раз показывался сайт

#### Ключевые запросы для отслеживания:

- "символическая свеча онлайн"
- "зажечь свечу онлайн"
- "виртуальная свеча"
- "свеча намерения"
- "candletime"

### 3.4. Мониторинг индексации

#### Проверка индексации страниц:

1. Перейди в **"Индексирование"** → **"Страницы"** (Indexing → Pages)
2. Здесь ты увидишь:
   - Сколько страниц проиндексировано
   - Какие страницы не проиндексированы
   - Причины, если страницы не индексируются

#### Проверка конкретной страницы:

1. Используй инструмент **"Проверка URL"** (URL Inspection)
2. Введи URL страницы (например: `https://candletime.ru/light`)
3. Нажми **"Проверить URL"**
4. Увидишь:
   - Проиндексирована ли страница
   - Когда последний раз Google видел страницу
   - Есть ли проблемы

### 3.5. Исправление проблем

#### Частые проблемы и решения:

1. **"Страница не проиндексирована"**
   - Проверь, что страница доступна (нет ошибок 404)
   - Убедись, что страница не заблокирована в `robots.txt`
   - Проверь мета-теги (нет ли `noindex`)

2. **"Дублированный контент"**
   - Убедись, что канонические URL настроены правильно
   - Проверь, что нет дублей страниц (с www и без www)

3. **"Мобильная версия недоступна"**
   - Проверь адаптивность сайта
   - Используй инструмент Google "Проверка мобильной версии"

---

## 4. 🔧 Дополнительные настройки

### 4.1. Настройка фильтров (Google Analytics)

#### Исключить внутренний трафик:

1. В GA4 перейди в **"Администратор"** → **"Фильтры данных"**
2. Создай фильтр для исключения своего IP:
   - Название: "Исключить мой IP"
   - Тип: IP-адрес
   - Введи свой IP

### 4.2. Настройка уведомлений

#### Google Analytics:
- Настрой уведомления о важных событиях (падение трафика, ошибки)

#### Yandex.Metrica:
- Настрой email-уведомления о достижении целей

#### Google Search Console:
- Настрой уведомления о проблемах с индексацией

### 4.3. Интеграция с другими инструментами

#### Google Data Studio (Looker Studio):
- Создай дашборды для визуализации данных из GA4

#### Yandex.Direct:
- Подключи Yandex.Metrica для отслеживания рекламных кампаний

---

## 5. 📋 Чек-лист настройки

### Google Analytics 4:
- [ ] Создан аккаунт и свойство
- [ ] Получен Measurement ID
- [ ] Код установлен на сайте
- [ ] Настроены события (light_candle, sign_up, view_candle)
- [ ] Настроены конверсии
- [ ] Проверена работа отслеживания

### Yandex.Metrica:
- [ ] Создан счетчик
- [ ] Получен ID счетчика
- [ ] Код установлен на сайте
- [ ] Настроены цели
- [ ] Включены карты кликов и записи сессий
- [ ] Проверена работа отслеживания

### Google Search Console:
- [ ] Добавлен ресурс
- [ ] Подтверждено право собственности
- [ ] Отправлен sitemap
- [ ] Проверена индексация основных страниц
- [ ] Настроены уведомления

---

## 6. 🎯 Рекомендации по использованию

### Еженедельный анализ:
1. **Понедельник:** Просмотр отчетов за прошлую неделю
2. **Среда:** Проверка новых событий и конверсий
3. **Пятница:** Анализ источников трафика

### Ежемесячный анализ:
1. Сравнение метрик с предыдущим месяцем
2. Выявление трендов
3. Корректировка стратегии на основе данных

### Что отслеживать:
- **Трафик:** Количество посетителей, сессий, просмотров страниц
- **Конверсии:** Зажигание свечей, регистрации
- **Источники:** Откуда приходят пользователи
- **Поведение:** Какие страницы популярнее, где пользователи уходят
- **Поиск:** Позиции по ключевым запросам, новые запросы

---

## 7. 🚨 Важные замечания

### GDPR и приватность:
- **Обязательно:** Добавь уведомление о cookies на сайте
- Получи согласие пользователей перед отслеживанием
- Предоставь возможность отказаться от отслеживания

### Производительность:
- Аналитика не должна замедлять сайт
- Используй `strategy="afterInteractive"` для скриптов
- Минимизируй количество отслеживающих скриптов

### Безопасность:
- Не публикуй Measurement ID и ID счетчиков в публичных репозиториях
- Используй переменные окружения (`.env.local`)
- Регулярно проверяй доступы к аккаунтам аналитики

---

*Документ создан: 2025*  
*Версия: 1.0*  
*Проект: CandleTime (candletime.ru)*

