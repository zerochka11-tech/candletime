# 📦 Типы кеша в Vercel и когда их очищать

## 🔍 Типы кеша в Vercel

В Vercel есть несколько типов кеша, каждый из которых решает разные задачи:

### 1. Build Cache (Кеш сборки)
**Что это:** Кеш для ускорения сборки проекта
**Что кешируется:**
- `node_modules` (если не изменился `package.json`)
- Результаты компиляции TypeScript
- Результаты сборки Next.js (`.next`)

**Где находится:** В Vercel Dashboard → **Settings** → **General** → **Build Cache**

**Когда очищать:**
- После изменения зависимостей (`package.json`)
- После изменения конфигурации сборки
- При проблемах со сборкой проекта
- При проблемах с путями к статическим файлам

**Как очистить:**
1. Vercel Dashboard → **Deployments**
2. Нажми на три точки (⋮) у деплоя
3. Выбери **"Redeploy"** (очистит Build Cache автоматически)

Или через CLI:
```bash
vercel --prod --force
```

---

### 2. Data Cache (Кеш данных)
**Что это:** Кеш для данных и компонентов, автоматически управляемый Next.js
**Что кешируется:**
- Результаты `fetch()` запросов (с `cache: 'force-cache'`)
- Результаты `revalidate` в Next.js
- Кешированные компоненты (React Server Components)
- Данные из `unstable_cache`

**Где находится:** В Vercel Dashboard → **Settings** → **Data Cache**

**Когда очищать:**
- После изменения данных в базе данных
- После изменения логики получения данных
- При проблемах с устаревшими данными
- После изменения API endpoints

**Как очистить:**
1. Vercel Dashboard → **Settings** → **Data Cache**
2. Нажми **"Purge Data Cache"**
3. Подожди несколько секунд

**Важно:** Data Cache НЕ влияет на статические файлы (CSS/JS). Для них используй Redeploy.

---

### 3. CDN Cache (Кеш CDN)
**Что это:** Кеш статических файлов на CDN Vercel
**Что кешируется:**
- CSS файлы
- JavaScript файлы
- Изображения
- Другие статические файлы из `/public`

**Где находится:** Управляется автоматически Vercel CDN

**Когда очищать:**
- При проблемах с загрузкой CSS/JS файлов
- После изменения статических файлов
- При проблемах с путями к файлам

**Как очистить:**
1. Vercel Dashboard → **Deployments**
2. Нажми на три точки (⋮) у деплоя
3. Выбери **"Redeploy"**
4. CDN кеш обновится автоматически после нового деплоя

Или через CLI:
```bash
vercel --prod --force
```

---

## 🎯 Какой кеш очищать при проблемах?

### Проблема: Статические файлы (CSS/JS) не загружаются
**Решение:** Очисти **Build Cache** и **CDN Cache**
- Сделай **Redeploy** в Vercel Dashboard
- Это очистит оба кеша автоматически

### Проблема: Устаревшие данные на сайте
**Решение:** Очисти **Data Cache**
- Vercel Dashboard → **Settings** → **Data Cache** → **"Purge Data Cache"**

### Проблема: Ошибки сборки
**Решение:** Очисти **Build Cache**
- Сделай **Redeploy** в Vercel Dashboard

### Проблема: Неизвестно, что не так
**Решение:** Очисти все кеши
1. Очисти **Data Cache**: Settings → Data Cache → Purge
2. Сделай **Redeploy**: Deployments → Redeploy

---

## 📋 Чеклист очистки кеша

### Для статических файлов (CSS/JS):
- [ ] Сделал Redeploy в Vercel Dashboard
- [ ] Дождался завершения деплоя (2-5 минут)
- [ ] Проверил сайт в режиме Incognito
- [ ] Проверил Network tab - файлы загружаются (200)

### Для кешированных данных:
- [ ] Очистил Data Cache: Settings → Data Cache → Purge
- [ ] Подождал несколько секунд
- [ ] Проверил сайт - данные обновились

### Для проблем со сборкой:
- [ ] Сделал Redeploy (очистит Build Cache)
- [ ] Проверил Build Logs на ошибки
- [ ] Если ошибки - исправил код и сделал новый деплой

---

## ⚠️ Важные замечания

1. **Data Cache ≠ Build Cache**
   - Data Cache - для данных и компонентов
   - Build Cache - для сборки проекта
   - Для статических файлов используй Redeploy, а не Data Cache

2. **Redeploy очищает несколько кешей**
   - Build Cache
   - CDN Cache
   - Но НЕ Data Cache (его нужно очищать отдельно)

3. **Data Cache очищается автоматически**
   - При новом деплое (если изменились данные)
   - Но иногда нужно очистить вручную

4. **CDN Cache обновляется автоматически**
   - При новом деплое
   - Но может занять несколько минут для распространения

---

## 🔗 Полезные ссылки

- [Vercel Data Cache Documentation](https://vercel.com/docs/storage/vercel-cache)
- [Next.js Caching](https://nextjs.org/docs/app/building-your-application/caching)
- [Vercel Build Cache](https://vercel.com/docs/build-step#caching)

---

**Последнее обновление:** 2025-01-27

