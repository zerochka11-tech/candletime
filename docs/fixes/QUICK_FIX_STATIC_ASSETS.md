# ⚡ Быстрое исправление проблемы загрузки статических файлов

## 🚨 Проблема
Сайт не загружается, CSS и JS файлы не загружаются (статус `failed`).

## ✅ Быстрое решение (5 минут)

### Шаг 1: Очисти кеш Vercel и пересобери

**Для статических файлов (CSS/JS):**
1. Зайди в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выбери проект **candletime**
3. Перейди в **Deployments**
4. Нажми на три точки (⋮) у последнего деплоя
5. Выбери **"Redeploy"** или **"Redeploy with existing Build Cache"**
6. Подожди 2-5 минут

**Для кешированных данных (если проблема с данными):**
1. Зайди в **Settings** → **Data Cache**
2. Нажми **"Purge Data Cache"**
3. Подожди несколько секунд

**Примечание:** Для проблемы со статическими файлами используй **Redeploy**, а не Data Cache.

### Шаг 2: Проверь переменные окружения
1. В Vercel Dashboard → **Settings** → **Environment Variables**
2. Убедись, что есть:
   - ✅ `NEXT_PUBLIC_SUPABASE_URL`
   - ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - ✅ `NEXT_PUBLIC_SITE_URL` (если используется)
3. Убедись, что они для **Production** окружения
4. Если что-то изменил → сделай **Redeploy**

### Шаг 3: Проверь домен
1. В Vercel Dashboard → **Settings** → **Domains**
2. Убедись, что статус **"Valid Configuration"**
3. Если нет → проверь DNS записи

### Шаг 4: Проверь результат
1. Открой сайт в режиме **Incognito** (Ctrl+Shift+N)
2. Открой DevTools (F12) → **Network** tab
3. Обнови страницу (Ctrl+Shift+R)
4. Проверь, что CSS и JS файлы загружаются (статус `200`)

## 🔧 Если не помогло

### Вариант 1: Принудительная пересборка
```bash
git commit --allow-empty -m "Force rebuild - fix static assets"
git push
```

### Вариант 2: Через Vercel CLI
```bash
vercel --prod --force
```

### Вариант 3: Проверь конфигурацию
Убедись, что в `next.config.ts` нет `basePath` или `assetPrefix`:
```typescript
// ❌ НЕ ДЕЛАЙ ТАК (если не деплоишь в подпапку):
// basePath: '/subfolder',
// assetPrefix: 'https://cdn.example.com',

// ✅ Правильно для обычного деплоя:
const nextConfig: NextConfig = {
  compress: true,
  poweredByHeader: false,
};
```

## 📋 Чеклист

- [ ] Очистил кеш Vercel / сделал Redeploy
- [ ] Проверил переменные окружения
- [ ] Проверил статус домена
- [ ] Проверил сайт в режиме Incognito
- [ ] Проверил Network tab - файлы загружаются (200)
- [ ] Нет ошибок в консоли

## 🆘 Если всё ещё не работает

1. Проверь логи в Vercel: **Deployments** → последний деплой → **Build Logs**
2. Проверь статус Vercel: [vercel-status.com](https://www.vercel-status.com/)
3. Свяжись с поддержкой Vercel

---

## 📚 Дополнительная информация

- **Подробная документация:** [STATIC_ASSETS_LOADING_FIX.md](./STATIC_ASSETS_LOADING_FIX.md)
- **Типы кеша в Vercel:** [VERCEL_CACHE_TYPES.md](./VERCEL_CACHE_TYPES.md)

---

**Время исправления:** ~5 минут

