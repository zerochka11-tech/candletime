# 🔧 Исправление проблемы загрузки статических файлов (CSS/JS) в продакшене

## ❌ Проблема

На production версии сайта candletime.ru:
- CSS файлы не загружаются (статус `failed` в Network tab)
- JavaScript файлы не загружаются (статус `failed` в Network tab)
- Очень долгое время загрузки (DOMContentLoaded: 48.44s, Finish: 5.4 min)
- 5+ ошибок в консоли браузера
- Сайт не работает ни с одного устройства

**Примеры ошибок:**
- `685409aa38e6c3a9.css` - failed
- `8cc002cde2e6d025.js` - failed
- `24389127c2098a17.js` - failed
- `eaad74200afeb6e0.js` - failed
- `b8c1436e8cb9d743.js` - failed

## 🔍 Анализ причин

### 1. Проблемы с CDN Vercel
- Статические файлы могут быть недоступны из-за проблем с CDN
- Возможны проблемы с распространением файлов по CDN узлам
- Проблемы с кешем CDN

### 2. Проблемы с кешем браузера/CDN
- Старые хеши файлов могут быть закешированы
- CDN может отдавать устаревшие файлы
- Браузер может пытаться загрузить несуществующие файлы

### 3. Проблемы с доменом или SSL
- Проблемы с SSL сертификатом
- Проблемы с DNS
- Проблемы с CORS

### 4. Проблемы с конфигурацией Next.js
- Отсутствие правильной конфигурации для продакшена
- Проблемы с assetPrefix или basePath
- Проблемы с output конфигурацией

### 5. Проблемы с билдом
- Файлы не были правильно собраны
- Проблемы с хешированием файлов
- Проблемы с путями к статическим файлам

## ✅ Решения

### Решение 1: Очистка кеша Vercel и пересборка

**Важно:** В Vercel есть несколько типов кеша:
- **Build Cache** - кеш сборки (node_modules, .next и т.д.)
- **Data Cache** - кеш данных и компонентов (Next.js cache, fetch cache)
- **CDN Cache** - кеш статических файлов на CDN

#### Очистка Data Cache (для кешированных данных)

Если проблема с кешированными данными или компонентами:
1. Зайди в Vercel Dashboard
2. Перейди в проект → **Settings** → **Data Cache**
3. Нажми **"Purge Data Cache"**
4. Подожди несколько секунд

**Когда использовать:**
- Проблемы с кешированными данными из API
- Проблемы с кешированными компонентами
- После изменения данных в базе данных

#### Очистка Build Cache и пересборка (для статических файлов)

Если проблема со статическими файлами (CSS/JS):
1. Зайди в Vercel Dashboard
2. Перейди в проект → **Deployments**
3. Нажми на три точки (⋮) у последнего деплоя
4. Выбери **"Redeploy"** или **"Redeploy with existing Build Cache"**
5. Дождись завершения нового деплоя (2-5 минут)

**Альтернатива через CLI:**
```bash
vercel --prod --force
```

**Когда использовать:**
- Проблемы с загрузкой CSS/JS файлов
- Проблемы с путями к статическим файлам
- После изменения конфигурации Next.js

### Решение 2: Проверка и обновление конфигурации Next.js

Добавь в `next.config.ts` правильную конфигурацию для продакшена:

```typescript
import type { NextConfig } from "next";
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

const nextConfig: NextConfig = {
  // Убедись, что нет basePath или assetPrefix (если не нужны)
  // basePath: '', // Не устанавливай, если не используешь подпапку
  
  // Оптимизация для продакшена
  compress: true,
  
  // Правильная обработка статических файлов
  images: {
    formats: ['image/avif', 'image/webp'],
  },
  
  // Настройки для продакшена
  poweredByHeader: false,
  
  // Если используешь кастомный домен, убедись что нет assetPrefix
  // assetPrefix: '', // Не устанавливай для обычного деплоя
};

export default withNextIntl(nextConfig);
```

### Решение 3: Проверка переменных окружения в Vercel

Убедись, что все переменные окружения установлены правильно:

1. Зайди в Vercel Dashboard
2. Перейди в проект → **Settings** → **Environment Variables**
3. Проверь наличие:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `NEXT_PUBLIC_SITE_URL` (если используется)
   - `NEXT_PUBLIC_ADMIN_EMAILS` (если используется)
4. Убедись, что переменные добавлены для **Production** окружения
5. После изменения переменных - сделай **Redeploy**

### Решение 4: Проверка домена и DNS

1. **Проверь DNS записи:**
   - Зайди в панель управления доменом
   - Проверь, что DNS записи правильные
   - Используй [whatsmydns.net](https://www.whatsmydns.net) для проверки

2. **Проверь SSL сертификат:**
   - В Vercel Dashboard → **Settings** → **Domains**
   - Убедись, что статус домена **"Valid Configuration"**
   - Проверь, что SSL сертификат активен

3. **Проверь редиректы:**
   - Убедись, что нет конфликтующих редиректов
   - Проверь, что www и без www настроены правильно

### Решение 5: Очистка кеша браузера

Для тестирования:
1. Открой сайт в режиме **Incognito/Private**
2. Или используй **Hard Refresh**: `Ctrl+Shift+R` (Windows) / `Cmd+Shift+R` (Mac)
3. Или очисти кеш браузера полностью

### Решение 6: Проверка логов Vercel

1. Зайди в Vercel Dashboard
2. Перейди в проект → **Deployments**
3. Открой последний деплой
4. Проверь **Build Logs** на ошибки
5. Проверь **Function Logs** на ошибки во время выполнения

### Решение 7: Принудительная пересборка

Если ничего не помогает:

1. **Удали старый деплой:**
   - В Vercel Dashboard → **Deployments**
   - Найди проблемный деплой
   - Удали его (если возможно)

2. **Сделай новый коммит:**
   ```bash
   git commit --allow-empty -m "Force rebuild"
   git push
   ```

3. **Или через Vercel CLI:**
   ```bash
   vercel --prod --force
   ```

## 🎯 Пошаговая инструкция для быстрого исправления

### Шаг 1: Проверь конфигурацию Next.js
- [ ] Открой `next.config.ts`
- [ ] Убедись, что нет `basePath` или `assetPrefix` (если не нужны)
- [ ] Сохрани файл

### Шаг 2: Обнови конфигурацию (если нужно)
- [ ] Добавь правильную конфигурацию из Решения 2
- [ ] Сохрани файл
- [ ] Сделай коммит и пуш

### Шаг 3: Очисти кеш Vercel
- [ ] Зайди в Vercel Dashboard
- [ ] Перейди в проект → **Settings** → **General**
- [ ] Найди опцию очистки кеша или сделай Redeploy

### Шаг 4: Проверь переменные окружения
- [ ] Зайди в Vercel Dashboard → **Settings** → **Environment Variables**
- [ ] Проверь все переменные
- [ ] Убедись, что они для Production

### Шаг 5: Проверь домен
- [ ] Зайди в Vercel Dashboard → **Settings** → **Domains**
- [ ] Убедись, что статус **"Valid Configuration"**
- [ ] Проверь SSL сертификат

### Шаг 6: Дождись нового деплоя
- [ ] Подожди завершения деплоя (2-5 минут)
- [ ] Проверь сайт в режиме Incognito
- [ ] Проверь Network tab в DevTools

### Шаг 7: Если не помогло - принудительная пересборка
- [ ] Сделай пустой коммит: `git commit --allow-empty -m "Force rebuild"`
- [ ] Запуш: `git push`
- [ ] Дождись нового деплоя

## 🔍 Диагностика

### Проверка в браузере:
1. Открой DevTools (F12)
2. Перейди в **Network** tab
3. Обнови страницу (Ctrl+Shift+R)
4. Проверь:
   - Какие файлы не загружаются (статус `failed`)
   - Какие ошибки в консоли (Console tab)
   - Время загрузки файлов

### Проверка в Vercel:
1. Зайди в Vercel Dashboard
2. Перейди в проект → **Deployments**
3. Открой последний деплой
4. Проверь:
   - Build Logs на ошибки
   - Function Logs на ошибки
   - Время сборки

### Проверка DNS:
1. Используй [whatsmydns.net](https://www.whatsmydns.net)
2. Введи домен `candletime.ru`
3. Проверь, что DNS записи правильные

## ⚠️ Важные замечания

1. **Не устанавливай `basePath` или `assetPrefix`** без необходимости
   - Это может сломать пути к статическим файлам
   - Используй только если деплоишь в подпапку

2. **Всегда проверяй переменные окружения**
   - Они должны быть для Production окружения
   - После изменения - делай Redeploy

3. **Кеш может быть проблемой**
   - Всегда тестируй в режиме Incognito
   - Очищай кеш Vercel при проблемах

4. **Проверяй логи**
   - Build Logs покажут проблемы со сборкой
   - Function Logs покажут проблемы во время выполнения

## 🚀 После исправления

После применения решений:
1. ✅ Статические файлы должны загружаться
2. ✅ Время загрузки должно быть нормальным (< 3 секунд)
3. ✅ Не должно быть ошибок в консоли
4. ✅ Сайт должен работать на всех устройствах

## 📊 Мониторинг

Рекомендуется:
1. Мониторить логи в Vercel
2. Проверять Network tab в DevTools
3. Следить за временем загрузки
4. Проверять статус домена в Vercel

## 🆘 Если ничего не помогает

1. **Свяжись с поддержкой Vercel:**
   - [Vercel Support](https://vercel.com/support)
   - Опиши проблему с деталями

2. **Проверь статус Vercel:**
   - [Vercel Status](https://www.vercel-status.com/)
   - Возможны проблемы с инфраструктурой

3. **Проверь документацию:**
   - [Next.js Deployment](https://nextjs.org/docs/deployment)
   - [Vercel Documentation](https://vercel.com/docs)

---

**Дата создания:** 2025-01-27  
**Статус:** 🔧 Требует исправления

