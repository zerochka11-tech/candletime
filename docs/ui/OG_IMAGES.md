# 🖼️ Руководство по OG изображениям

## ✅ Что реализовано

### 1. Динамическая генерация OG изображений

Проект использует **@vercel/og** для автоматической генерации OG изображений:

- **Базовое OG изображение**: `/og` - для всех страниц по умолчанию
- **OG изображение для свечей**: `/og/candle?id={candleId}` - динамически генерируется для каждой свечи

### 2. API Routes

#### `/app/og/route.tsx`
Генерирует базовое OG изображение с параметрами:
- `title` - заголовок (по умолчанию "CandleTime")
- `description` - описание (по умолчанию "Тихое место для символических свечей")

**Пример использования:**
```
https://candletime.ru/og?title=CandleTime&description=Тихое место для символических свечей
```

#### `/app/og/candle/route.tsx`
Генерирует OG изображение для конкретной свечи:
- Получает данные свечи из БД
- Отображает название, сообщение и тип свечи
- Показывает соответствующий эмодзи для типа свечи

**Пример использования:**
```
https://candletime.ru/og/candle?id={candle-id}
```

### 3. Автоматическая интеграция

- Все страницы автоматически используют динамические OG изображения
- Страницы свечей (`/candle/[id]`) используют специальные OG изображения с данными свечи
- Мета-теги обновляются автоматически через `DynamicMeta` компонент

---

## 📝 Создание статического OG изображения (опционально)

Если нужно создать статическое PNG изображение для использования в других местах:

### Вариант 1: Конвертация SVG в PNG

1. Открой файл `public/og-image.svg` в браузере или редакторе
2. Используй онлайн-конвертер:
   - [CloudConvert](https://cloudconvert.com/svg-to-png)
   - [Convertio](https://convertio.co/svg-png/)
3. Установи размер: **1200x630px**
4. Сохрани как `public/og-image.png`

### Вариант 2: Использование инструментов командной строки

#### С ImageMagick:
```bash
magick convert -background none -resize 1200x630 public/og-image.svg public/og-image.png
```

#### С Inkscape:
```bash
inkscape public/og-image.svg --export-filename=public/og-image.png --export-width=1200 --export-height=630
```

### Вариант 3: Использование Node.js скрипта

Создай файл `scripts/generate-og-image.js`:

```javascript
const { createCanvas, loadImage } = require('canvas');
const fs = require('fs');
const path = require('path');

async function generateOGImage() {
  const width = 1200;
  const height = 630;
  const canvas = createCanvas(width, height);
  const ctx = canvas.getContext('2d');

  // Загрузка SVG (требует дополнительной библиотеки для SVG)
  // Или создание изображения программно
  
  // Сохранение
  const buffer = canvas.toBuffer('image/png');
  fs.writeFileSync(path.join(__dirname, '../public/og-image.png'), buffer);
}

generateOGImage();
```

---

## 🎨 Кастомизация OG изображений

### Изменение дизайна базового изображения

Отредактируй `app/og/route.tsx`:
- Измени цвета градиентов
- Измени размеры и позиции элементов
- Добавь дополнительные декоративные элементы

### Изменение дизайна изображений для свечей

Отредактируй `app/og/candle/route.tsx`:
- Измени отображение данных свечи
- Добавь дополнительные элементы (дата, тип свечи)
- Измени стиль и цвета

---

## 🧪 Тестирование

### Проверка OG изображений

1. **Локально:**
   ```
   http://localhost:3000/og
   http://localhost:3000/og?title=Тест&description=Описание
   http://localhost:3000/og/candle?id={candle-id}
   ```

2. **В продакшене:**
   ```
   https://candletime.ru/og
   https://candletime.ru/og/candle?id={candle-id}
   ```

3. **Проверка мета-тегов:**
   - Используй [Open Graph Debugger](https://www.opengraph.xyz/)
   - Или [Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/)
   - Или [Twitter Card Validator](https://cards-dev.twitter.com/validator)

---

## 📊 Технические детали

### Размеры изображений
- **Ширина:** 1200px
- **Высота:** 630px
- **Соотношение:** 1.91:1 (стандарт для OG изображений)

### Формат
- Генерируется в формате PNG через `@vercel/og`
- Использует Edge Runtime для быстрой генерации

### Производительность
- Изображения генерируются на лету
- Кэшируются Vercel автоматически
- Edge Runtime обеспечивает низкую задержку

---

## 🔧 Устранение неполадок

### Изображение не генерируется

1. Проверь, что `@vercel/og` установлен:
   ```bash
   npm list @vercel/og
   ```

2. Проверь логи в Vercel Dashboard

3. Убедись, что Edge Runtime включен:
   ```typescript
   export const runtime = 'edge';
   ```

### Изображение не отображается в соцсетях

1. Проверь мета-теги через инструменты отладки
2. Убедись, что URL изображения доступен публично
3. Очисти кэш соцсетей (Facebook, Twitter имеют кэш)

### Ошибки при генерации для свечей

1. Проверь, что переменные окружения Supabase установлены
2. Убедись, что свеча существует в БД
3. Проверь логи на наличие ошибок

---

## 📚 Полезные ссылки

- [Vercel OG Image Generation](https://vercel.com/docs/concepts/functions/edge-functions/og-image-generation)
- [Open Graph Protocol](https://ogp.me/)
- [Twitter Cards](https://developer.twitter.com/en/docs/twitter-for-websites/cards/overview/abouts-cards)
- [Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/)

---

**Последнее обновление:** 2024

