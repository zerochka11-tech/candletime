# 🎨 Документация: Декоративные элементы HERO блока

**Дата создания:** 2025-01-15  
**Версия:** 1.0  
**Проект:** CandleTime - Декоративные элементы главной страницы

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Архитектура](#архитектура)
3. [Типы декоративных элементов](#типы-декоративных-элементов)
4. [Анимации](#анимации)
5. [Позиционирование](#позиционирование)
6. [Z-index и слои](#z-index-и-слои)
7. [Производительность](#производительность)
8. [Кастомизация](#кастомизация)

---

## 🎯 Обзор

Декоративные элементы HERO блока создают атмосферную визуальную среду, вдохновленную астрологической и мистической тематикой. Они служат фоновым слоем, который не перекрывает основной контент (текст, кнопки, свечу).

### Основные принципы

- **Фоновый слой**: Все декоративные элементы находятся на `z-index: 0`
- **Невзаимодействующие**: `pointer-events-none` предотвращает блокировку кликов
- **Равномерное распределение**: Элементы распределены по всей высоте HERO блока (0-100%)
- **Плавные анимации**: Используются CSS-анимации для создания живого эффекта
- **Адаптивность**: Элементы работают на всех размерах экрана

---

## 🏗️ Архитектура

### Структура компонента

```tsx
<section className="relative overflow-hidden ...">
  {/* Декоративный градиент */}
  <div className="absolute inset-0 bg-gradient-to-r ... z-0" />
  
  {/* Контейнер для всех декоративных элементов */}
  <div className="absolute inset-0 pointer-events-none z-0">
    {/* Звезды */}
    {/* Облака */}
    {/* Астрологические символы */}
    {/* Золотые элементы */}
  </div>
  
  {/* Основной контент */}
  <div className="relative z-10 ...">
    {/* Текст, кнопки, свеча */}
  </div>
</section>
```

### Ключевые классы

- `absolute inset-0`: Покрывает весь родительский контейнер
- `pointer-events-none`: Отключает взаимодействие с элементами
- `z-0`: Фоновый слой
- `z-10`: Слой с основным контентом

---

## ✨ Типы декоративных элементов

### 1. Звезды (Stars)

**Назначение**: Создают эффект ночного неба с мерцающими звездами.

**Характеристики**:
- **Размеры**: `w-0.5 h-0.5` (маленькие), `w-1 h-1` (средние)
- **Цвет**: `bg-white` с различной прозрачностью (opacity: 20-80%)
- **Анимация**: `animate-twinkle` (мерцание)
- **Распределение**: По всей высоте HERO блока (0-100%)
- **Количество**: ~100+ элементов

**Пример**:
```tsx
<div 
  className="absolute top-[5%] left-[5%] w-0.5 h-0.5 bg-white rounded-full opacity-70 animate-twinkle" 
  style={{ animationDelay: '0s', animationDuration: '3s' }} 
/>
```

**Зоны распределения**:
- Верхняя часть (0-20%): 15 звезд
- Верхняя-средняя часть (20-40%): 20 звезд
- Средняя часть (40-60%): 24 звезды
- Нижняя-средняя часть (60-80%): 20 звезд
- Нижняя часть (80-100%): 15 звезд

### 2. Облака (Clouds)

**Назначение**: Создают мягкий, воздушный фон с плавным движением.

**Характеристики**:
- **Размеры**: `w-24` до `w-40`, `h-12` до `h-20`
- **Цвет**: `bg-sky-200/15` (светлая тема), `bg-sky-300/8` (темная тема)
- **Эффект**: `blur-xl` или `blur-2xl` для мягкости
- **Анимация**: `animate-float` (быстрое) или `animate-float-slow` (медленное)
- **Распределение**: По всей высоте (0-100%)
- **Количество**: ~20 элементов

**Пример**:
```tsx
<div 
  className="absolute top-[2%] left-0 w-40 h-20 bg-sky-200/15 dark:bg-sky-300/8 rounded-full blur-2xl animate-float-slow" 
  style={{ animationDelay: '0s', animationDuration: '20s' }} 
/>
```

**Зоны распределения**:
- Верхние облака (0-25%): 8 элементов
- Средние облака (25-75%): 7 элементов
- Нижние облака (75-100%): 6 элементов

### 3. Астрологические символы (Astrological Symbols)

**Назначение**: Добавляют мистическую атмосферу, едва заметные в облаках.

**Характеристики**:
- **Типы**: Знаки зодиака (♈, ♉, ♊, ...), планеты (☉, ☽, ☿, ...)
- **Размер**: `text-xs`
- **Цвет**: `text-sky-200/40` с низкой прозрачностью
- **Непрозрачность контейнера**: `opacity-[0.08]`
- **Шрифт**: `font-family: serif`
- **Распределение**: По всей высоте (0-100%)
- **Количество**: ~17 символов

**Пример**:
```tsx
<div className="absolute inset-0 pointer-events-none opacity-[0.08] z-0">
  <div className="absolute top-[8%] left-[12%] text-sky-200/40 text-xs font-light" style={{ fontFamily: 'serif' }}>
    ♈
  </div>
</div>
```

**Зоны распределения**:
- Верхние символы (0-25%): 9 символов
- Средние символы (25-75%): 4 символа
- Нижние символы (75-100%): 4 символа

### 4. Золотые элементы (Golden Elements)

**Назначение**: Добавляют теплые акценты, напоминающие лепестки или листья.

**Характеристики**:
- **Размеры**: `w-1.5 h-1.5` или `w-2 h-2`
- **Цвет**: `bg-amber-300/25` или `bg-amber-400/20`
- **Эффект**: `blur-sm` для мягкости
- **Анимация**: `animate-float` или `animate-float-slow`
- **Распределение**: По всей высоте (0-100%)
- **Количество**: ~25 элементов

**Пример**:
```tsx
<div 
  className="absolute top-[12%] left-[8%] w-1.5 h-1.5 bg-amber-300/25 rounded-full blur-sm animate-float" 
  style={{ animationDelay: '5s', animationDuration: '12s' }} 
/>
```

**Зоны распределения**:
- Верхние элементы (0-25%): 9 элементов
- Средние элементы (25-75%): 7 элементов
- Нижние элементы (75-100%): 9 элементов

---

## 🎬 Анимации

### 1. Мерцание (Twinkle)

**Использование**: Звезды

**CSS определение** (`app/globals.css`):
```css
@keyframes twinkle {
  0%, 100% {
    opacity: 0.3;
    transform: scale(1);
  }
  50% {
    opacity: 1;
    transform: scale(1.2);
  }
}
```

**Tailwind класс**: `animate-twinkle`

**Параметры**:
- **Длительность**: 3-6 секунд (задается через `animationDuration`)
- **Задержка**: 0-12 секунд (задается через `animationDelay`)
- **Эффект**: Плавное изменение прозрачности и масштаба

### 2. Плавное движение (Float)

**Использование**: Облака, золотые элементы

**CSS определение**:
```css
@keyframes float {
  0%, 100% {
    transform: translateY(0) translateX(0);
    opacity: 0.6;
  }
  33% {
    transform: translateY(-10px) translateX(5px);
    opacity: 0.8;
  }
  66% {
    transform: translateY(-5px) translateX(-5px);
    opacity: 0.7;
  }
}
```

**Tailwind класс**: `animate-float`

**Параметры**:
- **Длительность**: 12-17 секунд
- **Эффект**: Плавное движение вверх-вниз и влево-вправо

### 3. Медленное плавное движение (Float Slow)

**Использование**: Облака, золотые элементы

**CSS определение**:
```css
@keyframes float-slow {
  0%, 100% {
    transform: translateY(0) translateX(0);
    opacity: 0.5;
  }
  33% {
    transform: translateY(-15px) translateX(8px);
    opacity: 0.7;
  }
  66% {
    transform: translateY(-8px) translateX(-8px);
    opacity: 0.6;
  }
}
```

**Tailwind класс**: `animate-float-slow`

**Параметры**:
- **Длительность**: 13-22 секунды
- **Эффект**: Более медленное и плавное движение

---

## 📍 Позиционирование

### Стратегия распределения

Элементы распределены по всей высоте HERO блока (0-100%) для создания равномерного визуального эффекта.

### Используемые единицы

- **Проценты**: `top-[5%]`, `left-[12%]`, `right-[10%]`
- **Абсолютные значения**: Используются редко

### Зоны распределения

1. **Верхняя часть (0-20%)**: Более яркие элементы, привлекающие внимание
2. **Верхняя-средняя часть (20-40%)**: Переходная зона
3. **Средняя часть (40-60%)**: Центральная зона, менее насыщенная
4. **Нижняя-средняя часть (60-80%)**: Переходная зона
5. **Нижняя часть (80-100%)**: Завершающие элементы

### Примеры позиций

```tsx
// Верхняя часть
top-[5%] left-[5%]
top-[8%] left-[12%]

// Средняя часть
top-[50%] left-[50%]

// Нижняя часть
top-[95%] left-[35%]
```

---

## 🎚️ Z-index и слои

### Иерархия слоев

```
z-0: Декоративные элементы (фон)
  ├─ Декоративный градиент
  ├─ Звезды
  ├─ Облака
  ├─ Астрологические символы
  └─ Золотые элементы

z-10: Основной контент (передний план)
  ├─ Текст
  ├─ Кнопки
  └─ Свеча
```

### Важные правила

1. **Все декоративные элементы**: `z-0`
2. **Основной контент**: `z-10`
3. **Нет перекрытия**: Декоративные элементы не должны перекрывать контент

---

## ⚡ Производительность

### Оптимизации

1. **CSS-анимации**: Используются вместо JavaScript для лучшей производительности
2. **`will-change`**: Не используется, так как может вызвать проблемы с производительностью
3. **`pointer-events-none`**: Предотвращает обработку событий, улучшая производительность
4. **`transform` и `opacity`**: Используются для анимаций (GPU-ускорение)

### Рекомендации

- **Не добавлять слишком много элементов**: Текущее количество (~160 элементов) оптимально
- **Использовать `blur` умеренно**: Слишком много размытия может замедлить рендеринг
- **Мониторить производительность**: Проверять FPS в DevTools

---

## 🎨 Кастомизация

### Изменение количества элементов

Отредактируйте `app/page.tsx` и добавьте/удалите элементы в соответствующих секциях.

### Изменение цветов

**Звезды**:
```tsx
bg-white → bg-blue-200 (для голубых звезд)
```

**Облака**:
```tsx
bg-sky-200/15 → bg-purple-200/15 (для фиолетовых облаков)
```

**Золотые элементы**:
```tsx
bg-amber-300/25 → bg-yellow-300/25 (для более ярких элементов)
```

### Изменение анимаций

Отредактируйте `app/globals.css` и `tailwind.config.js` для изменения ключевых кадров анимаций.

### Добавление новых типов элементов

1. Создайте новый контейнер `<div className="absolute inset-0 pointer-events-none z-0">`
2. Добавьте элементы с абсолютным позиционированием
3. Примените нужные анимации и стили
4. Распределите элементы по всей высоте HERO блока

---

## 📝 Примеры использования

### Добавление новой звезды

```tsx
<div 
  className="absolute top-[45%] left-[25%] w-0.5 h-0.5 bg-white rounded-full opacity-60 animate-twinkle" 
  style={{ animationDelay: '2.5s', animationDuration: '4s' }} 
/>
```

### Добавление нового облака

```tsx
<div 
  className="absolute top-[30%] right-[10%] w-36 h-18 bg-sky-200/15 dark:bg-sky-300/8 rounded-full blur-2xl animate-float-slow" 
  style={{ animationDelay: '3s', animationDuration: '18s' }} 
/>
```

### Добавление нового астрологического символа

```tsx
<div className="absolute top-[40%] left-[60%] text-sky-200/35 text-xs font-light" style={{ fontFamily: 'serif' }}>
  ♄
</div>
```

---

## 🧪 Тестирование

См. `__tests__/components/HeroDecorativeElements.test.tsx` для юнит-тестов.

---

## 📚 Связанные файлы

- `app/page.tsx` - Основной компонент с декоративными элементами
- `app/globals.css` - CSS анимации
- `tailwind.config.js` - Конфигурация Tailwind с анимациями
- `__tests__/components/HeroDecorativeElements.test.tsx` - Юнит-тесты

---

## 🔄 История изменений

- **2025-01-15**: Создана документация, добавлены декоративные элементы
- **2025-01-15**: Уменьшено количество больших звезд для более сбалансированного вида

