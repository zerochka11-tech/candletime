# 🗺️ Руководство по поставщикам карт

## Варианты поставщиков карт

### 1. Mapbox (Рекомендуется) ⭐⭐⭐

**Преимущества:**
- ✅ Красивый современный дизайн
- ✅ Бесплатный тариф: 50,000 запросов/месяц
- ✅ Поддержка темной темы
- ✅ Высокое качество тайлов
- ✅ Хорошая производительность

**Недостатки:**
- ⚠️ Требует API key (бесплатный)
- ⚠️ Нужна регистрация на mapbox.com

**Установка:**
```bash
# Не требует дополнительных пакетов, работает через Leaflet
```

**Настройка:**
1. Зарегистрируйтесь на https://www.mapbox.com
2. Получите Access Token
3. Добавьте в `.env.local`:
   ```
   NEXT_PUBLIC_MAPBOX_TOKEN=your_token_here
   ```

---

### 2. CartoDB (Positron/Dark Matter) ⭐⭐

**Преимущества:**
- ✅ Бесплатно, без регистрации
- ✅ Красивые стили (светлый и темный)
- ✅ Простая интеграция

**Недостатки:**
- ⚠️ Меньше функций, чем у Mapbox
- ⚠️ Ограниченная кастомизация

**Установка:**
```bash
# Не требует дополнительных пакетов
```

---

### 3. Stamen Maps ⭐

**Преимущества:**
- ✅ Бесплатно, без регистрации
- ✅ Художественные стили (Terrain, Watercolor)
- ✅ Уникальный внешний вид

**Недостатки:**
- ⚠️ Ограниченная функциональность
- ⚠️ Не подходит для всех случаев

---

### 4. Google Maps

**Преимущества:**
- ✅ Высокое качество
- ✅ Много функций
- ✅ Хорошая документация

**Недостатки:**
- ❌ Платный (после бесплатного лимита)
- ❌ Требует API key и настройки биллинга
- ❌ Сложнее интеграция

---

## Как переключиться на Mapbox

### Шаг 1: Получить API Token

1. Зайдите на https://www.mapbox.com
2. Создайте аккаунт (бесплатно)
3. Перейдите в Account → Access Tokens
4. Скопируйте Default Public Token

### Шаг 2: Добавить в переменные окружения

Добавьте в `.env.local`:
```
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_token_here
```

### Шаг 3: Обновить компонент карты

Замените TileLayer в `components/map/WorldMap.tsx`:

```typescript
<TileLayer
  attribution='&copy; <a href="https://www.mapbox.com/about/maps/">Mapbox</a>'
  url={`https://api.mapbox.com/styles/v1/mapbox/light-v11/tiles/256/{z}/{x}/{y}@2x?access_token=${process.env.NEXT_PUBLIC_MAPBOX_TOKEN}`}
/>
```

**Стили Mapbox:**
- `light-v11` - светлая тема
- `dark-v11` - темная тема
- `streets-v12` - стандартная
- `outdoors-v12` - для природы
- `satellite-v9` - спутник

---

## Как переключиться на CartoDB

Просто замените TileLayer:

```typescript
// Светлая тема (Positron)
<TileLayer
  attribution='&copy; <a href="https://carto.com/">CARTO</a>'
  url="https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png"
/>

// Темная тема (Dark Matter)
<TileLayer
  attribution='&copy; <a href="https://carto.com/">CARTO</a>'
  url="https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png"
/>
```

---

## Автоматическое переключение темы

Можно сделать автоматическое переключение между светлой и темной темой карты в зависимости от темы сайта:

```typescript
const [mapStyle, setMapStyle] = useState('light-v11');

useEffect(() => {
  // Определяем тему из localStorage или системных настроек
  const isDark = document.documentElement.classList.contains('dark');
  setMapStyle(isDark ? 'dark-v11' : 'light-v11');
}, []);
```

