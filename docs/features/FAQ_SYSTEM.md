# 📚 Руководство по системе FAQ/Статей

## ✅ Что реализовано

### 1. База данных
- ✅ Таблица `articles` для хранения статей
- ✅ Таблица `article_categories` для категорий
- ✅ Таблица `article_tags` для тегов
- ✅ Связующая таблица `article_tag_relations` (many-to-many)
- ✅ Row Level Security (RLS) политики
- ✅ Индексы для оптимизации запросов
- ✅ Функции для автоматического обновления `updated_at` и счетчика просмотров

### 2. Страницы
- ✅ `/faq` - список всех опубликованных статей с фильтрацией по категориям
- ✅ `/faq/[slug]` - страница отдельной статьи
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Темная тема

### 3. SEO оптимизация
- ✅ Мета-теги для каждой статьи
- ✅ Структурированные данные Schema.org Article
- ✅ BreadcrumbList для навигации
- ✅ Динамический sitemap (статьи автоматически добавляются)
- ✅ Canonical URLs

### 4. Функциональность
- ✅ Фильтрация по категориям
- ✅ Счетчик просмотров
- ✅ Время чтения
- ✅ Дата публикации и обновления
- ✅ SEO поля (title, description, keywords)
- ✅ Изображения для превью

### 5. Навигация
- ✅ Ссылка "FAQ" в десктопном меню
- ✅ Ссылка "FAQ" в мобильном меню

---

## 🚀 Настройка

### 1. Создание таблиц в Supabase

Выполни SQL скрипт в Supabase SQL Editor:

```bash
# Файл: supabase-faq-articles-setup.sql
```

Или скопируй содержимое файла и выполни в Supabase SQL Editor.

### 2. Начальные категории

После выполнения скрипта будут созданы категории:
- **FAQ** (slug: `faq`) - Часто задаваемые вопросы
- **SEO** (slug: `seo`) - Статьи о поисковой оптимизации
- **Руководства** (slug: `guides`) - Пошаговые руководства
- **Новости** (slug: `news`) - Новости и обновления проекта

---

## 📝 Создание статей

### Через Supabase Dashboard

1. Перейди в **Table Editor** → **articles**
2. Нажми **Insert** → **Insert row**
3. Заполни поля:
   - `title` - Заголовок статьи
   - `slug` - URL-friendly версия заголовка (например: `kak-zazhech-svechu`)
   - `excerpt` - Краткое описание (для превью)
   - `content` - Полный контент статьи (HTML)
   - `category_id` - ID категории (из таблицы `article_categories`)
   - `author_id` - ID автора (из `auth.users`, опционально)
   - `published` - `true` для публикации
   - `published_at` - Дата публикации (ISO формат)
   - `seo_title` - SEO заголовок (опционально)
   - `seo_description` - SEO описание (опционально)
   - `seo_keywords` - Массив ключевых слов (опционально)
   - `reading_time` - Время чтения в минутах (опционально)
   - `featured_image_url` - URL изображения для превью (опционально)

### Пример SQL запроса

```sql
INSERT INTO articles (
  title,
  slug,
  excerpt,
  content,
  category_id,
  published,
  published_at,
  seo_title,
  seo_description,
  seo_keywords,
  reading_time
) VALUES (
  'Как зажечь символическую свечу',
  'kak-zazhech-svechu',
  'Пошаговая инструкция по созданию символической свечи на CandleTime',
  '<h2>Введение</h2><p>Символические свечи - это способ выразить свои чувства...</p>',
  (SELECT id FROM article_categories WHERE slug = 'guides'),
  true,
  NOW(),
  'Как зажечь символическую свечу - Пошаговая инструкция',
  'Узнайте, как создать символическую свечу на CandleTime. Простая инструкция для начинающих.',
  ARRAY['свечи', 'инструкция', 'как создать'],
  5
);
```

---

## 🎨 Форматирование контента

Контент статей поддерживает HTML. Рекомендуется использовать:

```html
<h2>Заголовок раздела</h2>
<p>Обычный текст параграфа.</p>
<ul>
  <li>Пункт списка 1</li>
  <li>Пункт списка 2</li>
</ul>
<strong>Жирный текст</strong>
<em>Курсив</em>
<a href="/link">Ссылка</a>
```

Для лучшего отображения используется библиотека `@tailwindcss/typography` (prose классы).

---

## 🔍 SEO рекомендации

### Для каждой статьи:

1. **Slug** - должен быть уникальным и SEO-friendly:
   - ✅ `kak-zazhech-svechu`
   - ❌ `как-зажечь-свечу` (кириллица)
   - ❌ `article-1`

2. **Title** - заголовок статьи (50-60 символов)

3. **SEO Title** - если отличается от title, должен быть оптимизирован

4. **SEO Description** - краткое описание (150-160 символов) с ключевыми словами

5. **SEO Keywords** - массив релевантных ключевых слов:
   ```sql
   seo_keywords = ARRAY['свечи', 'символические свечи', 'онлайн свечи']
   ```

6. **Excerpt** - краткое описание для превью (100-150 символов)

7. **Reading Time** - время чтения в минутах (автоматически или вручную)

---

## 📊 Структура базы данных

### Таблица `articles`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `title` | TEXT | Заголовок статьи |
| `slug` | TEXT | URL-friendly идентификатор (уникальный) |
| `excerpt` | TEXT | Краткое описание |
| `content` | TEXT | Полный контент (HTML) |
| `category_id` | UUID | ID категории |
| `author_id` | UUID | ID автора |
| `published` | BOOLEAN | Опубликована ли статья |
| `published_at` | TIMESTAMPTZ | Дата публикации |
| `created_at` | TIMESTAMPTZ | Дата создания |
| `updated_at` | TIMESTAMPTZ | Дата обновления |
| `views_count` | INTEGER | Счетчик просмотров |
| `seo_title` | TEXT | SEO заголовок |
| `seo_description` | TEXT | SEO описание |
| `seo_keywords` | TEXT[] | Массив ключевых слов |
| `featured_image_url` | TEXT | URL изображения |
| `reading_time` | INTEGER | Время чтения в минутах |

### Таблица `article_categories`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `name` | TEXT | Название категории |
| `slug` | TEXT | URL-friendly идентификатор |
| `description` | TEXT | Описание категории |

### Таблица `article_tags`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `name` | TEXT | Название тега |
| `slug` | TEXT | URL-friendly идентификатор |

---

## 🔐 Безопасность

### Row Level Security (RLS)

- ✅ Все могут читать опубликованные статьи
- ✅ Только авторизованные пользователи могут создавать статьи
- ✅ Только автор может обновлять/удалять свою статью
- ✅ Все могут читать категории и теги

### Рекомендации

Для публикации статей рекомендуется:
1. Создать отдельную роль "Editor" или "Admin"
2. Настроить политики RLS для редакторов
3. Или использовать Supabase Admin API для публикации

---

## 🚀 Следующие шаги (опционально)

### 1. Админ-панель для публикации статей

Создать страницу `/admin/articles` для:
- Создания/редактирования статей
- Управления категориями и тегами
- Предпросмотра статей
- Управления публикацией

### 2. Поиск по статьям

Добавить поиск на странице `/faq`:
- По заголовку
- По содержанию
- По тегам

### 3. Связанные статьи

Показывать связанные статьи на странице статьи:
- Из той же категории
- С похожими тегами

### 4. Комментарии

Добавить систему комментариев к статьям (опционально)

### 5. Автоматический расчет времени чтения

Функция для автоматического расчета `reading_time` на основе длины контента

---

## 📚 Полезные ссылки

- [Next.js App Router](https://nextjs.org/docs/app)
- [Supabase Documentation](https://supabase.com/docs)
- [Schema.org Article](https://schema.org/Article)
- [SEO Best Practices](https://developers.google.com/search/docs/beginner/seo-starter-guide)

---

## 🐛 Решение проблем

### Статьи не отображаются

1. Проверь, что `published = true`
2. Проверь, что `published_at` установлена и не в будущем
3. Проверь RLS политики в Supabase

### Ошибка при загрузке статьи

1. Проверь, что slug правильный
2. Проверь консоль браузера на ошибки
3. Проверь логи Supabase

### Статьи не в sitemap

1. Проверь, что статьи опубликованы
2. Проверь, что `published_at` установлена
3. Проверь логи при генерации sitemap

---

**Готово!** Теперь у тебя есть полнофункциональная система FAQ/блога для публикации SEO статей! 🎉

