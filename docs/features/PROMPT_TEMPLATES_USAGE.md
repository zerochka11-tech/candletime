# 📝 Руководство по использованию промпт-шаблонов

## 🚀 Быстрый старт

### 1. Первоначальная настройка

Выполните SQL миграцию в Supabase SQL Editor:

```sql
-- Файл: supabase-prompt-templates-setup.sql
```

Это создаст таблицу `prompt_templates` и добавит системный шаблон по умолчанию.

### 2. Использование

#### Генерация статьи в простом режиме

1. Перейдите в админ-панель → Статьи
2. Нажмите "🤖 Сгенерировать статью"
3. Выберите режим "📝 Простой"
4. Заполните поля:
   - Тема статьи (обязательно, 10-200 символов)
   - Тип свечи (опционально)
   - Язык (русский/английский)
   - Категория (опционально)
5. Нажмите "Сгенерировать статью"

#### Генерация статьи с промпт-шаблоном

1. Перейдите в админ-панель → Статьи
2. Нажмите "🤖 Сгенерировать статью"
3. Выберите режим "🎨 Шаблон"
4. Выберите промпт-шаблон из списка
5. Заполните переменные шаблона (например: `topic`, `language`, `candleType`)
6. При необходимости просмотрите предпросмотр промпта
7. Нажмите "Сгенерировать статью"

### 3. Управление промпт-шаблонами

#### Просмотр списка шаблонов

1. Нажмите кнопку "📝 Шаблоны промптов" на странице статей
2. Просмотрите список всех доступных шаблонов
3. Используйте поиск для быстрого поиска шаблонов

#### Создание нового шаблона

1. Откройте "Управление промпт-шаблонами"
2. Нажмите "+ Создать шаблон"
3. Заполните форму:
   - **Название** - имя шаблона (минимум 3 символа)
   - **Описание** - краткое описание (опционально)
   - **Промпт-шаблон** - сам промпт с переменными (минимум 50 символов)
   - **По умолчанию** - установить как шаблон по умолчанию (опционально)
4. Нажмите "Создать шаблон"

#### Редактирование шаблона

1. Откройте "Управление промпт-шаблонами"
2. Найдите нужный шаблон
3. Нажмите "Редактировать"
4. Внесите изменения
5. Нажмите "Сохранить изменения"

**Примечание:** Системные шаблоны нельзя редактировать или удалять.

#### Удаление шаблона

1. Откройте "Управление промпт-шаблонами"
2. Найдите нужный шаблон (только пользовательские шаблоны)
3. Нажмите "Удалить"
4. Подтвердите удаление

**Примечание:** Системные шаблоны нельзя удалять.

## 📚 Создание промпт-шаблона

### Синтаксис переменных

Используйте переменные в формате `{variableName}`:

```markdown
Ты - эксперт по написанию SEO-статей для сайта CandleTime.

Напиши статью на тему "{topic}" на языке {language}.

{ctaSection}

Требования:
- Длина: 1200-1800 слов
- Формат: Markdown
- Тон: спокойный, теплый
```

### Доступные переменные

#### Обязательные:
- `{topic}` - тема статьи (обязательна для всех шаблонов)

#### Опциональные:
- `{language}` - язык статьи (`ru` или `en`)
- `{candleType}` - тип свечи (`calm`, `support`, `memory`, `gratitude`, `focus`)
- `{categoryName}` - название категории
- `{ctaSection}` - автоматически генерируемая секция призыва к действию (если указан `candleType`)
- `{candleTypeCTA}` - текст для CTA (если указан `candleType`)

#### Кастомные переменные

Вы можете создать свои переменные. Просто используйте `{myVariable}` в промпте, и система автоматически создаст поле для заполнения в форме.

### Примеры промпт-шаблонов

#### Пример 1: Простой шаблон

```markdown
Напиши SEO-статью на тему "{topic}" на языке {language}.

Статья должна быть:
- Длиной 1200-1800 слов
- В формате Markdown
- Оптимизирована для SEO
- Содержать практические советы

{ctaSection}
```

#### Пример 2: Расширенный шаблон

```markdown
Ты - эксперт по написанию SEO-оптимизированных статей для сайта CandleTime.

Задача: Напиши статью на тему "{topic}".

Параметры:
- Язык: {language}
- Тип свечи: {candleType}
- Категория: {categoryName}

Требования:
- Длина: 1500-2000 слов
- Формат: Markdown с H1, H2, H3
- Тон: спокойный, теплый, человечный
- Структура: введение, основной контент, практические советы, заключение

{ctaSection}

Важно:
- Используй реальные примеры
- Избегай клише
- Пиши естественно

Верни только Markdown контент статьи.
```

## 🔧 Технические детали

### API Endpoints

#### Получить список шаблонов
```
GET /api/admin/prompt-templates
```

#### Создать шаблон
```
POST /api/admin/prompt-templates
Body: {
  name: string,
  description?: string,
  prompt: string,
  is_default?: boolean
}
```

#### Получить шаблон
```
GET /api/admin/prompt-templates/[id]
```

#### Обновить шаблон
```
PATCH /api/admin/prompt-templates/[id]
Body: {
  name?: string,
  description?: string,
  prompt?: string,
  is_default?: boolean
}
```

#### Удалить шаблон
```
DELETE /api/admin/prompt-templates/[id]
```

### Генерация статьи с шаблоном

```
POST /api/admin/articles/generate
Body: {
  useTemplate: true,
  templateId: string,
  templateVariables: {
    topic: string,
    language?: string,
    candleType?: string,
    ...
  },
  categoryId?: string
}
```

## 💡 Советы

1. **Используйте описательные названия** для шаблонов, чтобы легко находить нужный
2. **Добавляйте описания** к шаблонам для лучшей навигации
3. **Тестируйте шаблоны** перед установкой как шаблон по умолчанию
4. **Используйте переменные** для гибкости, но не переусердствуйте
5. **Сохраняйте копии** важных шаблонов перед редактированием

## ❓ FAQ

**Q: Можно ли использовать несколько шаблонов для одной статьи?**
A: Нет, каждый запрос использует один шаблон. Но вы можете создать шаблон, который объединяет несколько подходов.

**Q: Что делать, если шаблон не работает как ожидалось?**
A: Проверьте:
- Все переменные заполнены
- Промпт содержит обязательную переменную `{topic}`
- Синтаксис переменных правильный (фигурные скобки)

**Q: Можно ли экспортировать/импортировать шаблоны?**
A: Пока нет, но это можно добавить в будущем. Сейчас можно скопировать промпт вручную.

**Q: Как удалить системный шаблон?**
A: Системные шаблоны нельзя удалять, но вы можете создать свой шаблон и установить его как шаблон по умолчанию.

---

**Дата создания:** 2025-01-27
**Версия:** 1.0

