# 🔍 SEO оптимизация CandleTime

## ✅ Что реализовано

### 1. Мета-теги (Open Graph, Twitter Cards)
- ✅ Базовые мета-теги в `app/layout.tsx`
- ✅ Open Graph теги для всех страниц
- ✅ Twitter Cards для всех страниц
- ✅ Динамические мета-теги для страниц свечей (`/candle/[id]`)

### 2. Структурированные данные (JSON-LD)
- ✅ Schema.org для организации (в `app/layout.tsx`)
- ✅ Schema.org для главной страницы (WebSite)
- ✅ Schema.org для страниц свечей (CreativeWork)

### 3. Sitemap
- ✅ Автоматическая генерация sitemap (`app/sitemap.ts`)
- ✅ Доступен по адресу: `https://candletime.ru/sitemap.xml`

### 4. Robots.txt
- ✅ Автоматическая генерация robots.txt (`app/robots.ts`)
- ✅ Доступен по адресу: `https://candletime.ru/robots.txt`
- ✅ Исключает `/dashboard` и `/api/` из индексации

## 📝 Настройка

### Переменная окружения

Добавь в `.env.local` и в Vercel:

```env
NEXT_PUBLIC_SITE_URL=https://candletime.ru
```

Если переменная не установлена, по умолчанию используется `https://candletime.ru`.

## 🔍 Проверка SEO

### 1. Проверка мета-тегов
- Открой страницу в браузере
- Просмотри исходный код (Ctrl+U)
- Проверь наличие `<meta>` тегов с `og:` и `twitter:`

### 2. Проверка структурированных данных
- Используй [Google Rich Results Test](https://search.google.com/test/rich-results)
- Или [Schema.org Validator](https://validator.schema.org/)

### 3. Проверка sitemap
- Открой `https://candletime.ru/sitemap.xml`
- Убедись, что все страницы включены

### 4. Проверка robots.txt
- Открой `https://candletime.ru/robots.txt`
- Убедись, что правила корректны

## 📊 Страницы с мета-тегами

1. **Главная** (`/`) - базовые мета-теги + структурированные данные
2. **Зажечь свечу** (`/light`) - мета-теги через layout
3. **Все свечи** (`/candles`) - мета-теги через layout
4. **Мои свечи** (`/dashboard`) - мета-теги через layout
5. **Страница свечи** (`/candle/[id]`) - динамические мета-теги через `DynamicMeta` компонент
6. **Вход** (`/auth/login`) - мета-теги через layout
7. **Регистрация** (`/auth/signup`) - мета-теги через layout

## 🎯 Следующие шаги (опционально)

1. **Создать OG изображение** (`/public/og-image.png`)
   - Размер: 1200x630px
   - Формат: PNG или JPG
   - Добавить логотип и описание проекта

2. **Добавить favicon для разных устройств**
   - `favicon.ico` (16x16, 32x32)
   - `apple-touch-icon.png` (180x180)
   - `manifest.json` для PWA

3. **Настроить Google Search Console**
   - Добавить сайт
   - Отправить sitemap
   - Мониторить индексацию

4. **Добавить hreflang теги** (если планируется мультиязычность)

## 📚 Полезные ссылки

- [Next.js Metadata API](https://nextjs.org/docs/app/api-reference/functions/generate-metadata)
- [Open Graph Protocol](https://ogp.me/)
- [Twitter Cards](https://developer.twitter.com/en/docs/twitter-for-websites/cards/overview/abouts-cards)
- [Schema.org](https://schema.org/)
- [Google Search Central](https://developers.google.com/search)

