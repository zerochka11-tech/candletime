# Полное руководство по деплою CandleTime в продакшн

## 📋 Содержание
1. [Подготовка проекта](#1-подготовка-проекта)
2. [Настройка Supabase](#2-настройка-supabase)
3. [Деплой на Vercel](#3-деплой-на-vercel)
4. [Покупка домена](#4-покупка-домена)
5. [Подключение домена](#5-подключение-домена)
6. [Финальная настройка](#6-финальная-настройка)
7. [Проверка работы](#7-проверка-работы)

---

## 1. Подготовка проекта

### 1.1. Создание файла .env.local
Создай файл `.env.local` в корне проекта (если его еще нет):

```env
NEXT_PUBLIC_SUPABASE_URL=твой_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=твой_supabase_anon_key
```

**Важно:** Этот файл уже должен быть в `.gitignore` (не коммитится в Git).

### 1.2. Проверка сборки проекта
Убедись, что проект собирается без ошибок:

```bash
npm run build
```

Если есть ошибки — исправь их перед деплоем.

### 1.3. Инициализация Git (если еще не сделано)
```bash
git init
git add .
git commit -m "Initial commit"
```

---

## 2. Настройка Supabase

### 2.1. Создание проекта в Supabase
1. Перейди на [supabase.com](https://supabase.com)
2. Зарегистрируйся или войди в аккаунт
3. Нажми **"New Project"**
4. Заполни данные:
   - **Name:** CandleTime (или любое другое)
   - **Database Password:** придумай надежный пароль (сохрани его!)
   - **Region:** выбери ближайший регион (например, `West EU` для России)
   - **Pricing Plan:** Free (для начала)

5. Нажми **"Create new project"**
6. Подожди 2-3 минуты, пока проект создается

### 2.2. Получение ключей Supabase
1. В проекте Supabase перейди в **Settings** → **API**
2. Скопируй:
   - **Project URL** → это `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** key → это `NEXT_PUBLIC_SUPABASE_ANON_KEY`

3. Добавь их в `.env.local`:
```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 2.3. Создание таблицы candles
1. В Supabase перейди в **SQL Editor**
2. Выполни следующий SQL:

```sql
-- Создание таблицы candles
CREATE TABLE IF NOT EXISTS candles (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  title TEXT NOT NULL,
  message TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL,
  expires_at TIMESTAMPTZ NOT NULL,
  status TEXT DEFAULT 'active' NOT NULL,
  is_anonymous BOOLEAN DEFAULT false NOT NULL,
  candle_type TEXT,
  duration_hours INTEGER,
  user_id UUID REFERENCES auth.users(id) ON DELETE SET NULL
);

-- Индексы для оптимизации
CREATE INDEX IF NOT EXISTS idx_candles_expires_at ON candles(expires_at);
CREATE INDEX IF NOT EXISTS idx_candles_user_id ON candles(user_id);
CREATE INDEX IF NOT EXISTS idx_candles_created_at ON candles(created_at);
CREATE INDEX IF NOT EXISTS idx_candles_status ON candles(status);

-- Row Level Security (RLS) политики
ALTER TABLE candles ENABLE ROW LEVEL SECURITY;

-- Политика: все могут читать все свечи
CREATE POLICY "Anyone can read candles"
  ON candles FOR SELECT
  USING (true);

-- Политика: все могут создавать свечи
CREATE POLICY "Anyone can create candles"
  ON candles FOR INSERT
  WITH CHECK (true);

-- Политика: только владелец может обновлять свою свечу
CREATE POLICY "Users can update own candles"
  ON candles FOR UPDATE
  USING (auth.uid() = user_id);

-- Политика: только владелец может удалять свою свечу
CREATE POLICY "Users can delete own candles"
  ON candles FOR DELETE
  USING (auth.uid() = user_id);
```

3. Нажми **Run** и убедись, что запрос выполнен успешно

### 2.4. Настройка аутентификации
1. В Supabase перейди в **Authentication** → **Settings**
2. Включи **Email** провайдер (должен быть включен по умолчанию)
3. Опционально: настрой **Site URL** (пока можно оставить пустым, настроим после деплоя)

---

## 3. Деплой на Vercel

### 3.1. Регистрация на Vercel
1. Перейди на [vercel.com](https://vercel.com)
2. Зарегистрируйся через GitHub (рекомендуется) или email
3. Подтверди email, если нужно

### 3.2. Подключение GitHub репозитория
1. Если проект еще не на GitHub:
   - Создай репозиторий на [github.com](https://github.com)
   - Загрузи код:
   ```bash
   git remote add origin https://github.com/твой-username/candletime.git
   git branch -M main
   git push -u origin main
   ```

2. В Vercel:
   - Нажми **"Add New Project"**
   - Выбери репозиторий с проектом
   - Нажми **"Import"**

### 3.3. Настройка проекта в Vercel
1. **Framework Preset:** Next.js (определится автоматически)
2. **Root Directory:** `./` (оставь как есть)
3. **Build Command:** `npm run build` (по умолчанию)
4. **Output Directory:** `.next` (по умолчанию)
5. **Install Command:** `npm install` (по умолчанию)

### 3.4. Добавление переменных окружения
1. В разделе **Environment Variables** добавь:
   - `NEXT_PUBLIC_SUPABASE_URL` = твой Supabase URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = твой Supabase anon key

2. Убедись, что переменные добавлены для всех окружений:
   - ✅ Production
   - ✅ Preview
   - ✅ Development

3. Нажми **"Deploy"**

### 3.5. Ожидание деплоя
- Деплой займет 2-5 минут
- После завершения получишь URL вида: `candletime.vercel.app`

---

## 4. Покупка домена

### 4.1. Выбор регистратора
Рекомендуемые для .ru доменов:
- **Reg.ru** (reg.ru) - популярный в России
- **Timeweb** (timeweb.com) - хорошие цены
- **Яндекс.Коннект** (connect.yandex.ru) - интеграция с Яндекс

Для международных доменов:
- **Namecheap** (namecheap.com)
- **GoDaddy** (godaddy.com)
- **Cloudflare** (cloudflare.com) - лучшие цены

### 4.2. Покупка домена
1. Зайди на сайт регистратора
2. Проверь доступность домена (например, `candletime.ru`)
3. Добавь домен в корзину
4. Оформи заказ и оплати
5. Подожди активации (обычно 5-30 минут)

**Стоимость:** 
- `.ru` домен: ~200-500₽/год
- `.com` домен: ~$10-15/год

---

## 5. Подключение домена

### 5.1. В Vercel
1. В проекте Vercel перейди в **Settings** → **Domains**
2. Введи свой домен (например, `candletime.ru`)
3. Нажми **"Add"**

### 5.2. Настройка DNS записей
Vercel покажет, какие DNS записи нужно добавить. Обычно это:

**Вариант 1: Поддомен (www.candletime.ru)**
```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

**Вариант 2: Корневой домен (candletime.ru)**
```
Type: A
Name: @
Value: 76.76.21.21
```

Или используй CNAME через ALIAS (если поддерживается):
```
Type: ALIAS или CNAME
Name: @
Value: cname.vercel-dns.com
```

### 5.3. В панели регистратора домена
1. Зайди в панель управления доменом
2. Перейди в раздел **DNS** или **Управление DNS**
3. Добавь записи, которые показал Vercel
4. Сохрани изменения

### 5.4. Ожидание распространения DNS
- Обычно занимает 5-60 минут
- Может занять до 24 часов (редко)
- Проверить можно на [whatsmydns.net](https://www.whatsmydns.net)

### 5.5. Проверка в Vercel
1. После распространения DNS Vercel автоматически выдаст SSL сертификат
2. Статус домена изменится на **"Valid Configuration"**
3. Сайт будет доступен по твоему домену!

---

## 6. Финальная настройка

### 6.1. Обновление Site URL в Supabase
1. В Supabase перейди в **Authentication** → **URL Configuration**
2. Добавь в **Site URL:** `https://candletime.ru` (твой домен)
3. В **Redirect URLs** добавь:
   - `https://candletime.ru/**`
   - `https://www.candletime.ru/**` (если используешь www)
4. Сохрани изменения

### 6.2. Обновление метаданных в проекте
Проверь файл `app/layout.tsx`:
- Title должен быть правильным
- Description актуальна
- Favicon настроен

### 6.3. Настройка редиректов (опционально)
Если хочешь редирект с www на без www (или наоборот), в Vercel:
1. **Settings** → **Domains**
2. Настрой редиректы

---

## 7. Проверка работы

### 7.1. Базовые проверки
- [ ] Сайт открывается по домену
- [ ] HTTPS работает (замочек в браузере)
- [ ] Главная страница загружается
- [ ] Можно зажечь свечу
- [ ] Можно посмотреть свечи
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Dashboard работает

### 7.2. Проверка Supabase
- [ ] Свечи создаются в базе
- [ ] Свечи отображаются на странице /candles
- [ ] Аутентификация работает
- [ ] Dashboard показывает свечи пользователя

### 7.3. Производительность
- Проверь скорость загрузки на [PageSpeed Insights](https://pagespeed.web.dev/)
- Проверь мобильную версию
- Проверь разные браузеры

---

## 🎯 Быстрый чеклист

- [ ] Создан проект в Supabase
- [ ] Создана таблица candles
- [ ] Настроены RLS политики
- [ ] Проект загружен на GitHub
- [ ] Проект задеплоен на Vercel
- [ ] Добавлены переменные окружения в Vercel
- [ ] Куплен домен
- [ ] Настроены DNS записи
- [ ] Домен подключен в Vercel
- [ ] Обновлен Site URL в Supabase
- [ ] Все функции работают

---

## 💰 Примерная стоимость

**Бесплатно:**
- Vercel (Hobby план) - бесплатно
- Supabase (Free план) - бесплатно (до 500MB БД, 2GB bandwidth)

**Платно:**
- Домен `.ru`: ~200-500₽/год
- Домен `.com`: ~$10-15/год

**Итого:** от 200₽/год (только домен)

---

## 🔧 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Supabase Documentation](https://supabase.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [DNS Checker](https://www.whatsmydns.net)

---

## ⚠️ Важные замечания

1. **Не коммить `.env.local`** - он уже в `.gitignore`
2. **Сохрани пароли** - Supabase database password, ключи API
3. **Backup базы данных** - настрой автоматические бэкапы в Supabase
4. **Мониторинг** - следи за использованием ресурсов в Supabase
5. **Обновления** - регулярно обновляй зависимости (`npm update`)

---

## 🆘 Решение проблем

### Проблема: Сайт не открывается
- Проверь DNS записи (может быть еще не распространились)
- Проверь статус домена в Vercel
- Проверь SSL сертификат

### Проблема: Ошибки с Supabase
- Проверь переменные окружения в Vercel
- Проверь RLS политики в Supabase
- Проверь логи в Vercel (Deployments → View Function Logs)

### Проблема: Ошибки сборки
- Проверь логи в Vercel
- Убедись, что все зависимости установлены
- Проверь TypeScript ошибки локально

---

## 📞 Поддержка

Если что-то не работает:
1. Проверь логи в Vercel
2. Проверь логи в Supabase
3. Проверь консоль браузера (F12)
4. Обратись в поддержку Vercel/Supabase

