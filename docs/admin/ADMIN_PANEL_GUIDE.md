# 🔐 Админ-панель CandleTime

Руководство по использованию админ-панели для управления статьями и настройками сайта CandleTime.

## 🚀 Настройка

### 1. Переменные окружения

Добавьте следующие переменные в `.env.local`:

```env
# Supabase Service Role Key (для обхода RLS в API routes)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Email адреса администраторов (через запятую)
NEXT_PUBLIC_ADMIN_EMAILS=admin@example.com,another@example.com
```

**Важно:**
- `SUPABASE_SERVICE_ROLE_KEY` можно найти в Supabase Dashboard → Settings → API → Service Role Key
- Никогда не коммитьте этот ключ в репозиторий!
- `NEXT_PUBLIC_ADMIN_EMAILS` — список email адресов пользователей, которые имеют доступ к админ-панели

### 2. Доступ к админ-панели

Админ-панель доступна по следующим адресам:
- `/admin/articles` — управление статьями
- `/admin/settings` — настройки сайта

Доступ имеют только пользователи, email которых указан в `NEXT_PUBLIC_ADMIN_EMAILS`.

## 📋 Функциональность

### Список статей (`/admin/articles`)

- **Просмотр всех статей** — опубликованных и черновиков
- **Фильтрация** по статусу:
  - Все статьи
  - Опубликованные
  - Черновики
- **Статистика** — общее количество статей, опубликованных и черновиков
- **Импорт статей** — загрузка статей из файлов `seo-articles/*.md`
- **Быстрые действия**:
  - Просмотр статьи
  - Опубликовать/снять с публикации

### Просмотр статьи (`/admin/articles/[id]`)

- **Предпросмотр** статьи с рендерингом Markdown
- **Статистика**:
  - Количество просмотров
  - Время чтения
  - Даты создания и обновления
  - Slug
- **Редактирование метаданных**:
  - SEO Title
  - SEO Description
  - SEO Keywords
  - Excerpt (краткое описание)
- **Управление публикацией**:
  - Опубликовать статью
  - Снять с публикации
  - Просмотр на сайте (если опубликована)

### Настройки сайта (`/admin/settings`)

Раздел для управления настройками сайта, включая рождественскую тему.

**Функциональность:**
- **Управление рождественской темой** — включение/выключение новогоднего оформления
- **Отображение текущего состояния** — визуальная индикация активных настроек
- **Автоматическое обновление** — изменения применяются сразу после сохранения

**Как использовать:**
1. Перейдите в `/admin/settings` (или нажмите кнопку "Настройки" в профиле пользователя)
2. Найдите раздел "Рождественская тема"
3. Переключите переключатель для включения/выключения темы
4. Изменения применяются автоматически

**Примечания:**
- Рождественская тема включает анимацию снежинок на главной странице
- При включенной теме отображается блок "Загадай желание на 2026 год" на главной странице
- Плавающая кнопка для загадывания желаний появляется в правом нижнем углу экрана
- Пользователи увидят изменения в течение 30 секунд (автообновление через хук `useChristmasTheme`)

**Подробнее:** См. [Рождественская тема](../features/CHRISTMAS_THEME.md)

## 🔄 Рабочий процесс

### 1. Генерация статьи

```bash
npm run generate:seo -- \
  --slug my-new-article \
  --title "Заголовок статьи" \
  --h1 "Заголовок статьи" \
  --candleType gratitude \
  --language ru
```

Статья будет сохранена в `seo-articles/my-new-article.md`

### 2. Импорт в базу данных

1. Откройте `/admin/articles`
2. В разделе "📁 Файлы готовы к импорту" найдите нужный файл
3. Нажмите кнопку "Импортировать"
4. Статья будет создана в БД со статусом "Черновик"

### 3. Редактирование и публикация

1. Откройте статью из списка (кнопка "Просмотр")
2. Проверьте предпросмотр статьи
3. При необходимости отредактируйте метаданные (SEO Title, Description, Keywords, Excerpt)
4. Нажмите "Опубликовать" для публикации статьи

## 🔒 Безопасность

### Проверка прав доступа

Админ-панель проверяет права доступа на двух уровнях:

1. **Frontend** (`lib/admin.ts`):
   - Проверяет аутентификацию пользователя
   - Проверяет, что email пользователя в списке администраторов

2. **API Routes** (`app/api/admin/**`):
   - Проверяет токен авторизации
   - Использует Service Role Key для обхода RLS
   - Дополнительная проверка прав на уровне API

### Рекомендации по безопасности

- ✅ Используйте HTTPS в production
- ✅ Храните Service Role Key в секретах
- ✅ Ограничьте список администраторов минимально необходимым количеством
- ✅ Регулярно проверяйте логи доступа
- ✅ Используйте strong passwords для админ-аккаунтов

## 🛠 Технические детали

### Структура API

```
GET  /api/admin/articles/import      - Список файлов для импорта
POST /api/admin/articles/import      - Импорт статьи из файла
POST /api/admin/articles/[id]/approve - Апрув/отклонение статьи
```

### База данных

Админ-панель работает с таблицей `articles` в Supabase:

- `published` (boolean) — статус публикации
- `published_at` (timestamp) — дата публикации
- `seo_title`, `seo_description`, `seo_keywords` — SEO метаданные
- `excerpt` — краткое описание

### Импорт статей

При импорте статьи из файла:

1. Читается Markdown файл из `seo-articles/*.md`
2. Извлекается H1 как заголовок
3. Остальной контент становится содержимым статьи
4. Генерируется excerpt из первых 150 символов
5. Вычисляется время чтения (200 слов/минуту)
6. Статья создается со статусом "Черновик" (published = false)

Если статья с таким slug уже существует, она будет обновлена.

## 📝 Примеры использования

### Добавление нового администратора

1. Откройте `.env.local`
2. Добавьте email в `NEXT_PUBLIC_ADMIN_EMAILS`:
   ```env
   NEXT_PUBLIC_ADMIN_EMAILS=admin1@example.com,admin2@example.com,newadmin@example.com
   ```
3. Перезапустите dev сервер

### Публикация статьи с отложенной датой

1. Импортируйте статью
2. Откройте страницу статьи
3. Нажмите "Опубликовать"
4. В API можно передать `published_at` для отложенной публикации (требует доработки UI)

### Массовый импорт статей

Можно создать скрипт для массового импорта:

```typescript
// scripts/bulk-import.ts
import fs from 'fs';
import path from 'path';

const articlesDir = path.join(process.cwd(), 'seo-articles');
const files = fs.readdirSync(articlesDir).filter(f => f.endsWith('.md'));

// Импортируйте каждый файл через API
```

## 🐛 Решение проблем

### "Unauthorized" при доступе к админке

- Проверьте, что вы вошли в систему
- Убедитесь, что ваш email добавлен в `NEXT_PUBLIC_ADMIN_EMAILS`
- Проверьте формат переменной (email через запятую, без пробелов)

### Ошибка при импорте статьи

- Проверьте, что файл существует в `seo-articles/`
- Убедитесь, что файл содержит валидный Markdown
- Проверьте логи сервера для деталей ошибки

### Статья не публикуется

- Проверьте права доступа (Service Role Key)
- Убедитесь, что таблица `articles` существует
- Проверьте RLS политики в Supabase

## 📚 Дополнительные ресурсы

- [Документация по генератору статей](./SEO_ARTICLE_GENERATOR.md)
- [Структура базы данных](./PROJECT_COMPREHENSIVE_ANALYSIS.md)
- [Supabase Documentation](https://supabase.com/docs)

