# 📝 Процесс обновления документации

**Версия:** 1.0  
**Дата создания:** 2025-01-27

---

## 🎯 Цель

Поддержание технической документации в актуальном состоянии после каждого коммита.

---

## 🔄 Автоматическое обновление

### Git Hook

После каждого коммита автоматически запускается скрипт обновления документации, который:

1. ✅ Обновляет дату последнего обновления в документации
2. ✅ Проверяет изменения в коде
3. ✅ Предупреждает о необходимости обновления документации при изменениях в:
   - API endpoints (`app/api/`)
   - Типах (`lib/types.ts`)
   - SQL миграциях (`.sql` файлы)
   - Компонентах (`components/`)
   - Зависимостях (`package.json`)
   - Переменных окружения (`.env` файлы)

### Установка Git Hook

#### Linux/macOS

```bash
# Сделать скрипт исполняемым
chmod +x scripts/update-docs.sh

# Создать Git hook
mkdir -p .git/hooks
cat > .git/hooks/post-commit << 'EOF'
#!/bin/bash
exec scripts/update-docs.sh
EOF
chmod +x .git/hooks/post-commit
```

#### Windows (PowerShell)

```powershell
# Создать Git hook
New-Item -ItemType Directory -Force -Path .git\hooks | Out-Null
@"
#!/bin/sh
exec pwsh -File scripts/update-docs.ps1
"@ | Out-File -FilePath .git\hooks\post-commit -Encoding utf8
```

### Ручной запуск

```bash
# Linux/macOS
bash scripts/update-docs.sh

# Windows
pwsh scripts/update-docs.ps1
```

---

## 📋 Ручное обновление

### Когда обновлять документацию

Обновляйте документацию при следующих изменениях:

#### 1. Изменения в API

**Файлы для обновления:**
- `docs/API.md` — добавьте/обновите описание endpoint
- `docs/TECHNICAL_DOCUMENTATION.md` — обновите раздел "API Endpoints"

**Что обновить:**
- URL endpoint
- Параметры запроса
- Формат ответа
- Коды ошибок
- Примеры использования

#### 2. Изменения в базе данных

**Файлы для обновления:**
- `docs/TECHNICAL_DOCUMENTATION.md` — обновите раздел "База данных"

**Что обновить:**
- Новые таблицы
- Новые поля в существующих таблицах
- Изменения в индексах
- Изменения в RLS политиках

#### 3. Изменения в типах

**Файлы для обновления:**
- `docs/TECHNICAL_DOCUMENTATION.md` — обновите раздел "Компоненты и модули"

**Что обновить:**
- Новые типы в `lib/types.ts`
- Изменения в интерфейсах

#### 4. Новые компоненты

**Файлы для обновления:**
- `docs/TECHNICAL_DOCUMENTATION.md` — добавьте описание компонента

**Что добавить:**
- Назначение компонента
- Основные функции
- Примеры использования

#### 5. Изменения в зависимостях

**Файлы для обновления:**
- `docs/TECHNICAL_DOCUMENTATION.md` — обновите раздел "Технический стек"
- `package.json` — обновите версии

**Что обновить:**
- Версии библиотек
- Новые зависимости
- Удаленные зависимости

#### 6. Изменения в переменных окружения

**Файлы для обновления:**
- `docs/TECHNICAL_DOCUMENTATION.md` — обновите раздел "Переменные окружения"
- `docs/deployment/ENVIRONMENT_SETUP.md` — обновите инструкции

**Что обновить:**
- Новые переменные
- Изменения в обязательных/опциональных переменных
- Описание назначения переменных

---

## 📝 Шаблон обновления

### Для API Endpoint

```markdown
#### [METHOD] /api/[path]

[Краткое описание endpoint]

**Параметры:**
- `param1` (type, required/optional) — Описание
- `param2` (type, optional) — Описание

**Заголовки:**
```http
Authorization: Bearer <token>
```

**Пример запроса:**
```http
[METHOD] /api/[path]?param1=value
```

**Пример ответа:**
```json
{
  "field": "value"
}
```

**Ошибки:**
- `400` — Описание ошибки
- `500` — Описание ошибки
```

### Для компонента

```markdown
#### `ComponentName`

[Описание компонента]

**Файл:** `components/ComponentName.tsx`

**Функции:**
- `function1()` — Описание
- `function2()` — Описание

**Пропсы:**
- `prop1` (type) — Описание
- `prop2` (type, optional) — Описание
```

### Для таблицы БД

```markdown
#### Таблица `table_name`

[Описание таблицы]

**Поля:**

| Поле | Тип | Описание |
|------|-----|----------|
| `field1` | TYPE | Описание |
| `field2` | TYPE | Описание |

**Индексы:**
- `idx_field1` — Описание

**RLS Политики:**
- ✅ Описание политики
```

---

## ✅ Чеклист обновления

Перед коммитом проверьте:

- [ ] Обновлена дата последнего обновления
- [ ] Добавлены/обновлены описания новых API endpoints
- [ ] Обновлена документация по БД (если были изменения)
- [ ] Обновлены описания компонентов (если были изменения)
- [ ] Обновлен список зависимостей (если были изменения)
- [ ] Обновлены переменные окружения (если были изменения)
- [ ] Проверена актуальность примеров кода
- [ ] Проверены ссылки на другие документы

---

## 🔍 Проверка документации

### Автоматическая проверка

```bash
# Запуск скрипта обновления
bash scripts/update-docs.sh
```

### Ручная проверка

1. Проверьте, что все новые функции задокументированы
2. Проверьте, что примеры кода актуальны
3. Проверьте, что ссылки работают
4. Проверьте, что форматирование Markdown корректно

---

## 📚 Структура документации

```
docs/
├── TECHNICAL_DOCUMENTATION.md    # Главная техническая документация
├── API.md                         # Документация API
├── README.md                      # Индекс документации
├── DOCUMENTATION_UPDATE_PROCESS.md # Этот файл
└── [другие документы]
```

---

## 🚀 Быстрый старт

1. **После изменений в коде:**
   ```bash
   # Скрипт автоматически запустится после коммита
   git commit -m "Описание изменений"
   ```

2. **Если нужно обновить документацию вручную:**
   - Откройте соответствующий файл документации
   - Внесите изменения согласно шаблону
   - Обновите дату последнего обновления
   - Закоммитьте изменения

3. **Проверка:**
   ```bash
   bash scripts/update-docs.sh
   ```

---

## 💡 Советы

1. **Обновляйте документацию сразу** после изменений в коде
2. **Используйте примеры кода** для лучшего понимания
3. **Проверяйте ссылки** на другие документы
4. **Добавляйте диаграммы** для сложных концепций
5. **Обновляйте даты** при каждом изменении

---

**Последнее обновление:** 2025-01-27

