# 🚀 План оптимизации запросов для FAQ и Admin/Articles

## 📊 Анализ текущих проблем

### ⚠️ Критические проблемы производительности

#### 1. FAQ Page (`app/faq/page.tsx`)
**Проблемы:**
- ❌ Загружает **ВСЕ** статьи сразу без лимита (может быть сотни/тысячи)
- ❌ Сортировка и пагинация выполняются **на клиенте** после загрузки всех данных
- ❌ При поиске загружаются все результаты без ограничений
- ❌ Два отдельных запроса (категории + статьи) - можно оптимизировать
- ❌ Нет кеширования категорий (загружаются каждый раз)
- ❌ При изменении фильтра/поиска перезагружаются все данные

**Текущие запросы:**
```typescript
// Загружает ВСЕ статьи (может быть 1000+ записей)
const { data: articlesData } = await supabase
  .from('articles')
  .select(`...`) // Загружает все поля для всех статей
  .eq('published', true)
  // НЕТ .limit() и .range()
```

#### 2. FAQ Article Page (`app/faq/[slug]/page.tsx`)
**Проблемы:**
- ❌ **Последовательные запросы** (статья → похожие → навигация)
- ❌ Навигация загружает **ВСЕ статьи категории** только чтобы найти prev/next
- ❌ `increment_article_views` вызывается синхронно, блокирует рендер
- ❌ Три отдельных запроса можно объединить или распараллелить
- ❌ Дублирование запросов: layout.tsx тоже делает запрос для метаданных

**Текущие запросы:**
```typescript
// 1. Загрузка статьи
loadArticle() // ~200-500ms

// 2. После загрузки статьи - загрузка похожих
loadRelatedArticles() // ~100-300ms

// 3. После загрузки статьи - загрузка ВСЕЙ категории для навигации
loadNavigation() // Загружает ВСЕ статьи категории!
  .from('articles')
  .select('id, title, slug')
  .eq('category_id', article.category.id)
  // НЕТ .limit() - загружает все!
```

#### 3. Admin Articles Page (`app/admin/articles/page.tsx`)
**Проблемы:**
- ❌ `select('*')` загружает **весь контент** статей (может быть мегабайты)
- ❌ Нет пагинации - загружает все статьи сразу
- ❌ Фильтрация на клиенте после загрузки всех данных
- ❌ Отдельный запрос для файлов (можно объединить)
- ❌ Нет кеширования

**Текущие запросы:**
```typescript
// Загружает ВСЕ статьи с ВСЕМИ полями включая content
const { data } = await supabase
  .from('articles')
  .select('*') // ❌ Загружает content (может быть очень большой!)
  .order('created_at', { ascending: false });
  // НЕТ .limit() и .range()
```

#### 4. Admin Article Detail (`app/admin/articles/[id]/page.tsx`)
**Проблемы:**
- ✅ Относительно хорошо (один запрос для одной статьи)
- ⚠️ Можно добавить prefetching для списка статей

---

## 🎯 План оптимизации

### Приоритет 1: Критичные оптимизации (быстрый эффект) ⚡

#### 1.1. Server-side пагинация для FAQ Page
**Проблема:** Загружаются все статьи сразу
**Решение:** Использовать `.range()` и `.limit()` в Supabase запросах

**Ожидаемый результат:** 
- Уменьшение размера ответа с ~500KB до ~50KB (для 12 статей)
- Ускорение загрузки в 5-10 раз

**Изменения:**
```typescript
// БЫЛО:
const { data: articlesData } = await query;

// СТАНЕТ:
const startIndex = (currentPage - 1) * ARTICLES_PER_PAGE;
const endIndex = startIndex + ARTICLES_PER_PAGE - 1;

const { data: articlesData, count } = await query
  .range(startIndex, endIndex)
  .order('published_at', { ascending: false }); // Сортировка на сервере
```

#### 1.2. Оптимизация запросов в Admin Articles
**Проблема:** `select('*')` загружает content (очень большой)
**Решение:** Выбирать только нужные поля для списка

**Ожидаемый результат:**
- Уменьшение размера ответа с ~2MB до ~100KB (для 100 статей)
- Ускорение загрузки в 10-20 раз

**Изменения:**
```typescript
// БЫЛО:
.select('*')

// СТАНЕТ:
.select(`
  id,
  title,
  slug,
  excerpt,
  published,
  published_at,
  created_at,
  updated_at,
  views_count,
  reading_time
  -- НЕ загружаем content!
`)
```

#### 1.3. Оптимизация навигации в FAQ Article Page
**Проблема:** Загружает все статьи категории для навигации
**Решение:** Использовать SQL запрос с OFFSET/LIMIT или найти через id

**Ожидаемый результат:**
- Уменьшение запросов с загрузки всех статей до 2 запросов (prev/next)
- Ускорение в 5-10 раз

**Изменения:**
```typescript
// БЫЛО:
const { data: articles } = await supabase
  .from('articles')
  .select('id, title, slug')
  .eq('category_id', article.category.id)
  .order('published_at', { ascending: false }); // Загружает ВСЕ

// СТАНЕТ:
// Оптимальный вариант: использовать window functions или два отдельных запроса
const prev = await supabase
  .from('articles')
  .select('id, title, slug')
  .eq('category_id', article.category.id)
  .eq('published', true)
  .lt('published_at', article.published_at)
  .order('published_at', { ascending: false })
  .limit(1)
  .single();

const next = await supabase
  .from('articles')
  .select('id, title, slug')
  .eq('category_id', article.category.id)
  .eq('published', true)
  .gt('published_at', article.published_at)
  .order('published_at', { ascending: true })
  .limit(1)
  .single();
```

#### 1.4. Параллельные запросы в FAQ Article Page
**Проблема:** Последовательные запросы (статья → похожие → навигация)
**Решение:** Использовать `Promise.all()` для параллельных запросов

**Ожидаемый результат:**
- Уменьшение времени загрузки с 600-800ms до 200-300ms
- Ускорение в 2-3 раза

**Изменения:**
```typescript
// БЫЛО:
useEffect(() => {
  if (article) {
    incrementViews();
    addStructuredData();
    loadRelatedArticles(); // Ждем
    loadNavigation(); // Ждем
  }
}, [article]);

// СТАНЕТ:
useEffect(() => {
  if (article) {
    // Параллельная загрузка
    Promise.all([
      loadRelatedArticles(),
      loadNavigation()
    ]);
    // incrementViews не блокирует
    incrementViews();
    addStructuredData();
  }
}, [article]);
```

#### 1.5. Оптимизация поиска в FAQ Page
**Проблема:** При поиске загружаются все результаты
**Решение:** Добавить лимит и пагинацию для результатов поиска

**Ожидаемый результат:**
- Ограничение результатов поиска (например, первые 50)
- Более быстрый поиск

---

### Приоритет 2: Важные оптимизации (средний эффект) 🔧

#### 2.1. Кеширование категорий
**Проблема:** Категории загружаются каждый раз
**Решение:** Использовать React Query/SWR или localStorage

**Ожидаемый результат:**
- Категории загружаются один раз и кешируются
- Уменьшение запросов к БД

#### 2.2. Server-side сортировка
**Проблема:** Сортировка выполняется на клиенте
**Решение:** Переместить сортировку в Supabase запрос

**Ожидаемый результат:**
- База данных сортирует быстрее чем JavaScript
- Меньше данных передается по сети (после пагинации)

#### 2.3. Пагинация для Admin Articles
**Проблема:** Загружаются все статьи
**Решение:** Добавить server-side пагинацию (20-50 статей на страницу)

**Ожидаемый результат:**
- Быстрая загрузка админки даже при большом количестве статей

#### 2.4. Debounce для поиска
**Проблема:** Запрос при каждом изменении символа
**Решение:** Уже есть debounce 300ms, но можно оптимизировать

#### 2.5. Индексы базы данных
**Проблема:** Медленные запросы при фильтрации
**Решение:** Проверить и добавить недостающие индексы

**Индексы для проверки:**
```sql
-- Проверить существующие
-- Рекомендуемые индексы:
CREATE INDEX IF NOT EXISTS idx_articles_published_at ON articles(published_at DESC);
CREATE INDEX IF NOT EXISTS idx_articles_category_published ON articles(category_id, published, published_at);
CREATE INDEX IF NOT EXISTS idx_articles_search ON articles USING gin(to_tsvector('russian', title || ' ' || COALESCE(excerpt, '')));
```

---

### Приоритет 3: Дополнительные улучшения (долгосрочный эффект) 📈

#### 3.1. React Query / SWR для кеширования
**Проблема:** Нет кеширования запросов
**Решение:** Интегрировать React Query или SWR

**Ожидаемый результат:**
- Кеширование запросов
- Автоматическое обновление при изменении данных
- Optimistic updates

#### 3.2. Prefetching для ссылок
**Проблема:** Медленный переход между страницами
**Решение:** Использовать Next.js prefetching

**Ожидаемый результат:**
- Мгновенный переход на предзагруженные страницы

#### 3.3. Server Components для FAQ Page
**Проблема:** Все компоненты клиентские
**Решение:** Конвертировать в Server Components где возможно

**Ожидаемый результат:**
- Меньше JavaScript на клиенте
- Быстрее первый рендер

#### 3.4. Incremental Static Regeneration (ISR)
**Проблема:** Динамическая загрузка для всех статей
**Решение:** Использовать ISR для списка статей и метаданных

**Ожидаемый результат:**
- Статические страницы загружаются мгновенно
- Автоматическое обновление через заданный интервал

#### 3.5. Batch запросы
**Проблема:** Множество отдельных запросов
**Решение:** Объединить запросы где возможно

---

## 📋 Детальный план реализации

### Этап 1: Критичные оптимизации (1-2 дня)

#### 1.1. FAQ Page - Server-side пагинация
**Файл:** `app/faq/page.tsx`

**Изменения:**
- Убрать клиентскую сортировку и пагинацию
- Добавить server-side пагинацию с `.range()`
- Добавить server-side сортировку
- Добавить `count` для правильного отображения пагинации

**Код:**
```typescript
const loadData = async () => {
  try {
    setLoading(true);

    // Параллельная загрузка категорий и статей
    const startIndex = (currentPage - 1) * ARTICLES_PER_PAGE;
    const endIndex = startIndex + ARTICLES_PER_PAGE - 1;

    // Определяем порядок сортировки для сервера
    let orderColumn = 'published_at';
    let ascending = false;
    
    switch (sortBy) {
      case 'date-asc':
        orderColumn = 'published_at';
        ascending = true;
        break;
      case 'date-desc':
        orderColumn = 'published_at';
        ascending = false;
        break;
      case 'popularity':
        orderColumn = 'views_count';
        ascending = false;
        break;
      case 'reading-time':
        orderColumn = 'reading_time';
        ascending = true;
        break;
    }

    const [categoriesResult, articlesResult] = await Promise.all([
      supabase
        .from('article_categories')
        .select('id, name, slug')
        .order('name'),
      (() => {
        let query = supabase
          .from('articles')
          .select(`
            id,
            title,
            slug,
            excerpt,
            published_at,
            views_count,
            reading_time,
            featured_image_url,
            article_categories (
              id,
              name,
              slug
            )
          `, { count: 'exact' })
          .eq('published', true)
          .not('published_at', 'is', null)
          .lte('published_at', new Date().toISOString())
          .order(orderColumn, { ascending })
          .range(startIndex, endIndex);

        if (selectedCategory) {
          query = query.eq('category_id', selectedCategory);
        }

        return query;
      })()
    ]);

    if (categoriesResult.data) {
      setCategories(categoriesResult.data);
    }

    if (articlesResult.data) {
      const formattedArticles = articlesResult.data.map((article: any) => ({
        id: article.id,
        title: article.title,
        slug: article.slug,
        excerpt: article.excerpt,
        published_at: article.published_at,
        category: article.article_categories,
        views_count: article.views_count || 0,
        reading_time: article.reading_time,
        featured_image_url: article.featured_image_url,
      }));

      setArticles(formattedArticles);
      setTotalCount(articlesResult.count || 0);
    }
  } catch (error) {
    console.error('Error:', error);
  } finally {
    setLoading(false);
  }
};
```

#### 1.2. FAQ Page - Оптимизация поиска
**Файл:** `app/faq/page.tsx`

**Изменения:**
- Добавить лимит для результатов поиска (50 статей максимум)
- Добавить пагинацию для результатов поиска

#### 1.3. FAQ Article Page - Оптимизация навигации
**Файл:** `app/faq/[slug]/page.tsx`

**Изменения:**
- Заменить `loadNavigation()` на два отдельных запроса (prev/next)
- Использовать `Promise.all()` для параллельной загрузки

**Код:**
```typescript
const loadNavigation = async () => {
  if (!article?.category || !article.published_at) return;

  try {
    const [prevResult, nextResult] = await Promise.all([
      // Предыдущая статья (более старая)
      supabase
        .from('articles')
        .select('id, title, slug')
        .eq('category_id', article.category.id)
        .eq('published', true)
        .lt('published_at', article.published_at)
        .order('published_at', { ascending: false })
        .limit(1)
        .single(),
      
      // Следующая статья (более новая)
      supabase
        .from('articles')
        .select('id, title, slug')
        .eq('category_id', article.category.id)
        .eq('published', true)
        .gt('published_at', article.published_at)
        .order('published_at', { ascending: true })
        .limit(1)
        .single()
    ]);

    setNavigation({
      prev: prevResult.data || null,
      next: nextResult.data || null,
    });
  } catch (error) {
    console.error('Error loading navigation:', error);
  }
};
```

#### 1.4. FAQ Article Page - Параллельные запросы
**Файл:** `app/faq/[slug]/page.tsx`

**Изменения:**
- Загружать похожие статьи и навигацию параллельно
- Вынести `incrementViews` в background (не блокирует)

#### 1.5. Admin Articles - Убрать select('*')
**Файл:** `app/admin/articles/page.tsx`

**Изменения:**
- Выбирать только нужные поля (без content)
- Добавить пагинацию (50 статей на страницу)

**Код:**
```typescript
const loadArticles = async () => {
  try {
    const startIndex = (currentPage - 1) * ARTICLES_PER_PAGE;
    const endIndex = startIndex + ARTICLES_PER_PAGE - 1;

    let query = supabase
      .from('articles')
      .select(`
        id,
        title,
        slug,
        excerpt,
        published,
        published_at,
        created_at,
        updated_at,
        views_count,
        reading_time,
        seo_title,
        seo_description
        -- НЕ загружаем content!
      `, { count: 'exact' })
      .order('created_at', { ascending: false })
      .range(startIndex, endIndex);

    if (filter === 'published') {
      query = query.eq('published', true);
    } else if (filter === 'draft') {
      query = query.eq('published', false);
    }

    const { data, error, count } = await query;

    if (error) {
      console.error('Error loading articles:', error);
      return;
    }

    setArticles(data || []);
    setTotalCount(count || 0);
  } catch (error) {
    console.error('Error:', error);
  } finally {
    setLoading(false);
  }
};
```

---

### Этап 2: Важные оптимизации (2-3 дня)

#### 2.1. Кеширование категорий
**Файл:** `app/faq/page.tsx`

**Решение:** Использовать localStorage или React Query

#### 2.2. Индексы базы данных
**Файл:** Создать миграцию SQL

**SQL скрипт:**
```sql
-- Оптимизированные индексы для FAQ
CREATE INDEX IF NOT EXISTS idx_articles_published_at_desc 
  ON articles(published_at DESC) 
  WHERE published = true AND published_at IS NOT NULL;

CREATE INDEX IF NOT EXISTS idx_articles_category_published 
  ON articles(category_id, published, published_at DESC);

CREATE INDEX IF NOT EXISTS idx_articles_views_desc 
  ON articles(views_count DESC) 
  WHERE published = true;

CREATE INDEX IF NOT EXISTS idx_articles_reading_time 
  ON articles(reading_time) 
  WHERE published = true;

-- Полнотекстовый поиск (опционально, для лучшего поиска)
CREATE INDEX IF NOT EXISTS idx_articles_search 
  ON articles USING gin(
    to_tsvector('russian', 
      COALESCE(title, '') || ' ' || 
      COALESCE(excerpt, '') || ' ' || 
      COALESCE(content, '')
    )
  )
  WHERE published = true;
```

---

### Этап 3: Дополнительные улучшения (по желанию)

#### 3.1. React Query интеграция
- Установить `@tanstack/react-query`
- Обернуть запросы в useQuery hooks
- Настроить кеширование

#### 3.2. Server Components
- Конвертировать FAQ page в Server Component
- Использовать Suspense для загрузки данных

---

## 📊 Ожидаемые результаты

### До оптимизации:
- **FAQ Page:** ~2-5 секунд загрузки (при 100+ статьях)
- **FAQ Article:** ~600-800ms загрузка
- **Admin Articles:** ~3-10 секунд (при 100+ статьях с content)

### После оптимизации:
- **FAQ Page:** ~200-400ms загрузка (только 12 статей)
- **FAQ Article:** ~200-300ms загрузка (параллельные запросы)
- **Admin Articles:** ~300-500ms загрузка (без content, с пагинацией)

### Улучшения:
- ⚡ **Ускорение в 5-10 раз** для FAQ Page
- ⚡ **Ускорение в 2-3 раза** для FAQ Article Page
- ⚡ **Ускорение в 10-20 раз** для Admin Articles Page
- 📉 **Уменьшение трафика** на 80-90%
- 📉 **Уменьшение нагрузки на БД** на 70-80%

---

## 🔍 Приоритеты реализации

### Неделя 1: Критичные оптимизации
1. ✅ FAQ Page - Server-side пагинация
2. ✅ FAQ Article Page - Оптимизация навигации
3. ✅ FAQ Article Page - Параллельные запросы
4. ✅ Admin Articles - Убрать select('*')

### Неделя 2: Важные оптимизации
5. ✅ Кеширование категорий
6. ✅ Индексы базы данных
7. ✅ Server-side сортировка

### Неделя 3+: Дополнительные улучшения
8. ⚪ React Query интеграция
9. ⚪ Server Components
10. ⚪ ISR для статических страниц

---

## 📝 Примечания

- Все изменения обратно совместимы
- Можно реализовывать поэтапно
- После каждого этапа можно тестировать производительность
- Рекомендуется мониторить метрики производительности (Web Vitals)

