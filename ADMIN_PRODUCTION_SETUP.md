# 🔐 Настройка админ-панели на продакшене (candletime.ru)

## Проблема
Админ-панель не отображается на продакшене, потому что переменная окружения `NEXT_PUBLIC_ADMIN_EMAILS` не установлена в Vercel.

## Решение

### Шаг 1: Определи свой email администратора

Убедись, что ты:
1. Зарегистрирован на сайте candletime.ru
2. Знаешь точный email, который использовал при регистрации
3. Email должен быть в нижнем регистре (проверка чувствительна к регистру)

### Шаг 2: Добавь переменную в Vercel

1. Открой проект в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выбери проект `online-candles` (или как называется твой проект)
3. Перейди в **Settings** → **Environment Variables**
4. Нажми **"Add New"**
5. Заполни форму:
   - **Key:** `NEXT_PUBLIC_ADMIN_EMAILS`
   - **Value:** твой email (например: `admin@example.com`)
     - Для нескольких админов: `admin1@example.com,admin2@example.com` (через запятую, БЕЗ пробелов)
   - **Environment:** выбери все три чекбокса:
     - ✅ Production
     - ✅ Preview  
     - ✅ Development
6. Нажми **Save**

### Шаг 3: Добавь Service Role Key (для API)

1. Получи Service Role Key из Supabase:
   - Открой [Supabase Dashboard](https://app.supabase.com)
   - Выбери проект
   - Перейди в **Settings** → **API**
   - Скопируй **service_role** key (⚠️ НЕ anon key!)

2. Добавь в Vercel:
   - **Key:** `SUPABASE_SERVICE_ROLE_KEY`
   - **Value:** твой service_role key
   - **Environment:** все три (Production, Preview, Development)
   - **Save**

### Шаг 4: Перезапусти деплой

После добавления переменных нужно перезапустить деплой:

1. Перейди в **Deployments**
2. Найди последний деплой (Production)
3. Нажми на **"..."** (три точки) → **Redeploy**
4. Дождись завершения деплоя (2-5 минут)

### Шаг 5: Проверка

1. Открой сайт candletime.ru
2. **Войди в аккаунт** (используй тот email, который добавил в `NEXT_PUBLIC_ADMIN_EMAILS`)
3. Проверь следующие места:
   - ✅ В хедере (мобильное меню) должна появиться ссылка "🔐 Админ-панель"
   - ✅ На странице `/profile` должна быть карточка "Админ-панель"
   - ✅ На странице `/dashboard` должна быть кнопка "🔐 Админ-панель"
   - ✅ Прямой переход на `/admin/articles` должен работать

## ⚠️ Важные моменты

### Регистр email
Email проверяется **точным совпадением**. Убедись, что:
- Email в Vercel: `admin@example.com`
- Email при регистрации: `admin@example.com` (не `Admin@example.com` или `ADMIN@example.com`)

### Формат для нескольких админов
```env
NEXT_PUBLIC_ADMIN_EMAILS=admin1@example.com,admin2@example.com,admin3@example.com
```
**БЕЗ пробелов** после запятых!

### Переменные, которые должны быть в Vercel

Обязательные переменные для админ-панели:

1. ✅ `NEXT_PUBLIC_SUPABASE_URL` - URL проекта Supabase
2. ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY` - anon key из Supabase
3. ✅ `NEXT_PUBLIC_ADMIN_EMAILS` - **добавь эту переменную!**
4. ✅ `SUPABASE_SERVICE_ROLE_KEY` - service_role key из Supabase

## 🔍 Отладка

Если админ-панель все еще не видна:

1. **Проверь переменные в Vercel:**
   - Settings → Environment Variables
   - Убедись, что `NEXT_PUBLIC_ADMIN_EMAILS` есть и добавлена для Production

2. **Проверь email:**
   - Открой консоль браузера (F12)
   - Перейди на `/profile`
   - Посмотри, какой email у тебя в аккаунте
   - Сравни с email в `NEXT_PUBLIC_ADMIN_EMAILS`

3. **Проверь, что ты залогинен:**
   - Админ-панель видна только залогиненным пользователям
   - Убедись, что ты вошел в аккаунт

4. **Очисти кеш:**
   - Нажми Ctrl+Shift+R (или Cmd+Shift+R на Mac) для жесткой перезагрузки
   - Или открой сайт в режиме инкогнито

5. **Проверь логи Vercel:**
   - Deployments → последний деплой → Logs
   - Ищи ошибки связанные с переменными окружения

## 📝 Быстрая команда для проверки

После настройки переменных в Vercel, можешь проверить так:

1. Открой сайт candletime.ru
2. Войди в аккаунт
3. Открой консоль браузера (F12 → Console)
4. Выполни:
   ```javascript
   // Проверка текущего пользователя
   fetch('/api/auth/me').then(r => r.json()).then(console.log)
   ```

## 🆘 Если ничего не помогло

1. Проверь, что переменная точно называется `NEXT_PUBLIC_ADMIN_EMAILS` (с префиксом `NEXT_PUBLIC_`)
2. Убедись, что переменная добавлена для **Production** окружения
3. После изменения переменных **обязательно перезапусти деплой** (Redeploy)
4. Подожди 2-5 минут после перезапуска

---

**После выполнения всех шагов админ-панель должна появиться на продакшене!** ✅

